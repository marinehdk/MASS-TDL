# D1.8 — Malacca 全航路避碰演示 · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Malacca 近海群岛 ODD 下，本船接收 L2 `GncRoutePlan` 航路并沿计划航线航行；运行中按预设按钮注入 COLREGs 相遇（对头/交叉/追越），系统检测→分类→机动→归航全流程在海图可视。

**Architecture:** 后端纯函数几何生成器（CBDR 碰撞构造）按本船实时态算出目标生成几何；通过新增 `AddTarget` ROS2 service 在运行中注入 `target_vessel_node`；同一 `POST /encounters/inject` 端点既驱动前端按钮、也供无人值守脚本（测试台就绪）。航路经 `ship_interfaces/GncRoutePlan`（mock L2 发布器→可重入 L3 订阅器）流入，前端以虚线计划航路 + 实线实际航迹双层渲染。

**Tech Stack:** ROS2 (rclpy, ament_cmake/ament_python, rosidl) · FastAPI (sil_orchestrator) · pytest · React + MapLibre GL + RTK Query (web) · Playwright (E2E) · 部署 A4000 服务器（CLAUDE.md §13）。

**Spec:** [`D1.8-spec.md`](./D1.8-spec.md)

---

## 契约（并行开工前先锁 — 所有 stream 对此编码）

并行各 stream 只依赖以下契约的**形状**，不依赖彼此实现，故可对 mock 同时开发。

### C-1 几何生成器签名（Stream 1）
```python
# src/sil_orchestrator/encounter_geometry.py
@dataclass(frozen=True)
class OwnState:    lat: float; lon: float; heading_deg: float; sog_kn: float
@dataclass(frozen=True)
class TargetSpawn: lat: float; lon: float; course_deg: float; sog_kn: float

def generate_encounter(rule: str, own: OwnState,
                       range_nm: float | None = None,
                       construct_cpa_m: float | None = None,
                       approach_angle_deg: float | None = None) -> TargetSpawn
# rule ∈ {"head_on","crossing_giveway","crossing_standon","overtaking"}; 未知 → ValueError
```

### C-2 ROS2 接口（Stream 2）
```
# sil_msgs/srv/AddTarget.srv
uint32 mmsi
float64 lat
float64 lon
float64 heading_deg
float64 sog_kn
string  mode
---
bool   success
string message
```
```
# sil_msgs/srv/RemoveTarget.srv
uint32 mmsi
---
bool   success
string message
```
```
# ship_interfaces/msg/GncRoutePlan.msg   (用户给定契约 [R1]，勿改字段)
std_msgs/Header header
float64[] latitude
float64[] longitude
int32     total_waypoints
```
- Service 名（绝对）：`/target_vessel_node/add_target`、`/target_vessel_node/remove_target`
- Topic 名：`/route_planning/gnc_route_plan`（GncRoutePlan）；内部下游复用现有 `/l2/planned_route`

### C-3 REST 契约（Stream 6，前端 Stream 7 对此 mock）
```
POST /api/v1/encounters/inject
  body: {"rule": "head_on"|"crossing_giveway"|"crossing_standon"|"overtaking",
         "range_nm"?: number, "construct_cpa_m"?: number, "approach_angle_deg"?: number}
  200:  {"accepted": true, "mmsi": <int>}
  409:  {"detail": "<sim not active | own-ship state unavailable>"}
DELETE /api/v1/encounters/{mmsi}        200: {"removed": bool}
DELETE /api/v1/encounters               200: {"removed_count": int}
```

### C-4 默认预设参数（Stream 1 内嵌 + Stream 7 按钮）
| rule | b_rel | speed_factor | range_nm | construct_cpa_m | 模式 |
|---|---|---|---|---|---|
| head_on | 0° | 1.0 | 1.5 | 0 | intercept |
| crossing_giveway | +45°(右舷) | 1.0 | 2.0 | 300 | intercept |
| crossing_standon | −45°(左舷) | 1.0 | 2.0 | 300 | intercept |
| overtaking | 0° | 0.5 | 1.0 | 200 | overtake |

---

## 并行执行图（subagent-driven）

```
锁契约(本文档) ─┬─▶ Stream 1  encounter_geometry.py        [纯 Python · 零依赖 · 可立即并行]
               ├─▶ Stream 2  sil_msgs srv + ship_interfaces [ROS 接口 · 零依赖 · 立即并行]
               ├─▶ Stream 7  前端层 + api + 面板            [TS · 对 C-3 mock · 立即并行]
               └─▶ Stream 8  Malacca 场景 YAML              [独立 · 立即并行]
Stream 2 完成 ─┬─▶ Stream 3  gnc_route_mock_publisher.py    [依赖 GncRoutePlan]
               ├─▶ Stream 4  route_ingest_node.py           [依赖 GncRoutePlan]
               └─▶ Stream 5  target_vessel_node service     [依赖 AddTarget srv]
Stream 1+2+5 ─▶ Stream 6  orchestrator 集成(own_ship sub + client + router)  [集成节点]
全部完成     ─▶ Stream 9  E2E @ A4000                       [最后]
```
**可同时跑**：S1, S2, S7, S8（开局 4 路并行）。S2 落地后 S3/S4/S5 三路并行。S6 待 S1+S2+S5。S9 最后。

**构建/部署**：纯 Python（S1）本地 pytest。ROS 改动（S2-S6）按 CLAUDE.md §13：scp→A4000 容器内 `colcon build --packages-select <pkg>`→`restart sil-nodes`。前端（S7）本地 `cd web && npm run build`。

---

## Stream 1 — 几何生成器（纯函数，TDD 优先）

**Files:**
- Create: `src/sil_orchestrator/encounter_geometry.py`
- Test: `src/sil_orchestrator/tests/test_encounter_geometry.py`

### Task 1.1: 生成器核心 + 4 规则

- [ ] **Step 1: 写失败测试**

Create `src/sil_orchestrator/tests/test_encounter_geometry.py`:
```python
import math
import pytest
from sil_orchestrator.encounter_geometry import (
    OwnState, TargetSpawn, generate_encounter,
)

OWN = OwnState(lat=2.500, lon=101.000, heading_deg=0.0, sog_kn=12.0)  # 向正北


def _dcpa_m(own: OwnState, sp: TargetSpawn) -> float:
    """初始最近会遇距离(米)，用于断言构造几何。"""
    kn = 0.514444
    de = (sp.lon - own.lon) * math.cos(math.radians(own.lat)) * 111120.0
    dn = (sp.lat - own.lat) * 111120.0
    vo = (own.sog_kn*kn*math.sin(math.radians(own.heading_deg)),
          own.sog_kn*kn*math.cos(math.radians(own.heading_deg)))
    vt = (sp.sog_kn*kn*math.sin(math.radians(sp.course_deg)),
          sp.sog_kn*kn*math.cos(math.radians(sp.course_deg)))
    vr = (vt[0]-vo[0], vt[1]-vo[1])
    m = math.hypot(*vr)
    if m < 1e-6:
        return abs(de)
    return abs(de*vr[1] - dn*vr[0]) / m


def _spawn_east_m(own: OwnState, sp: TargetSpawn) -> float:
    return (sp.lon - own.lon) * math.cos(math.radians(own.lat)) * 111120.0


def test_head_on_reciprocal_collision():
    sp = generate_encounter("head_on", OWN)
    assert 175.0 <= sp.course_deg <= 185.0      # reciprocal of 0°
    assert 11.0 <= sp.sog_kn <= 13.0            # ≈ own speed
    assert _dcpa_m(OWN, sp) < 60.0              # 碰撞航线


def test_crossing_giveway_starboard():
    sp = generate_encounter("crossing_giveway", OWN)
    assert _spawn_east_m(OWN, sp) > 0.0         # 目标在右舷
    assert 260.0 <= sp.course_deg <= 280.0      # 自右向左横越
    assert 250.0 <= _dcpa_m(OWN, sp) <= 350.0   # ≈ construct_cpa 300


def test_crossing_standon_port():
    sp = generate_encounter("crossing_standon", OWN)
    assert _spawn_east_m(OWN, sp) < 0.0         # 目标在左舷
    assert 80.0 <= sp.course_deg <= 100.0       # 自左向右横越
    assert 250.0 <= _dcpa_m(OWN, sp) <= 350.0


def test_overtaking_slower_ahead_same_course():
    sp = generate_encounter("overtaking", OWN)
    assert (sp.course_deg <= 5.0 or sp.course_deg >= 355.0)  # 同向
    assert 5.5 <= sp.sog_kn <= 6.5              # 0.5 × own
    assert 150.0 <= _dcpa_m(OWN, sp) <= 250.0


def test_unknown_rule_raises():
    with pytest.raises(ValueError):
        generate_encounter("rule_42", OWN)


def test_override_range_and_cpa():
    sp = generate_encounter("crossing_giveway", OWN, range_nm=3.0, construct_cpa_m=500.0)
    assert 420.0 <= _dcpa_m(OWN, sp) <= 580.0
```

- [ ] **Step 2: 运行测试确认失败**

Run: `cd "src/sil_orchestrator" && python -m pytest tests/test_encounter_geometry.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'sil_orchestrator.encounter_geometry'`
（若 import 路径报错，从仓库根跑：`python -m pytest src/sil_orchestrator/tests/test_encounter_geometry.py -v`，并确保 `src` 在 `PYTHONPATH`。）

- [ ] **Step 3: 写最小实现**

Create `src/sil_orchestrator/encounter_geometry.py`:
```python
"""Encounter geometry generator (D1.8).

Pure functions — NO ROS imports. Given own-ship state + a COLREGs rule,
produce a target spawn (position + course + speed) on a CBDR collision
construction, so the avoidance stack is forced to act. Reusable by both
the REST endpoint and headless test-bed scripts.
"""
from __future__ import annotations

import math
from dataclasses import dataclass

NM_M = 1852.0
KN_MS = 0.514444
M_PER_DEG_LAT = 111120.0


@dataclass(frozen=True)
class OwnState:
    lat: float
    lon: float
    heading_deg: float  # true, 0=N, CW (use COG)
    sog_kn: float


@dataclass(frozen=True)
class TargetSpawn:
    lat: float
    lon: float
    course_deg: float
    sog_kn: float


# rule → (b_rel deg [+=starboard], target speed factor, default range NM,
#         default construct CPA m, mode)
_RULE_DEFAULTS: dict[str, dict] = {
    "head_on":          dict(b_rel=0.0,   speed_factor=1.0, range_nm=1.5,
                             construct_cpa_m=0.0,   mode="intercept"),
    "crossing_giveway": dict(b_rel=45.0,  speed_factor=1.0, range_nm=2.0,
                             construct_cpa_m=300.0, mode="intercept"),
    "crossing_standon": dict(b_rel=-45.0, speed_factor=1.0, range_nm=2.0,
                             construct_cpa_m=300.0, mode="intercept"),
    "overtaking":       dict(b_rel=0.0,   speed_factor=0.5, range_nm=1.0,
                             construct_cpa_m=200.0, mode="overtake"),
}


def _enu_from_brg(range_m: float, brg_deg: float) -> tuple[float, float]:
    a = math.radians(brg_deg)
    return range_m * math.sin(a), range_m * math.cos(a)  # east, north


def _course_deg(east: float, north: float) -> float:
    return (math.degrees(math.atan2(east, north)) + 360.0) % 360.0


def _offset_to_latlon(lat0, lon0, east_m, north_m):
    dlat = north_m / M_PER_DEG_LAT
    dlon = east_m / (M_PER_DEG_LAT * math.cos(math.radians(lat0)))
    return lat0 + dlat, lon0 + dlon


def generate_encounter(rule, own, range_nm=None, construct_cpa_m=None,
                       approach_angle_deg=None):
    if rule not in _RULE_DEFAULTS:
        raise ValueError(f"unknown rule: {rule!r}")
    d = _RULE_DEFAULTS[rule]
    R = (range_nm if range_nm is not None else d["range_nm"]) * NM_M
    cpa = construct_cpa_m if construct_cpa_m is not None else d["construct_cpa_m"]
    b_rel = approach_angle_deg if approach_angle_deg is not None else d["b_rel"]

    psi = math.radians(own.heading_deg)
    v0 = own.sog_kn * KN_MS
    voe, von = v0 * math.sin(psi), v0 * math.cos(psi)          # own velocity ENU
    se, sn = _enu_from_brg(R, own.heading_deg + b_rel)          # spawn rel ENU
    st = d["speed_factor"] * v0

    if d["mode"] == "overtake":
        vte, vtn = st * math.sin(psi), st * math.cos(psi)      # same course, slower
    else:  # intercept: fixed target speed, solve course so they collide (DCPA=0)
        a = se * se + sn * sn
        vo_dot_s = voe * se + von * sn
        c = (voe * voe + von * von) - st * st
        disc = vo_dot_s * vo_dot_s - a * c
        if a <= 0.0 or disc < 0.0:
            raise ValueError("no intercept solution for given geometry")
        x = (vo_dot_s + math.sqrt(disc)) / a                   # x = 1/T, take T>0 (smallest T)
        if x <= 0.0:
            raise ValueError("no positive intercept time")
        vte, vtn = voe - se * x, von - sn * x

    # impose construct CPA: shift spawn ⊥ to relative velocity by `cpa`
    vrele, vreln = vte - voe, vtn - von
    vrel_mag = math.hypot(vrele, vreln)
    if vrel_mag > 1e-6 and cpa != 0.0:
        pe, pn = -vreln / vrel_mag, vrele / vrel_mag           # unit ⊥ to v_rel
        se, sn = se + cpa * pe, sn + cpa * pn

    lat, lon = _offset_to_latlon(own.lat, own.lon, se, sn)
    return TargetSpawn(lat=lat, lon=lon,
                       course_deg=_course_deg(vte, vtn),
                       sog_kn=math.hypot(vte, vtn) / KN_MS)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `python -m pytest src/sil_orchestrator/tests/test_encounter_geometry.py -v`
Expected: PASS（7 passed）

- [ ] **Step 5: 提交**

```bash
git add src/sil_orchestrator/encounter_geometry.py src/sil_orchestrator/tests/test_encounter_geometry.py
git commit -m "feat(d1.8): CBDR encounter geometry generator (4 COLREGs presets, pure fn)"
```

---

## Stream 2 — ROS2 接口（srv + GncRoutePlan 包）

**Files:**
- Create: `src/sim_workbench/sil_msgs/srv/AddTarget.srv`, `.../srv/RemoveTarget.srv`
- Modify: `src/sim_workbench/sil_msgs/CMakeLists.txt`
- Create: `src/ship_interfaces/package.xml`, `.../CMakeLists.txt`, `.../msg/GncRoutePlan.msg`

### Task 2.1: sil_msgs 增 AddTarget / RemoveTarget srv

- [ ] **Step 1: 建 srv 文件**

Create `src/sim_workbench/sil_msgs/srv/AddTarget.srv`:
```
# Inject a target vessel at runtime (D1.8)
uint32 mmsi
float64 lat
float64 lon
float64 heading_deg
float64 sog_kn
string  mode
---
bool   success
string message
```
Create `src/sim_workbench/sil_msgs/srv/RemoveTarget.srv`:
```
# Remove an injected target by MMSI (D1.8)
uint32 mmsi
---
bool   success
string message
```

- [ ] **Step 2: 改 CMakeLists 注册 srv**

In `src/sim_workbench/sil_msgs/CMakeLists.txt`, after the `set(msg_files ...)` block add:
```cmake
set(srv_files
  "srv/AddTarget.srv"
  "srv/RemoveTarget.srv"
)
```
And change the `rosidl_generate_interfaces` call to include `${srv_files}`:
```cmake
rosidl_generate_interfaces(${PROJECT_NAME}
  ${msg_files}
  ${srv_files}
  DEPENDENCIES builtin_interfaces std_msgs geometry_msgs
)
```

- [ ] **Step 3: 构建并验证接口生成**（A4000，CLAUDE.md §13）

Run (A4000 容器内): `colcon build --packages-select sil_msgs && source install/setup.bash && ros2 interface show sil_msgs/srv/AddTarget`
Expected: 打印 AddTarget 的 request/response 字段（含 `uint32 mmsi` … `bool success`）。

- [ ] **Step 4: 提交**

```bash
git add src/sim_workbench/sil_msgs/srv/AddTarget.srv src/sim_workbench/sil_msgs/srv/RemoveTarget.srv src/sim_workbench/sil_msgs/CMakeLists.txt
git commit -m "feat(d1.8): sil_msgs AddTarget/RemoveTarget srv for runtime target injection"
```

### Task 2.2: 新建 ship_interfaces 包 + GncRoutePlan

> 现状：`src/ship_interfaces/` 仅有空 `msg/`，无 package.xml / CMakeLists。

- [ ] **Step 1: 建 GncRoutePlan.msg（用户契约，勿改字段）**

Create `src/ship_interfaces/msg/GncRoutePlan.msg`:
```
# L2 → L3 route plan (D1.8). Contract per L2 integration spec [R1].
std_msgs/Header header
float64[] latitude
float64[] longitude
int32     total_waypoints
```

- [ ] **Step 2: 建 package.xml**

Create `src/ship_interfaces/package.xml`:
```xml
<?xml version="1.0"?>
<package format="3">
  <name>ship_interfaces</name>
  <version>0.1.0</version>
  <description>Ship L1/L2 integration interfaces — GncRoutePlan and future L1/L2 messages</description>
  <maintainer email="marinehdk@gmail.com">MASS L3 Team</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>rosidl_default_generators</buildtool_depend>

  <build_depend>builtin_interfaces</build_depend>
  <build_depend>std_msgs</build_depend>

  <exec_depend>rosidl_default_runtime</exec_depend>
  <exec_depend>builtin_interfaces</exec_depend>
  <exec_depend>std_msgs</exec_depend>

  <member_of_group>rosidl_interface_packages</member_of_group>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
```

- [ ] **Step 3: 建 CMakeLists.txt**

Create `src/ship_interfaces/CMakeLists.txt`:
```cmake
cmake_minimum_required(VERSION 3.8)
project(ship_interfaces)

if(NOT CMAKE_CXX_STANDARD)
  set(CMAKE_CXX_STANDARD 17)
endif()

find_package(ament_cmake REQUIRED)
find_package(rosidl_default_generators REQUIRED)
find_package(builtin_interfaces REQUIRED)
find_package(std_msgs REQUIRED)

set(msg_files
  "msg/GncRoutePlan.msg"
)

rosidl_generate_interfaces(${PROJECT_NAME}
  ${msg_files}
  DEPENDENCIES builtin_interfaces std_msgs
)

ament_export_dependencies(rosidl_default_runtime)

ament_package()
```

- [ ] **Step 4: 构建并验证**（A4000）

Run: `colcon build --packages-select ship_interfaces && source install/setup.bash && ros2 interface show ship_interfaces/msg/GncRoutePlan`
Expected: 打印 `std_msgs/Header header` / `float64[] latitude` / `float64[] longitude` / `int32 total_waypoints`。

- [ ] **Step 5: 提交**

```bash
git add src/ship_interfaces/package.xml src/ship_interfaces/CMakeLists.txt src/ship_interfaces/msg/GncRoutePlan.msg
git commit -m "feat(d1.8): create ship_interfaces pkg + GncRoutePlan.msg (L2 route contract)"
```

---

## Stream 3 — Mock L2 GncRoutePlan 发布器（依赖 Stream 2）

**Files:**
- Create: `docker/gnc_route_mock_publisher.py`

> 镜像 `docker/mock_l2_publisher.py` 的"订阅 `/sil/lifecycle_status` → 读场景 YAML `ownShip.nominalRoute` → ACTIVE 时发布"逻辑，但发布**用户契约** `GncRoutePlan` 到 `/route_planning/gnc_route_plan`，TRANSIENT_LOCAL latched。

### Task 3.1: GncRoutePlan 发布节点

- [ ] **Step 1: 写节点**

Create `docker/gnc_route_mock_publisher.py`:
```python
#!/usr/bin/env python3
"""GNC Route Mock Publisher (D1.8) — stands in for real L2 in Phase 1.

Subscribes /sil/lifecycle_status; on ACTIVE reads the scenario YAML's
ownShip.nominalRoute and publishes it as ship_interfaces/GncRoutePlan on
/route_planning/gnc_route_plan (latched). Re-publishes the latest plan at
1 Hz so late subscribers receive it.
"""
import os
import signal
import sys

import rclpy
from rclpy.node import Node
from rclpy.qos import (QoSProfile, QoSReliabilityPolicy,
                       QoSDurabilityPolicy, QoSHistoryPolicy)
from std_msgs.msg import String, Header
from builtin_interfaces.msg import Time as BuiltinTime
from sil_msgs.msg import LifecycleStatus
from ship_interfaces.msg import GncRoutePlan

SCENARIO_DIR = os.environ.get("SIL_SCENARIO_DIR", "/var/sil/scenarios")
PUBLISH_HZ = 1.0
LC_STATE_ACTIVE = 3


def _now(node: Node) -> BuiltinTime:
    s, ns = node.get_clock().now().seconds_nanoseconds()
    return BuiltinTime(sec=s, nanosec=ns)


class GncRouteMockPublisher(Node):
    def __init__(self):
        super().__init__("gnc_route_mock_publisher")
        self.declare_parameter("scenario_dir", SCENARIO_DIR)
        self._scenario_dir = self.get_parameter("scenario_dir").value
        self._is_active = False
        self._current_scenario_id = ""
        self._waypoints: list[tuple[float, float]] = []

        route_qos = QoSProfile(
            reliability=QoSReliabilityPolicy.RELIABLE,
            durability=QoSDurabilityPolicy.TRANSIENT_LOCAL,
            history=QoSHistoryPolicy.KEEP_LAST, depth=5)
        self._pub = self.create_publisher(
            GncRoutePlan, "/route_planning/gnc_route_plan", route_qos)

        self.create_subscription(LifecycleStatus, "/sil/lifecycle_status",
                                 self._on_lifecycle, 10)
        self.create_subscription(String, "/sil/scenario_loaded",
                                 self._on_scenario_loaded, 10)
        self.create_timer(1.0 / PUBLISH_HZ, self._on_timer)
        self.get_logger().info(
            f"GncRouteMockPublisher ready (dir={self._scenario_dir})")

    def _on_lifecycle(self, msg: LifecycleStatus):
        if msg.current_state != LC_STATE_ACTIVE:
            self._is_active = False
            return
        sid = msg.scenario_id
        if sid and not (sid == self._current_scenario_id and self._is_active):
            self._current_scenario_id = sid
            self._load(sid)

    def _on_scenario_loaded(self, msg: String):
        if msg.data and not (msg.data == self._current_scenario_id and self._is_active):
            self._current_scenario_id = msg.data
            self._load(msg.data)

    def _load(self, scenario_id: str):
        yaml_path = None
        for root, _, files in os.walk(self._scenario_dir):
            if f"{scenario_id}.yaml" in files:
                yaml_path = os.path.join(root, f"{scenario_id}.yaml")
                break
        if not yaml_path:
            self.get_logger().warn(f"YAML not found for {scenario_id}")
            return
        try:
            import yaml
            with open(yaml_path) as f:
                scenario = yaml.safe_load(f)
        except Exception as exc:
            self.get_logger().error(f"parse {yaml_path}: {exc}")
            return
        nominal = scenario.get("ownShip", {}).get("nominalRoute")
        if not nominal or len(nominal) < 2:
            self.get_logger().warn(f"no nominalRoute in {scenario_id}")
            return
        self._waypoints = [(float(wp["latitude"]), float(wp["longitude"]))
                           for wp in nominal]
        self._is_active = True
        self.get_logger().info(
            f"GNC route ACTIVE — {len(self._waypoints)} wp from {scenario_id}")

    def _on_timer(self):
        if not self._is_active or not self._waypoints:
            return
        msg = GncRoutePlan()
        msg.header = Header(stamp=_now(self), frame_id="map")
        msg.latitude = [lat for lat, _ in self._waypoints]
        msg.longitude = [lon for _, lon in self._waypoints]
        msg.total_waypoints = len(self._waypoints)
        self._pub.publish(msg)


def main(args=None):
    rclpy.init(args=args)
    node = GncRouteMockPublisher()
    def _sig(_s, _f):
        node.destroy_node(); rclpy.shutdown(); sys.exit(0)
    signal.signal(signal.SIGINT, _sig)
    signal.signal(signal.SIGTERM, _sig)
    try:
        rclpy.spin(node)
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 验证（A4000，需 Stream 2 已 build + Malacca 场景 Stream 8）**

Run (A4000，载入 Malacca 场景并 ACTIVATE 后):
`ros2 topic echo /route_planning/gnc_route_plan --once`
Expected: 打印 `latitude:[...]`、`longitude:[...]`、`total_waypoints: 5`（=Malacca 航点数）。

- [ ] **Step 3: 提交**

```bash
git add docker/gnc_route_mock_publisher.py
git commit -m "feat(d1.8): GncRoutePlan mock L2 publisher (fixed route from scenario)"
```

> **集成接线（Stream 6/9 处理）**：把本节点加入 SIL 启动编排（与 `mock_l2_publisher.py` 同处）。Malacca 场景下令本节点为 `/route_planning/gnc_route_plan` 唯一写者。

---

## Stream 4 — 可重入航路接收订阅器（依赖 Stream 2）

**Files:**
- Create: `docker/route_ingest_node.py`

> L3 侧真正消费点（未来并入 M3）。订阅 `/route_planning/gnc_route_plan`，按 `header.stamp` **最新整条替换**旧航路（重入），并把航路转发到现有内部下游 `/l2/planned_route`（l3_external_msgs/PlannedRoute）供前端渲染；每收到新航路 log 一行。

### Task 4.1: 订阅器节点

- [ ] **Step 1: 写节点**

Create `docker/route_ingest_node.py`:
```python
#!/usr/bin/env python3
"""Route Ingest Node (D1.8) — L3-side consumer of L2 GncRoutePlan.

Subscribes /route_planning/gnc_route_plan, keeps the latest plan
(re-entrant: a newer stamp wholly replaces the old route), and forwards
it to the existing internal /l2/planned_route topic so the frontend route
layer renders it. This is the production seam for the real L2 link (later
merged into M3 Mission Manager).
"""
import signal
import sys

import rclpy
from rclpy.node import Node
from rclpy.qos import (QoSProfile, QoSReliabilityPolicy,
                       QoSDurabilityPolicy, QoSHistoryPolicy)
from ship_interfaces.msg import GncRoutePlan
from l3_external_msgs.msg import PlannedRoute  # existing downstream contract

_LATCHED = QoSProfile(
    reliability=QoSReliabilityPolicy.RELIABLE,
    durability=QoSDurabilityPolicy.TRANSIENT_LOCAL,
    history=QoSHistoryPolicy.KEEP_LAST, depth=5)


class RouteIngestNode(Node):
    def __init__(self):
        super().__init__("route_ingest_node")
        self._last_stamp_ns = -1
        self.create_subscription(
            GncRoutePlan, "/route_planning/gnc_route_plan",
            self._on_route, _LATCHED)
        self._pub = self.create_publisher(
            PlannedRoute, "/l2/planned_route", _LATCHED)
        self.get_logger().info("RouteIngestNode ready — subscribing GncRoutePlan")

    def _on_route(self, msg: GncRoutePlan):
        stamp_ns = msg.header.stamp.sec * 1_000_000_000 + msg.header.stamp.nanosec
        if stamp_ns < self._last_stamp_ns:
            return  # stale (older than current) — ignore
        replaced = self._last_stamp_ns >= 0 and stamp_ns > self._last_stamp_ns
        self._last_stamp_ns = stamp_ns
        n = msg.total_waypoints
        self.get_logger().info(
            f"{'REPLACED' if replaced else 'RECEIVED'} GncRoutePlan: "
            f"{n} waypoints (stamp={stamp_ns})")
        self._publish_internal(msg)

    def _publish_internal(self, msg: GncRoutePlan):
        # Convert GncRoutePlan → existing PlannedRoute. Field mapping is
        # verified against l3_external_msgs/PlannedRoute in Step 2 below.
        out = PlannedRoute()
        out.header = msg.header
        for lat, lon in zip(msg.latitude, msg.longitude):
            wp = self._make_waypoint(lat, lon)
            out.waypoints.append(wp)
        self._pub.publish(out)

    def _make_waypoint(self, lat: float, lon: float):
        # PlannedRoute waypoint construction — exact type filled in Step 2
        # after `ros2 interface show l3_external_msgs/msg/PlannedRoute`.
        from l3_external_msgs.msg import PlannedRoute as _PR
        wp_type = type(_PR().waypoints) and None  # placeholder removed in Step 2
        raise NotImplementedError  # replaced in Step 2


def main(args=None):
    rclpy.init(args=args)
    node = RouteIngestNode()
    def _sig(_s, _f):
        node.destroy_node(); rclpy.shutdown(); sys.exit(0)
    signal.signal(signal.SIGINT, _sig)
    signal.signal(signal.SIGTERM, _sig)
    try:
        rclpy.spin(node)
    finally:
        node.destroy_node()
        rclpy.shutdown()


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 核实 PlannedRoute 字段并补全 `_make_waypoint` / `_publish_internal`**

Run (A4000): `ros2 interface show l3_external_msgs/msg/PlannedRoute`
然后据其 `waypoints` 元素类型（很可能 `geographic_msgs/GeoPoint` 或自定义带 lat/lon 的元素）改写 `_make_waypoint`，删除 Step 1 中的 `raise NotImplementedError` 占位。范例（若元素为 `geographic_msgs/GeoPoint`）:
```python
def _make_waypoint(self, lat: float, lon: float):
    from geographic_msgs.msg import GeoPoint
    gp = GeoPoint()
    gp.latitude = lat
    gp.longitude = lon
    gp.altitude = 0.0
    return gp
```
> 参照 `docker/mock_l2_publisher.py` 中 `_publish_planned_route` 现有构造逻辑（它已正确填充 PlannedRoute），直接复用其 waypoint 构造代码以保证字段一致。

- [ ] **Step 3: 验证（A4000，Malacca 场景 ACTIVE）**

Run: `ros2 topic echo /l2/planned_route --once` 且 `ros2 node info /route_ingest_node`
Expected: `/l2/planned_route` 有 5 航点；订阅器日志出现 `RECEIVED GncRoutePlan: 5 waypoints`。重发新航路时日志出现 `REPLACED`。

- [ ] **Step 4: 提交**

```bash
git add docker/route_ingest_node.py
git commit -m "feat(d1.8): re-entrant route ingest node (GncRoutePlan -> internal PlannedRoute)"
```

---

## Stream 5 — target_vessel_node 运行时注入 service（依赖 Stream 2）

**Files:**
- Modify: `src/sim_workbench/sil_nodes/target_vessel/target_vessel/node.py`

### Task 5.1: 在 ACTIVE 暴露 AddTarget / RemoveTarget service

- [ ] **Step 1: 加 import（node.py 顶部，`from sil_msgs.msg import TargetVesselState` 之后）**
```python
from sil_msgs.srv import AddTarget, RemoveTarget
```

- [ ] **Step 2: `__init__` 加 service 句柄占位（在 `self._last_pub_wall_time` 行之前）**
```python
self._add_target_srv = None
self._remove_target_srv = None
```

- [ ] **Step 3: `on_activate` 创建 service（在 `self._timer = self.create_timer(...)` 之后、`return super().on_activate(state)` 之前）**
```python
self._add_target_srv = self.create_service(
    AddTarget, "/target_vessel_node/add_target", self._handle_add_target)
self._remove_target_srv = self.create_service(
    RemoveTarget, "/target_vessel_node/remove_target", self._handle_remove_target)
self._logger.info("AddTarget/RemoveTarget services up")
```

- [ ] **Step 4: `on_deactivate` 清理（在 publisher 清理之后）**
```python
for attr in ("_add_target_srv", "_remove_target_srv"):
    srv = getattr(self, attr)
    if srv is not None:
        self.destroy_service(srv)
        setattr(self, attr, None)
```

- [ ] **Step 5: 加 handler（在 `_step_callback` 方法之前）**
```python
def _handle_add_target(self, request, response):
    try:
        mode = request.mode if request.mode else "replay"
        self.add_target(
            mmsi=int(request.mmsi), lat=request.lat, lon=request.lon,
            heading_deg=request.heading_deg, sog_kn=request.sog_kn, mode=mode)
        response.success = True
        response.message = f"added MMSI {request.mmsi}"
        self._logger.info(f"Service add_target MMSI {request.mmsi}")
    except Exception as exc:  # noqa: BLE001
        response.success = False
        response.message = f"add_target error: {exc}"
        self._logger.error(response.message)
    return response

def _handle_remove_target(self, request, response):
    before = len(self._targets)
    self._targets = [t for t in self._targets if t.mmsi != int(request.mmsi)]
    removed = len(self._targets) < before
    response.success = removed
    response.message = (f"removed MMSI {request.mmsi}" if removed
                        else f"MMSI {request.mmsi} not found")
    self._logger.info(f"Service remove_target: {response.message}")
    return response
```

- [ ] **Step 6: 构建 + 运行时 smoke（A4000；需 Stream 2 build）**

Run (A4000，sim ACTIVE):
```
colcon build --packages-select target_vessel && source install/setup.bash
ros2 service call /target_vessel_node/add_target sil_msgs/srv/AddTarget \
  "{mmsi: 990000001, lat: 2.55, lon: 100.95, heading_deg: 180.0, sog_kn: 12.0, mode: 'replay'}"
ros2 topic echo /sil/target_vessel_state --once
```
Expected: service 返回 `success: true`；`/sil/target_vessel_state` 出现 mmsi 990000001 且坐标随时间推进。`remove_target` 后该 mmsi 消失。

- [ ] **Step 7: 提交**

```bash
git add src/sim_workbench/sil_nodes/target_vessel/target_vessel/node.py
git commit -m "feat(d1.8): runtime AddTarget/RemoveTarget services on target_vessel_node"
```

---

## Stream 6 — Orchestrator 集成（依赖 S1 + S2 + S5）

**Files:**
- Modify: `src/sil_orchestrator/lifecycle_bridge.py`（own_ship 订阅 + AddTarget 客户端）
- Create: `src/sil_orchestrator/encounters_routes.py`（REST router）
- Modify: `src/sil_orchestrator/main.py`（注册 router + `app.state.bridge`）

### Task 6.1: LifecycleBridge 加 own-ship 订阅 + 缓存 + getter

- [ ] **Step 1: import OwnShipState（文件顶部，与其他 sil_msgs import 同处）**
```python
from sil_msgs.msg import OwnShipState
```

- [ ] **Step 2: `__init__` 缓存变量（在 service-client 创建之前）**
```python
self._latest_own_ship: OwnShipState | None = None
```

- [ ] **Step 3: `__init__` 建订阅（在 client 创建块之后）**
```python
self.create_subscription(
    OwnShipState, "/sil/own_ship_state", self._on_own_ship_state, 10)
```

- [ ] **Step 4: 回调 + getter（类方法）**
```python
def _on_own_ship_state(self, msg: OwnShipState) -> None:
    self._latest_own_ship = msg

def get_latest_own_ship(self) -> "OwnShipState | None":
    return self._latest_own_ship
```

- [ ] **Step 5: AddTarget service 客户端（在现有 SetParameters client 创建处仿照加）**
```python
from sil_msgs.srv import AddTarget, RemoveTarget
self._add_target_client = self.create_client(
    AddTarget, "/target_vessel_node/add_target", callback_group=callback_group)
self._remove_target_client = self.create_client(
    RemoveTarget, "/target_vessel_node/remove_target", callback_group=callback_group)
```

- [ ] **Step 6: 调用 helper（仿现有 `_inject_params_to_node` 的 await 模式）**
```python
async def add_target(self, mmsi, lat, lon, heading_deg, sog_kn, mode="replay"):
    import asyncio
    if not self._add_target_client.wait_for_service(timeout_sec=3.0):
        raise RuntimeError("add_target service unavailable")
    req = AddTarget.Request()
    req.mmsi = int(mmsi); req.lat = float(lat); req.lon = float(lon)
    req.heading_deg = float(heading_deg); req.sog_kn = float(sog_kn); req.mode = mode
    future = self._add_target_client.call_async(req)
    deadline = 50
    while not future.done() and deadline > 0:
        await asyncio.sleep(0.1); deadline -= 1
    if not future.done():
        raise RuntimeError("add_target call timed out")
    return future.result()

async def remove_target(self, mmsi):
    import asyncio
    if not self._remove_target_client.wait_for_service(timeout_sec=3.0):
        raise RuntimeError("remove_target service unavailable")
    req = RemoveTarget.Request(); req.mmsi = int(mmsi)
    future = self._remove_target_client.call_async(req)
    deadline = 50
    while not future.done() and deadline > 0:
        await asyncio.sleep(0.1); deadline -= 1
    if not future.done():
        raise RuntimeError("remove_target call timed out")
    return future.result()
```

- [ ] **Step 7: 提交**
```bash
git add src/sil_orchestrator/lifecycle_bridge.py
git commit -m "feat(d1.8): bridge own_ship_state cache + AddTarget/RemoveTarget clients"
```

### Task 6.2: encounters REST router

- [ ] **Step 1: 写 router**

Create `src/sil_orchestrator/encounters_routes.py`:
```python
"""Encounter injection REST (D1.8).

POST /api/v1/encounters/inject  → geometry generator → AddTarget service.
Reusable by frontend buttons and headless test-bed scripts.
"""
import math
from fastapi import APIRouter, HTTPException, Request
from pydantic import BaseModel

from sil_orchestrator.encounter_geometry import OwnState, generate_encounter

router = APIRouter(prefix="/api/v1/encounters")

_MMSI_BASE = 990_000_000
_counter = {"n": 0}


class InjectBody(BaseModel):
    rule: str
    range_nm: float | None = None
    construct_cpa_m: float | None = None
    approach_angle_deg: float | None = None


def _own_state_from_msg(msg) -> OwnState:
    # OwnShipState (sil_msgs): pose.lat/lon/heading, kinematics.cog(rad)/sog(m/s)
    # Verify field units in Step 2; convert COG rad→deg, SOG m/s→kn here.
    heading_deg = math.degrees(msg.cog) % 360.0
    sog_kn = msg.sog / 0.514444
    return OwnState(lat=msg.lat, lon=msg.lon,
                    heading_deg=heading_deg, sog_kn=sog_kn)


@router.post("/inject")
async def inject_encounter(body: InjectBody, request: Request):
    bridge = request.app.state.bridge
    if bridge is None:
        raise HTTPException(503, "ROS bridge unavailable")
    own_msg = bridge.get_latest_own_ship()
    if own_msg is None:
        raise HTTPException(409, "own-ship state unavailable")
    try:
        own = _own_state_from_msg(own_msg)
        spawn = generate_encounter(
            body.rule, own, body.range_nm, body.construct_cpa_m,
            body.approach_angle_deg)
    except ValueError as exc:
        raise HTTPException(400, str(exc))
    _counter["n"] += 1
    mmsi = _MMSI_BASE + _counter["n"]
    res = await bridge.add_target(
        mmsi=mmsi, lat=spawn.lat, lon=spawn.lon,
        heading_deg=spawn.course_deg, sog_kn=spawn.sog_kn, mode="replay")
    if not getattr(res, "success", False):
        raise HTTPException(409, getattr(res, "message", "inject failed"))
    return {"accepted": True, "mmsi": mmsi}


@router.delete("/{mmsi}")
async def remove_encounter(mmsi: int, request: Request):
    bridge = request.app.state.bridge
    if bridge is None:
        raise HTTPException(503, "ROS bridge unavailable")
    res = await bridge.remove_target(mmsi)
    return {"removed": bool(getattr(res, "success", False))}
```

- [ ] **Step 2: 核实 OwnShipState ROS 字段名/单位**

Run (A4000): `ros2 interface show sil_msgs/msg/OwnShipState`
据其确认 `_own_state_from_msg` 用的字段名（`lat`/`lon` 还是 `pose.lat`；`cog`/`sog` 单位 rad/(m/s) 还是 deg/kn）。若 .msg 为扁平 `float64 lat/lon/heading/sog/cog`，则上述代码字段名正确；若嵌套（`pose.lat`），改为 `msg.pose.lat` 等。修正后保留 rad→deg、m/s→kn 转换（若 .msg 已是 deg/kn 则删转换）。

- [ ] **Step 3: main.py 注册 router + 暴露 bridge**

In `src/sil_orchestrator/main.py`:
- 顶部 import：`from sil_orchestrator.encounters_routes import router as encounters_router`
- bridge 创建后（`bridge = LifecycleBridge(...)` 之后）加：`app.state.bridge = bridge`（`_HAS_RCLPY` 为假时 `app.state.bridge = None`）
- 与其他 `app.include_router(...)` 同处加：`app.include_router(encounters_router)`

- [ ] **Step 4: 验证（A4000，sim ACTIVE）**

Run: `curl -sk -X POST https://127.0.0.1:18000/api/v1/encounters/inject -H 'content-type: application/json' -d '{"rule":"head_on"}'`
Expected: `{"accepted":true,"mmsi":990000001}`；`/sil/target_vessel_state` 现身该 mmsi，正前方 reciprocal 逼近。
负例：sim 非 ACTIVE 时 → HTTP 409 `own-ship state unavailable`。

- [ ] **Step 5: 提交**
```bash
git add src/sil_orchestrator/encounters_routes.py src/sil_orchestrator/main.py
git commit -m "feat(d1.8): POST /encounters/inject + DELETE /encounters/{mmsi} (geometry->AddTarget)"
```

---

## Stream 7 — 前端（航路双层 + api + 注入面板）（对 C-3 mock 可并行）

**Files:**
- Create: `web/src/map/PlannedRouteLayer.tsx`, `web/src/map/ActualTrackLayer.tsx`
- Create: `web/src/screens/shared/EncounterInjectPanel.tsx`
- Modify: `web/src/api/silApi.ts`, `web/src/screens/SimulationMonitor.tsx`

颜色（同族、深浅可辨）：航线 `#38bdf8`、航点标记 `#0369a1`（深一档）、实际航迹 `#0ea5e9`（实色）。

### Task 7.1: PlannedRouteLayer（虚线 + 航点 + 逐段标注）

- [ ] **Step 1: 写组件**

Create `web/src/map/PlannedRouteLayer.tsx`:
```tsx
import React, { useEffect, useRef } from 'react';
import type maplibregl from 'maplibre-gl';
import { computeRangeNm, computeBearing } from '../screens/shared/navMath';

interface Waypoint { lat: number; lon: number; }
interface Props {
  mapRef: React.MutableRefObject<maplibregl.Map | null>;
  waypoints: Waypoint[];
  visible: boolean;
}
const LINE_SRC = 'planned-route-src';
const LINE_LYR = 'planned-route-line';
const WP_SRC = 'planned-route-wp-src';
const WP_LYR = 'planned-route-wp-circle';
const LBL_SRC = 'planned-route-lbl-src';
const LBL_LYR = 'planned-route-lbl-symbol';

function buildLine(wps: Waypoint[]) {
  return {
    type: 'FeatureCollection',
    features: [{
      type: 'Feature',
      geometry: { type: 'LineString', coordinates: wps.map((w) => [w.lon, w.lat]) },
      properties: {},
    }],
  } as const;
}
function buildPoints(wps: Waypoint[]) {
  return {
    type: 'FeatureCollection',
    features: wps.map((w, i) => ({
      type: 'Feature',
      geometry: { type: 'Point', coordinates: [w.lon, w.lat] },
      properties: { idx: `WP${i}` },
    })),
  } as const;
}
function buildLabels(wps: Waypoint[]) {
  const feats = [];
  for (let i = 0; i < wps.length - 1; i++) {
    const a = wps[i], b = wps[i + 1];
    const nm = computeRangeNm(a.lat, a.lon, b.lat, b.lon);
    const brg = computeBearing(a.lat, a.lon, b.lat, b.lon);
    feats.push({
      type: 'Feature',
      geometry: { type: 'Point', coordinates: [(a.lon + b.lon) / 2, (a.lat + b.lat) / 2] },
      properties: { label: `${nm.toFixed(1)} NM · ${brg.toFixed(0)}°` },
    });
  }
  return { type: 'FeatureCollection', features: feats } as const;
}

export const PlannedRouteLayer: React.FC<Props> = React.memo(({ mapRef, waypoints, visible }) => {
  const added = useRef(false);
  useEffect(() => {
    const map = mapRef.current;
    if (!map) return;
    const opacity = visible && waypoints.length >= 2 ? 1 : 0;
    function setup() {
      const line = buildLine(waypoints);
      const pts = buildPoints(waypoints);
      const lbls = buildLabels(waypoints);
      if (!added.current) {
        map.addSource(LINE_SRC, { type: 'geojson', data: line as any });
        map.addLayer({
          id: LINE_LYR, type: 'line', source: LINE_SRC,
          layout: { 'line-cap': 'round', 'line-join': 'round' },
          paint: { 'line-color': '#38bdf8', 'line-width': 2,
                   'line-dasharray': [3, 2], 'line-opacity': opacity },
        });
        map.addSource(WP_SRC, { type: 'geojson', data: pts as any });
        map.addLayer({
          id: WP_LYR, type: 'circle', source: WP_SRC,
          paint: { 'circle-radius': 4, 'circle-color': '#0369a1',
                   'circle-stroke-width': 1, 'circle-stroke-color': '#e0f2fe',
                   'circle-opacity': opacity, 'circle-stroke-opacity': opacity },
        });
        map.addSource(LBL_SRC, { type: 'geojson', data: lbls as any });
        map.addLayer({
          id: LBL_LYR, type: 'symbol', source: LBL_SRC,
          layout: { 'text-field': ['get', 'label'], 'text-size': 11,
                    'text-offset': [0, -0.8], 'text-allow-overlap': false },
          paint: { 'text-color': '#0369a1', 'text-halo-color': '#f0f9ff',
                   'text-halo-width': 1, 'text-opacity': opacity },
        });
        added.current = true;
      } else {
        (map.getSource(LINE_SRC) as any)?.setData(line);
        (map.getSource(WP_SRC) as any)?.setData(pts);
        (map.getSource(LBL_SRC) as any)?.setData(lbls);
        for (const id of [LINE_LYR, WP_LYR, LBL_LYR]) {
          const prop = id === LINE_LYR ? 'line-opacity'
            : id === WP_LYR ? 'circle-opacity' : 'text-opacity';
          if (map.getLayer(id)) map.setPaintProperty(id, prop, opacity);
        }
      }
    }
    if (!map.isStyleLoaded()) map.once('style.load', setup);
    else setup();
  }, [mapRef, waypoints, visible]);
  return null;
});
```

- [ ] **Step 2: 提交**
```bash
git add web/src/map/PlannedRouteLayer.tsx
git commit -m "feat(d1.8): PlannedRouteLayer (dashed route + waypoints + leg NM/bearing labels)"
```

### Task 7.2: ActualTrackLayer（实线航迹）

- [ ] **Step 1: 写组件**

Create `web/src/map/ActualTrackLayer.tsx`:
```tsx
import React, { useEffect, useRef } from 'react';
import type maplibregl from 'maplibre-gl';

interface Props {
  mapRef: React.MutableRefObject<maplibregl.Map | null>;
  trail: [number, number][];  // [lon, lat] pairs
  visible: boolean;
}
const SRC = 'actual-track-src';
const LYR = 'actual-track-line';

export const ActualTrackLayer: React.FC<Props> = React.memo(({ mapRef, trail, visible }) => {
  const added = useRef(false);
  useEffect(() => {
    const map = mapRef.current;
    if (!map) return;
    const opacity = visible && trail.length >= 2 ? 1 : 0;
    const data = {
      type: 'FeatureCollection',
      features: [{ type: 'Feature',
        geometry: { type: 'LineString', coordinates: trail },
        properties: {} }],
    };
    function setup() {
      if (!added.current) {
        map.addSource(SRC, { type: 'geojson', data: data as any });
        map.addLayer({
          id: LYR, type: 'line', source: SRC,
          layout: { 'line-cap': 'round', 'line-join': 'round' },
          paint: { 'line-color': '#0ea5e9', 'line-width': 3, 'line-opacity': opacity },
        });
        added.current = true;
      } else {
        (map.getSource(SRC) as any)?.setData(data);
        if (map.getLayer(LYR)) map.setPaintProperty(LYR, 'line-opacity', opacity);
      }
    }
    if (!map.isStyleLoaded()) map.once('style.load', setup);
    else setup();
  }, [mapRef, trail, visible]);
  return null;
});
```

- [ ] **Step 2: 提交**
```bash
git add web/src/map/ActualTrackLayer.tsx
git commit -m "feat(d1.8): ActualTrackLayer (solid own-ship sailed track)"
```

### Task 7.3: silApi 加 encounter mutations

- [ ] **Step 1: 加 mutation（在 `injectFault`/`cancelFault` 同处 builder 块）**
```typescript
injectEncounter: builder.mutation<
  { accepted: boolean; mmsi: number },
  { rule: string; range_nm?: number; construct_cpa_m?: number; approach_angle_deg?: number }
>({
  query: (body) => ({ url: '/encounters/inject', method: 'POST', body }),
}),
removeEncounter: builder.mutation<{ removed: boolean }, number>({
  query: (mmsi) => ({ url: `/encounters/${mmsi}`, method: 'DELETE' }),
}),
```

- [ ] **Step 2: 加 hook 导出（底部 `export const { ... } = silApi`）**
```typescript
useInjectEncounterMutation,
useRemoveEncounterMutation,
```

- [ ] **Step 3: 提交**
```bash
git add web/src/api/silApi.ts
git commit -m "feat(d1.8): silApi injectEncounter/removeEncounter mutations"
```

### Task 7.4: EncounterInjectPanel（4 预设按钮）

- [ ] **Step 1: 写组件**

Create `web/src/screens/shared/EncounterInjectPanel.tsx`:
```tsx
import React, { useState } from 'react';
import { useInjectEncounterMutation, useRemoveEncounterMutation } from '../../api/silApi';

const PRESETS = [
  { rule: 'head_on',          name: '对头 R14',        desc: '正前方 reciprocal 碰撞航线' },
  { rule: 'crossing_giveway', name: '交叉让路 R15-GW', desc: '右舷来船，本船让路' },
  { rule: 'crossing_standon', name: '交叉直航 R15-SO', desc: '左舷来船，本船直航' },
  { rule: 'overtaking',       name: '追越 R13',        desc: '正前慢船，本船追越' },
] as const;

interface Props { inline?: boolean; }

export const EncounterInjectPanel: React.FC<Props> = ({ inline = false }) => {
  const [injectEncounter] = useInjectEncounterMutation();
  const [removeEncounter] = useRemoveEncounterMutation();
  const [activeMmsis, setActiveMmsis] = useState<number[]>([]);
  const [error, setError] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);

  const onInject = async (rule: string) => {
    setBusy(true); setError(null);
    try {
      const res = await injectEncounter({ rule }).unwrap();
      if (res.accepted) setActiveMmsis((p) => [...p, res.mmsi]);
    } catch (e: any) {
      setError(e?.data?.detail ?? '注入失败（确认仿真已启动）');
    } finally { setBusy(false); }
  };
  const onClear = async () => {
    setBusy(true);
    try {
      await Promise.all(activeMmsis.map((m) => removeEncounter(m).unwrap().catch(() => {})));
      setActiveMmsis([]);
    } finally { setBusy(false); }
  };

  return (
    <div data-testid="encounter-panel"
         style={{ display: 'flex', flexDirection: 'column', gap: 8, width: '100%' }}>
      <div style={{ fontWeight: 600 }}>遭遇注入（COLREGs 预设）</div>
      {PRESETS.map((p) => (
        <button key={p.rule} data-testid={`encounter-${p.rule}`}
                disabled={busy} onClick={() => onInject(p.rule)}
                style={{ textAlign: 'left', padding: '8px 10px', borderRadius: 6,
                         cursor: busy ? 'wait' : 'pointer' }}>
          <div style={{ fontWeight: 600 }}>{p.name}</div>
          <div style={{ fontSize: 12, opacity: 0.7 }}>{p.desc}</div>
        </button>
      ))}
      <button data-testid="encounter-clear" disabled={busy || !activeMmsis.length}
              onClick={onClear} style={{ padding: '6px 10px', borderRadius: 6 }}>
        清除注入船（{activeMmsis.length}）
      </button>
      {error && <div style={{ color: '#dc2626', fontSize: 12 }}>{error}</div>}
    </div>
  );
};
```

- [ ] **Step 2: 提交**
```bash
git add web/src/screens/shared/EncounterInjectPanel.tsx
git commit -m "feat(d1.8): EncounterInjectPanel with 4 COLREGs presets + clear"
```

### Task 7.5: 接入 SimulationMonitor（挂层 + tab）

- [ ] **Step 1: import（文件顶部 import 区）**
```tsx
import { PlannedRouteLayer } from '../map/PlannedRouteLayer';
import { ActualTrackLayer } from '../map/ActualTrackLayer';
import { EncounterInjectPanel } from './shared/EncounterInjectPanel';
```

- [ ] **Step 2: 挂层（在 `<SafetyDomainLayer .../>` 之后，与其它 layer 同处）**
```tsx
<PlannedRouteLayer mapRef={externalMapRef}
  waypoints={voyagePlan?.waypoints ?? []} visible={true} />
<ActualTrackLayer mapRef={externalMapRef}
  trail={trail} visible={true} />
```
> `voyagePlan`、`trail`、`externalMapRef` 已在 SimulationMonitor 内可用（见 telemetryStore：`voyagePlan.waypoints` = `{lat,lon}[]`，`ownShipTrail` = `[lon,lat][]`）。若局部变量名为 `ownShipTrail` 而非 `trail`，用实际名。

- [ ] **Step 3: 加 tab（`MONITOR_TABS` 数组，在 `'fault'` 条目后）**
```tsx
{ id: 'encounter', label: '遭遇注入', icon: <LucideAlertTriangle size={20} /> },
```
确保顶部已 import `LucideAlertTriangle`（lucide-react）；并把 `'encounter'` 加入 `MonitorTabId` 联合类型（若存在该类型别名）。

- [ ] **Step 4: 加 tab 内容（在 `activeRightTab === 'fault'` 渲染块之后）**
```tsx
{activeRightTab === 'encounter' && (
  <div style={{ display: 'flex', flexDirection: 'column' }}>
    <EncounterInjectPanel inline={true} />
  </div>
)}
```

- [ ] **Step 5: 构建验证**

Run: `cd web && npm run build`
Expected: 构建成功，无类型错误。

- [ ] **Step 6: 提交**
```bash
git add web/src/screens/SimulationMonitor.tsx
git commit -m "feat(d1.8): mount route layers + encounter inject tab in SimulationMonitor"
```

---

## Stream 8 — Malacca 演示场景 YAML（独立）

**Files:**
- Create: `scenarios/Malacca演示/malacca-archipelago-transit.yaml`

### Task 8.1: 场景文件

- [ ] **Step 1: 写场景（schema v3.x，仿 `scenarios/COLREGs测试/colreg-rule14-ho.yaml` 结构）**

Create `scenarios/Malacca演示/malacca-archipelago-transit.yaml`:
```yaml
title: 'Malacca 近海群岛全航路避碰演示'
description: >-
  Malacca 海峡西北向 transit（近海群岛 ODD）。本船沿 5 航点计划航路自主航行；
  运行中按需注入 COLREGs 相遇（对头/交叉/追越）。targetShips 为空——目标全运行时注入。
startTime: '2026-06-09T00:00:00Z'
ownShip:
  static:
    id: 1
    shipType: Cargo
    name: FCB Own Ship
  initial:
    position:
      latitude: 2.500
      longitude: 101.000
    cog: 315.0
    sog: 12.0
    heading: 315.0
  model: fcb_mmg_vessel
  controller: psbmpc_wrapper
  nominalRoute:
  - { latitude: 2.500, longitude: 101.000, target_sog_kn: 12.0 }
  - { latitude: 2.560, longitude: 100.940, target_sog_kn: 12.0 }
  - { latitude: 2.620, longitude: 100.880, target_sog_kn: 12.0 }
  - { latitude: 2.680, longitude: 100.820, target_sog_kn: 12.0 }
  - { latitude: 2.740, longitude: 100.760, target_sog_kn: 12.0 }
targetShips: []
environment:
  wind: &id001 { dir_deg: 0.0, speed_mps: 0.0 }
  current: &id002 { dir_deg: 0.0, speed_mps: 0.0 }
  visibility_nm: 6.0
metadata:
  schema_version: '3.0'
  scenario_id: malacca-archipelago-transit
  scenario_source: d1.8_demo
  vessel_class: FCB
  odd_cell:
    domain: coastal_archipelago      # [TBD-verify-odd-key] 见 spec §9
  seed: 108
  expected_outcome:
    cpa_min_m_ge: 500.0
  simulation_settings:
    total_time: 1200.0
    dt: 0.02
    n_rps_initial: 3.5
    coordinate_origin: [2.500, 101.000]
    dynamics_mode: internal
    backend: ros2
  disturbance:
    wind: *id001
    current: *id002
```

- [ ] **Step 2: 校验 schema**

Run: `python tools/sil/scenario_spec.py scenarios/Malacca演示/malacca-archipelago-transit.yaml`（若该工具存在；否则用 orchestrator schema 校验端点 `GET /api/v1/schema` 比对，或加载场景看 orchestrator 是否报错）。
- [ ] **核查 `[TBD-verify-odd-key]`**：grep ODD 枚举定义确认 `coastal_archipelago` 是否合法值（前端 ODD 过滤 + M1）。Run: `grep -rn "coastal_archipelago\|open_sea_offshore_wind_farm" src/ web/src/ | grep -i "enum\|domain\|odd" | head`。不合法则改为实际枚举串。
Expected: schema 校验通过；ODD 串合法或已修正。

- [ ] **Step 3: 提交**
```bash
git add "scenarios/Malacca演示/malacca-archipelago-transit.yaml"
git commit -m "feat(d1.8): Malacca archipelago transit demo scenario (5-wp route, runtime targets)"
```

---

## Stream 9 — 集成接线 + E2E（最后，依赖全部）

**Files:**
- Modify: SIL 启动编排（加 `gnc_route_mock_publisher` + `route_ingest_node`）— 具体文件见 Step 1
- Create: `web/e2e/d1.8_malacca_demo.spec.ts`

### Task 9.1: 接入两个新节点到启动编排

- [ ] **Step 1: 定位并修改启动编排**

Run: `grep -rn "mock_l2_publisher" docker/ scripts/ src/ | grep -iv "\.pyc"` 找到 `mock_l2_publisher.py` 的启动处（compose / launch / supervisor 脚本）。在同处按相同方式启动 `docker/gnc_route_mock_publisher.py` 与 `docker/route_ingest_node.py`。
> Malacca 场景下，确保 `/route_planning/gnc_route_plan` 仅由 `gnc_route_mock_publisher` 写、`/l2/planned_route` 仅由 `route_ingest_node` 写（避免与 `mock_l2_publisher` 双写 — 如冲突，Malacca 场景禁用 mock_l2 的 route 发布或令其让位）。

- [ ] **Step 2: 部署 + 重启（A4000，§13）**
```
# 本地→A4000 同路径 scp 两个 .py + 已 build 的 sil_msgs/ship_interfaces/target_vessel/sil_orchestrator
restart sil-nodes
```

- [ ] **Step 3: 端到端手测（A4000）**
- 载入 `malacca-archipelago-transit`，CONFIGURE→海图出现虚线 5 航点 + 逐段 [NM·针路°]。
- ACTIVATE→本船沿线航行，实线航迹增长。
- 点"对头 R14"→目标正前 reciprocal 现身→本船右转避让→实线鼓出虚线→past-and-clear 归航。
Expected: 全链路无异常；M6 分类 = Rule14，CPA ≥ 500m。

- [ ] **Step 4: 提交编排改动**
```bash
git add <编排文件>
git commit -m "feat(d1.8): wire gnc_route_mock_publisher + route_ingest_node into SIL startup"
```

### Task 9.2: Playwright E2E

- [ ] **Step 1: 写 E2E（仿 `web/e2e/mvp_consistency.spec.ts` 结构）**

Create `web/e2e/d1.8_malacca_demo.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

const BASE = process.env.HMI_URL ?? 'http://192.168.121.50:5173';

test('D1.8 Malacca demo: route renders + head-on injection avoidance', async ({ page }) => {
  await page.goto(BASE);
  // 选 Malacca 场景并 CONFIGURE（按现有 mvp_consistency 的选场景交互复用）
  await page.getByTestId('scenario-malacca-archipelago-transit').click();
  await page.getByRole('button', { name: /configure|配置/i }).click();
  // 计划航路虚线层存在（map source）
  await expect.poll(async () =>
    page.evaluate(() => (window as any).__map?.getSource?.('planned-route-src') != null)
  ).toBeTruthy();
  // ACTIVATE
  await page.getByRole('button', { name: /activate|启动/i }).click();
  // 打开遭遇注入 tab，注入对头
  await page.getByTestId('right-tab-encounter').click();
  await page.getByTestId('encounter-head_on').click();
  // 目标船出现（target layer / telemetry）
  await expect.poll(async () =>
    page.evaluate(() => (window as any).__telemetry?.targets?.length ?? 0),
    { timeout: 15000 }
  ).toBeGreaterThan(0);
});
```
> 选场景 / configure / activate 的具体选择子按 `web/e2e/mvp_consistency.spec.ts` 现有实现对齐；`window.__map`/`window.__telemetry` 若未暴露，改用现有 testid 断言（如目标船标记 testid）。

- [ ] **Step 2: 运行 E2E（A4000，§13）**

Run: `cd web && RATE=10 ORCH_PORT=18000 FOX_PORT=18765 npx playwright test e2e/d1.8_malacca_demo.spec.ts`
Expected: PASS — 航路源存在 + 注入后目标出现。

- [ ] **Step 3: 提交**
```bash
git add web/e2e/d1.8_malacca_demo.spec.ts
git commit -m "test(d1.8): E2E — Malacca route render + head-on injection avoidance"
```

---

## 自审（spec 覆盖 / 占位 / 类型一致）

**Spec 覆盖**：C1 航路接收=S3+S4 · C2 海图渲染=S7.1/7.2/7.5 · C3 注入生效=S1+S5+S6 · C4 避碰闭环=S9.1 手测+S9.2 E2E · C5 测试台就绪=S1 纯函数+S6 REST（脚本可直调）。spec §7 降级路径→S6 router 409/400 分支。✅ 全覆盖。

**已知占位（均带核查步骤，非裸 TODO）**：
- S4 `_make_waypoint`：Step 1 含 `raise NotImplementedError`，**Step 2 强制删除并按 `ros2 interface show PlannedRoute` 补全**（依赖运行时类型，无法静态写死）。
- S6.2 `_own_state_from_msg` 字段名/单位：Step 2 核查 `OwnShipState.msg` 后定稿。
- S8 `coastal_archipelago` ODD 串：Step 2 核查枚举。
- S9 启动编排文件：S9.1 Step 1 grep 定位。

**类型一致**：`OwnState`/`TargetSpawn`(S1) ↔ encounters_routes import(S6) ✅；srv 字段 `mmsi/lat/lon/heading_deg/sog_kn/mode`(S2) ↔ node handler(S5) ↔ bridge client(S6) ✅；REST 形状 C-3 ↔ silApi mutation(S7.3) ↔ router(S6.2) ✅；`generate_encounter` 签名 C-1 ↔ 调用(S6.2) ✅。

**部署纪律**：ROS 改动一次只一个 configure 驱动（CLAUDE.md §13 env_disturbance wedge）；复位 `restart sil-nodes`。
