# DEMO-1 全栈真实化设计文档

| 属性 | 值 |
|---|---|
| 文档编号 | SANGO-ADAS-L3-SPEC-DEMO1-REAL |
| 版本 | v1.0 |
| 日期 | 2026-05-25 |
| 基线 | [00-master-plan.md](../00-master-plan.md) v3.2-master / [00-demo1-demo2-gap-review.md](../Review/2026-05-25/00-demo1-demo2-gap-review.md) v1.0 |
| 目标 | DEMO-1（6/15）端到端零 mock 真实化演示 |
| 原则 | **绝对不使用 mock 值**；Phase 3 必要项提前拉取；完整实现优先 |

---

## 0. 设计总览

### 0.1 DEMO-1 Charter 回顾

端到端 5 min 演示：
1. AIS 历史数据回放（NOAA 1h 片段）→ **真 M2** → 仿真器 own-ship 运动
2. 1 个 Crossing 场景脚本 live → SIL HMI 显示 → **真 M1 ODD 评估** → SAT-1/2/3 视图
3. PR-trigger Smoke 10 跑通 + 覆盖率立方体热图（10 cell 亮）
4. ConOps PDF + V&V Plan PDF + cert-evidence-tracking.md 现场展示
5. 仿真器参考解（旋回）实测 + tool confidence 报告

### 0.2 全栈真实化核心变更

当前 DEMO-1 关键路径存在 23 项 mock/stub（1 项 🔴 BLOCKER + 6 项 🟠 HIGH）。全栈真实化要求：

- **数据流零 mock**：所有 SIL 管线节点输出真实计算值，不启动 sil_mock_publisher / l3_external_mock_publisher
- **M4 IvP 真实仲裁**：IvP solver 接入 node 回调，发布 ivp_contributions[]（8 方向梯度）
- **M5 BC-MPC 真实避碰**：线性化 Nomoto + 13 弧枚举，发布 trajectory_candidates[]
- **M7 Doer-Checker 真实 veto**：启用 veto_enabled=true，6 类硬约束实装
- **M8 SAT 桥接打通**：发布 /sil/sat2_data / /sil/sat3_data / /sil/sotif_metrics
- **SIL 节点去 mock**：tracker 计算 CPA/TCPA、env_disturbance 从 YAML 读环境参数

### 0.3 Phase 分布与依赖

```
Phase 1 修补（D1.x 补完）          ← 0.5 pw，本周完成
  │
  ├── D1.3.2.3 Web HMI SAT handler 验证
  └── D1.6 traceability-matrix.csv 基础版

Phase 2 修补（D2.x 去硬编码）       ← 3.0 pw，6/1 前完成
  │
  ├── D2.1 M1 ODD 去硬编码（scoring/health/zone_reason）
  ├── D2.2 M2 SAT-3 TDL/TMR 从 ODDState 提取
  ├── D2.4 M6 colregs_chain C++ 填充 + chain 截图
  └── D2.5 SIL 管线去 mock（tracker/env/sensor）

Phase 3 提前（D3.x DEMO-1 必需项）  ← 12.0 pw，6/15 前完成
  │
  ├── D3.1 M4 IvP 接入 + ivp_contributions publisher
  ├── D3.2 M5 BC-MPC + trajectory_candidates publisher
  ├── D3.3a M7-core 6 类硬约束 + veto 启用
  ├── D3.3b M7-sotif 6 项指标计算 + /sil/sotif_metrics publisher
  └── D3.4 M8 SAT-2/3/SOTIF 3 topic + sil_msgs IDL

总计：~15.5 pw，距 6/15 剩 21 天
```

---

## 1. Phase 1 修补（D1.x 补完）

### 1.1 D1.3.2.3 — Web HMI SAT handler 端到端验证

**现状**：前端 3 个 SAT handler（IvP / Trajectory / SOTIF）代码已完整，但后端无数据源导致"空转"。

**DEMO-1 修补内容**：

| 项 | 内容 | 工时 |
|---|---|---|
| 1.1.1 | 验证 foxglove_bridge → useFoxgloveLive → store → 组件的完整链路在**有数据时**能正常渲染 | 2h |
| 1.1.2 | 在前端增加"数据源断开"降级提示（当 SAT-2/3 handler 3s 无数据时显示 "Waiting for M4/M5 data..."） | 2h |
| 1.1.3 | SAT-1 威胁列表：确认 M8 ui_state 中 threat_list 字段从真实 M2 encounter 数据填充（非硬编码空列表） | 2h |

**接口契约**：
- 输入：foxglove_bridge WebSocket `/sil/sat2_data` / `/sil/sat3_data` / `/sil/sotif_metrics`
- 输出：IvpRiskGradientLayer / MpcTrajectoryLayer / SotifMonitorStrip 渲染
- 降级：数据断开时显示等待提示，不显示空壳或假数据

### 1.2 D1.6 — 场景 schema traceability-matrix.csv 基础版

**现状**：schema baseline ✅，但 traceability-matrix.csv 缺失。

**DEMO-1 修补内容**：

| 项 | 内容 | 工时 |
|---|---|---|
| 1.2.1 | 生成 Imazu-22 场景的 traceability-matrix.csv：Rule × ODD × 场景 ID 映射 | 4h |
| 1.2.2 | 在 coverage_cube.py 中集成 matrix 读取，热图可按 Rule/ODD 维度筛选 | 2h |

---

## 2. Phase 2 修补（D2.x 去硬编码）

### 2.1 D2.1 — M1 ODD Envelope Manager 去硬编码

**现状**：M1 ODD FSM 实装 ✅，但 `build_scoring_inputs()` / `build_system_health()` / `zone_reason` 含硬编码值。

**DEMO-1 修补内容**：

| 项 | MOCK ID | 修补内容 | 工时 |
|---|---|---|---|
| 2.1.1 | MOCK-03 | `build_scoring_inputs()`：无 `last_env_state_` 时 visibility/sea_state 标记为 DEGRADED（而非默认晴朗）；`radar_health_ok`/`comm_ok`/`tmr_available` 从 `/l3/diagnostics` topic 获取，无数据时默认 false（而非 true） | 4h |
| 2.1.2 | MOCK-04 | `build_system_health()`：`mttf_estimate_s` 从模块心跳统计计算（3-miss 阈值 → MTTF 估算）；`heartbeat_recency_s` 从实际心跳时间差计算；`has_redundancy` 从 Parameter Manifest 读取 | 4h |
| 2.1.3 | MOCK-05 | MRC 触发逻辑：`water_depth_m` 从 ENC/Chart 数据获取（DEMO-1 可从场景 YAML environment 段读取）；`in_anchorage_zone`/`is_moored` 从 M3 mission_state 获取 | 3h |
| 2.1.4 | MOCK-07 | `zone_reason` 动态生成：根据当前 zone 和评分维度生成原因字符串（如 "visibility=0.8nm < full_nm=2.0 → zone B"） | 2h |

**接口契约**：
- 新增订阅：`/l3/diagnostics`（DiagnosticArray，各模块心跳）
- 新增订阅：`/l3/m3/mission_state`（anchorage/moored 状态）
- 修改输出：`/l3/m1/odd_state` zone_reason 字段动态化
- 降级路径：无诊断数据时所有 health 标记 DEGRADED，ODD zone 至少为 B

### 2.2 D2.2 — M2 World Model SAT-3 TDL/TMR 修复

**现状**：M2 SAT-3 forecast 中 `tdl_s=0.0`/`tmr_s=0.0` 硬编码，与 M1 发布的 TDL=45/TMR=60 矛盾。

**DEMO-1 修补内容**：

| 项 | MOCK ID | 修补内容 | 工时 |
|---|---|---|---|
| 2.2.1 | MOCK-08 | M2 已订阅 `/l3/m1/odd_state`，从 `ODDState.tdl_s`/`ODDState.tmr_s` 提取并填入 SAT-3 forecast | 1h |
| 2.2.2 | MOCK-08 | `predicted_state` 从 encounter_classifier 输出动态生成（"nominal"/"risk_escalating"/"emergency"），而非硬编码 "nominal" | 2h |
| 2.2.3 | MOCK-08 | `prediction_uncertainty` 从 CPA/TCPA 不确定性传播计算（使用 Linear 方法），而非简化公式 `1.0-health` | 2h |
| 2.2.4 | MOCK-22 | l3_params.yaml 设置 `target_classification_enabled: true`；M2 目标分类功能启用：基于 SOG/size/encounter_type 推断 vessel_type（cargo/tanker/passenger/fishing） | 3h |

### 2.3 D2.4 — M6 COLREGs chain C++ 填充 + chain 截图

**现状**：IDL + Arrow 评分管线 ✅，C++ chain 填充 + colcon 测试待关闭（SIL-6）。

**DEMO-1 修补内容**：

| 项 | 内容 | 工时 |
|---|---|---|
| 2.3.1 | M6 colregs_reasoner_node.cpp 中 `colregs_chain` 字段完整填充：5 层（Rule identification → Geometric classification → Action determination → Priority resolution → Compliance check） | 4h |
| 2.3.2 | colcon 测试：≥3 个场景（head-on / crossing-gw / crossing-sg）的 chain 输出验证 | 2h |
| 2.3.3 | Playwright 截图 ≥10 场景的 ColregsRationaleTree 渲染 | 4h |

### 2.4 D2.5 — SIL 管线去 mock

**现状**：SIL 管线中 tracker_mock / env_disturbance / sensor_mock 含硬编码值。DEMO-1 launch 文件仍默认启动 sil_mock_publisher。

**DEMO-1 修补内容**：

| 项 | MOCK ID | 修补内容 | 工时 |
|---|---|---|---|
| 2.4.1 | MOCK-01 | DEMO-1 launch 文件移除 sil_mock_publisher 节点，改用真 M1+M2+M3+M6 节点 | 1h |
| 2.4.2 | MOCK-02 | DEMO-1 launch 文件移除 l3_external_mock_publisher 节点，改用 SIL 管线（ship_dynamics → sensor_mock → tracker_mock → M2） | 0.5h |
| 2.4.3 | MOCK-13 | tracker_mock 实现 CPA/TCPA 线性外推计算：基于 own-ship 和 target 的 SOG/COG，计算 CPA_m 和 TCPA_s；调用 M2 EncounterClassifier 判断 encounter_type | 6h |
| 2.4.4 | MOCK-16 | env_disturbance 从场景 YAML 的 environment 段读取 visibility_nm / sea_state_beaufort / wind_speed / wind_dir / current_speed / current_dir | 4h |
| 2.4.5 | MOCK-15 | sensor_mock 噪声参数从场景 YAML 的 sensor_noise 段读取（range_sigma_m / bearing_sigma_rad / rcs_range / clutter_max），无配置时使用保守默认值 | 3h |

**接口契约**：
- 场景 YAML 新增 `environment` 段：visibility_nm / sea_state_beaufort / wind / current
- 场景 YAML 新增 `sensor_noise` 段：range_sigma_m / bearing_sigma_rad / rcs_range / clutter_max
- tracker_mock 新增输出字段：cpa_m / tcpa_s / encounter_type / relative_bearing_deg / aspect_angle_deg / is_giveway（从真实计算填充）
- 降级路径：YAML 无 environment/sensor_noise 段时使用保守默认值（visibility=2nm / sea_state=4 / 噪声按 IMO 性能标准）

---

## 3. Phase 3 提前（D3.x DEMO-1 必需项）

### 3.1 D3.1 — M4 BehaviorArbiter IvP 接入 + ivp_contributions publisher

**上下文**：
- 架构权威：§8（M4 BehaviorArbiter）、§15 IDL
- 现有代码：IvP solver 底层 ~376 行真实逻辑（ivp_domain.hpp + ivp_function.hpp + ivp_combine.cpp + ivp_solver.cpp），但 node 回调从未调用
- 关联：D2.4（M6 COLREGs 约束作为 IvP 硬约束输入）、D3.4（M8 SAT-2 消费 ivp_contributions）

**核心交付物**：

| 项 | 内容 | 工时 |
|---|---|---|
| 3.1.1 | `arbitration_timer_callback()` 接入 IvPSolver：实例化 IvPSolver → 将 active_set 转换为 WeightedFunction[] → 调用 solve() → 用 IvPSolution 填充 BehaviorPlanMsg 的 heading/speed 区间（替代当前全域默认值 0–360°/0–22kn） | 8h |
| 3.1.2 | IvPFunctionFactory：根据 behavior_definitions.yaml 中 ivp_function_type 字段（transit / colreg_avoid / dp_hold / restricted_vis / mrc_drift）构建 IvPFunctionDefault 的 piece 定义 | 6h |
| 3.1.3 | `BehaviorDictionary::get_active_subset()` 去 stub：实现完整的 ODD/mode 条件过滤（当前硬编码只返回 TRANSIT） | 4h |
| 3.1.4 | M6 COLREGs_ConstraintMsg 作为 IvP 硬约束注入：将 M6 发布的 constraint 话题映射到 IvPHardConstraints 的 heading_allowed_ranges_deg / speed_min_max_kn | 4h |
| 3.1.5 | 新增 `ivp_contributions` publisher：发布 /sil/sat2_data.ivp_contributions[] @4Hz（8 方向梯度向量 {direction_deg, utility_value, behavior_id}） | 4h |
| 3.1.6 | 8 方向风险梯度提取：从 IvP 求解结果中提取 0/45/90/135/180/225/270/315° 的聚合效用，归一化到 [0,1] 作为 cost | 4h |
| 3.1.7 | confidence 字段写入 ASDR：BehaviorPlanMsg.confidence 从 IvP 求解质量（可行区间占比）计算 | 2h |

**需探讨的设计决策**：
- IvP solver 离散化分辨率：航向 5° 间隔（72 格）× 速度 1kn 间隔（22 格）= 1584 格，2Hz 求解 <10ms 可接受
- MRC_Drift 行为始终最高优先级（weight=1.0）的实现：在 BehaviorDictionary 中 MRC 行为 weight 固定为 1.0，其他行为 ≤0.8
- M6 硬约束将 IvP 可行集完全清空时的 fallback：回退 nearest_compliant（架构 §8.4），输出上一帧有效解 + confidence=0.0 + rationale="IvP infeasible, fallback to nearest_compliant"
- ivp_contributions[] 8 方向表示：8 × {direction_deg: float32, utility_value: float32, behavior_id: string}

**接口契约**：
- 新增订阅：`/l3/m6/colregs_constraints`（COLREGs_ConstraintMsg）
- 新增发布：`/sil/sat2_data`（sil_msgs/SAT2Data，含 ivp_contributions[]）
- 修改发布：`/l3/m4/behavior_plan` heading/speed 区间从 IvP 求解结果填充
- 降级路径：IvP 求解超时（>50ms）→ 使用上一帧解 + confidence 衰减；连续 3 次超时 → 回退硬优先级 + alert

### 3.2 D3.2 — M5 TacticalPlanner BC-MPC + trajectory_candidates publisher

**上下文**：
- 架构权威：§10（M5 TacticalPlanner）、§15 IDL
- 现有代码：Phase E1 几何枚举骨架（5/7 分支 + minimax CPA），trajectory_candidates 全链路缺失
- 关联：D3.1（消费 M4 Behavior_PlanMsg）、D2.4（消费 M6 COLREGs_ConstraintMsg）

**核心交付物**：

| 项 | 内容 | 工时 |
|---|---|---|
| 3.2.1 | 线性化 Nomoto 4-DOF 船舶模型：T·dψ/dt = K·δ + bias（含风流补偿），参数从 Capability Manifest 读取；l3_params.yaml 设置 `use_ipopt: false` 改为 `use_ipopt: false, use_nomoto: true`（明确使用 Nomoto 线性化路径，非 CasADi） | 8h |
| 3.2.2 | 13 弧 BC-MPC 枚举求解器：k=13 候选航向（当前航向 ±{0,5,10,15,20,25,30}°），每个候选前推 30s 计算 CPA，argmax min_CPA 选择 | 8h |
| 3.2.3 | trajectory_candidates[] IDL 定义 + publisher：sil_msgs/SAT3Data，含 13 × {waypoints[], cpa_m, rule_compliant, is_primary} | 4h |
| 3.2.4 | M4 Behavior_PlanMsg 消费：将 M4 输出的 heading/speed 区间作为 BC-MPC 搜索空间约束 | 4h |
| 3.2.5 | M6 COLREGs 约束集成：CPA ≥ CPA_safe(ODD) 作为硬约束过滤候选轨迹 | 4h |
| 3.2.6 | ROT_max 曲线从 Capability Manifest 读取（替代 l3_params.yaml 硬编码） | 2h |
| 3.2.7 | AvoidancePlanMsg → L4 输出：最佳轨迹的 (ψ_cmd, u_cmd, ROT) @2Hz | 2h |

**需探讨的设计决策**：
- 13 弧 vs 7 弧：DEMO-1 使用 13 弧（±30° 范围，5° 间隔），DEMO-3 升级为 CasADi/IPOPT 完整版
- trajectory_candidates[] IDL 结构：13 × {waypoints: geometry_msgs/Point32[7], cpa_m: float32, rule_compliant: bool, is_primary: bool, heading_deg: float32, speed_kn: float32}
- CasADi 求解超时处理：DEMO-1 不使用 CasADi，纯枚举无超时风险
- 8h 连续运行防内存泄漏：预分配 13 个候选缓冲区，禁止动态分配

**接口契约**：
- 新增订阅：`/l3/m4/behavior_plan`（BehaviorPlanMsg，heading/speed 区间约束）
- 新增订阅：`/l3/m6/colregs_constraints`（COLREGs_ConstraintMsg，CPA_safe 约束）
- 新增发布：`/sil/sat3_data`（sil_msgs/SAT3Data，含 trajectory_candidates[]）
- 修改发布：`/l3/m5/planning_output` 从 BC-MPC 求解结果填充
- 降级路径：BC-MPC 无可行解 → 输出当前航向保持 + confidence=0.0 + alert

### 3.3 D3.3a — M7-core Doer-Checker 6 类硬约束 + veto 启用

**上下文**：
- 架构权威：§11（M7 SafetySupervisor）、§11.9（override/handback 时序）
- 现有代码：Safety_AlertMsg 真实发布 ✅；6 类硬约束未实装；veto_enabled=false
- 关联：D3.1/D3.2（Doer 输出的监视对象）

**核心交付物**：

| 项 | 内容 | 工时 |
|---|---|---|
| 3.3a.1 | l3_params.yaml 设置 `veto_enabled: true` | 0.5h |
| 3.3a.2 | 6 类硬约束实装（<10ms 响应）：① CPA 一致性交叉验证（M2 vs M5 ±10%）② COLREGs 规则-几何一致性（M6 rule vs M5 trajectory）③ 模块心跳 watchdog（期望周期×1.5 容忍，3-miss 阈值）④ IEC 61508 SIL 2 DC≥90%（代码度量脚本）⑤ 速度限制合规（M5 output vs ODD speed limit）⑥ ROT 限制合规（M5 output vs ROT_max curve） | 12h |
| 3.3a.3 | VETO 逻辑：任一硬约束违反 → 发布 CheckerVetoMsg → M4/M5 回退 nearest_compliant | 4h |
| 3.3a.4 | PATH-S 独立性验证：CI 脚本确认 M7 不直接 import/link M1–M6、M8 | 2h |
| 3.3a.5 | M7 verdict 通过 /m7/sil_observability 只读 topic 发布（前端 Doer-Checker badge 数据源） | 2h |

**需探讨的设计决策**：
- 6 类硬约束的独立执行线程 vs 单循环：DEMO-1 使用单循环（2Hz tick），每 tick 顺序检查 6 类约束，总耗时 <10ms
- LOC ≥50:1（Doer:Checker）的保证：M7 核心约束逻辑控制在 ~200 LOC 以内（vs Doer M1–M6 合计 ~10000 LOC）
- watchdog 3-miss 阈值：M1/M2/M3/M4/M5/M6 各 2Hz → 期望周期 500ms → 容忍 750ms → 3-miss = 2.25s 无心跳 → 触发 veto

**接口契约**：
- 新增发布：`/l3/m7/veto`（CheckerVetoMsg，含 constraint_id / severity / rationale）
- 新增发布：`/m7/sil_observability`（M7VerdictMsg，含 pass/fail + 各约束结果）
- 修改订阅：M4/M5 监听 veto topic，收到后回退 nearest_compliant
- 降级路径：M7 自身故障 → M1 检测到 M7 心跳丢失 → ODD zone 升级 → 自动降级至 D2

### 3.4 D3.3b — M7-sotif 6 项指标计算 + /sil/sotif_metrics publisher

**上下文**：
- 架构权威：§11.3（SOTIF track）、§11.4（假设违反表）
- 现有代码：assumption_monitor.cpp（1 commit ahead）
- 关联：D2.7 HARA（SOTIF 危险源映射）

**核心交付物**：

| 项 | 内容 | 工时 |
|---|---|---|
| 3.3b.1 | 6 类假设违反检测实装：① AIS/雷达融合置信度 < 0.5 持续 >30s ② 目标运动预测 RMSE > 50m/30s ③ 感知盲区 > 20% ④ COLREGs 处理连续失败 3 次 ⑤ 通信 RTT > 2s 或丢包率 > 20% ⑥ X-axis Checker veto 比率 > 20%/100 周期 | 8h |
| 3.3b.2 | 100 周期 = 15s 滑动窗口（循环缓冲区） | 2h |
| 3.3b.3 | SotifMetrics IDL 定义 + publisher：sil_msgs/SotifMetrics，6 × {assumption_id, violation_score, window_count, is_violated} | 3h |
| 3.3b.4 | /sil/sotif_metrics publisher @10Hz | 1h |

**需探讨的设计决策**：
- 6 个假设的滑动窗口：独立缓冲区（每个假设 100 个 slot），内存开销可控（6 × 100 × sizeof(bool) = 600 bytes）
- DEMO-1 指标计算真实性：① 从 M2 track confidence 获取 ② 从 tracker_mock KF residual 获取 ③ 从 sensor_mock FOV 配置计算 ④ 从 M6 推理失败计数获取 ⑤ 从 DDS QoS 统计获取 ⑥ 从 M7 veto 计数获取
- sotif_metrics IDL 结构：6 × {assumption_id: string, violation_score: float32, window_count: uint16, is_violated: bool}

**接口契约**：
- 新增发布：`/sil/sotif_metrics`（sil_msgs/SotifMetrics）
- 新增订阅：M2 track confidence / tracker KF residual / M6 failure count / DDS QoS stats / M7 veto count
- 降级路径：无输入数据时 violation_score=0.0 / is_violated=false / window_count=0

### 3.5 D3.4 — M8 SAT-2/3/SOTIF 3 topic + sil_msgs IDL

**上下文**：
- 架构权威：§12（M8）、§12.2（SAT 自适应触发）
- 现有实装：SAT-1 12 topic ✅ / M8 C++ ROS + Python FastAPI ✅ / SatAggregator 已缓存 SAT-2/3 数据但被 (void)sat_cache 忽略
- 关联：D3.1（ivp_contributions 数据源）/ D3.2（trajectory_candidates 数据源）/ D3.3b（sotif_metrics 数据源）

**核心交付物**：

| 项 | 内容 | 工时 |
|---|---|---|
| 3.4.1 | sil_msgs 包新增 3 个 .msg 定义：SAT2Data / SAT3Data / SotifMetrics | 3h |
| 3.4.2 | M8 hmi_transparency_bridge_node.cpp 新增 3 个 publisher：/sil/sat2_data / /sil/sat3_data / /sil/sotif_metrics | 3h |
| 3.4.3 | 解除 UiStateBuilder 对 sat_cache 的忽略：从 SatAggregator 提取 SAT-2/3 数据填充到独立 topic | 2h |
| 3.4.4 | SAT-2 数据桥接：从 M4 ivp_contributions + M6 colregs_chain 聚合 → /sil/sat2_data | 3h |
| 3.4.5 | SAT-3 数据桥接：从 M5 trajectory_candidates + M2 forecast 聚合 → /sil/sat3_data | 2h |
| 3.4.6 | SOTIF 数据桥接：从 M7 sotif_metrics 转发 → /sil/sotif_metrics | 1h |

**sil_msgs IDL 定义**：

```
# SAT2Data.msg
std_msgs/Header header
IvpContribution[] ivp_contributions
ColregsChainLayer[] colregs_chain
string active_behavior
float32 reasoning_latency_ms

# IvpContribution.msg
float32 direction_deg
float32 utility_value
string behavior_id

# ColregsChainLayer.msg
string layer_name
string rule_id
float32 compliance_score
string rationale

# SAT3Data.msg
std_msgs/Header header
TrajectoryCandidate[] trajectory_candidates
float32 prediction_uncertainty
string predicted_state
float32 tdl_s
float32 tmr_s

# TrajectoryCandidate.msg
geometry_msgs/Point32[] waypoints
float32 cpa_m
bool rule_compliant
bool is_primary
float32 heading_deg
float32 speed_kn

# SotifMetrics.msg
std_msgs/Header header
SotifAssumption[] assumptions

# SotifAssumption.msg
string assumption_id
float32 violation_score
uint16 window_count
bool is_violated
```

**接口契约**：
- 新增订阅：`/l3/m4/behavior_plan`（ivp_contributions 来源）
- 新增订阅：`/l3/m6/colregs_output`（colregs_chain 来源）
- 新增订阅：`/l3/m5/planning_output`（trajectory_candidates 来源）
- 新增订阅：`/l3/m7/sotif_metrics`（SOTIF 指标来源）
- 新增发布：`/sil/sat2_data` @4Hz
- 新增发布：`/sil/sat3_data` @2Hz
- 新增发布：`/sil/sotif_metrics` @10Hz
- 降级路径：上游模块未启动时，对应 topic 不发布（前端显示等待提示）

---

## 4. DEMO-1 端到端数据流（真实化后）

```
AIS CSV ──→ ais_bridge/replay_node ──→ /sil/ais_tracks
                                            │
ship_dynamics (4-DOF MMG) ──→ /nav/filtered_state ──→ M2 World Model
                                            │               │
env_disturbance (from YAML) ──→ /environment_state    /world_model/tracks
                                            │               │
sensor_mock (from YAML noise) ──→ /sensor/detections     │
                                            │               │
tracker_mock (CPA/TCPA calc) ──→ /sil/tracked_targets ──→ M2 EncounterClassifier
                                            │
                                    M1 ODD Manager ←── /l3/diagnostics (heartbeat)
                                            │               │
                                    /l3/m1/odd_state     /l3/m2/encounter_class
                                            │               │
                                    M6 COLREGs Reasoner ←───┘
                                            │
                                    /l3/m6/colregs_output + /l3/m6/colregs_constraints
                                            │               │
                                    M4 Behavior Arbiter ←───┘
                                    (IvP solver 接入)
                                            │
                                    /l3/m4/behavior_plan + /sil/sat2_data
                                            │               │
                                    M5 Tactical Planner ←───┘
                                    (BC-MPC 13 弧枚举)
                                            │
                                    /l3/m5/planning_output + /sil/sat3_data
                                            │               │
                                    M7 Safety Supervisor ←───┘
                                    (6 类硬约束 + veto)
                                            │
                                    /l3/m7/veto + /m7/sil_observability + /sil/sotif_metrics
                                            │
                                    M8 HMI Bridge (SAT 聚合)
                                            │
                                    /l3/m8/ui_state + /sil/sat2_data + /sil/sat3_data + /sil/sotif_metrics
                                            │
                                    foxglove_bridge → WebSocket → Web HMI
                                            │
                                    SAT-1 (状态) + SAT-2 (IvP+COLREGs) + SAT-3 (轨迹) + SOTIF (假设)
```

**关键特性**：
- ✅ 零 mock：所有节点输出真实计算值
- ✅ M1 ODD 评估基于真实环境/传感器/心跳数据
- ✅ M4 IvP 仲裁基于真实多目标加权求和
- ✅ M5 BC-MPC 避碰基于真实 Nomoto + 13 弧枚举
- ✅ M7 Doer-Checker 真实 veto
- ✅ M8 SAT-2/3/SOTIF 真实桥接
- ✅ 前端 3 handler 有真实数据源

---

## 5. 工时汇总与关键路径

| Phase | D 任务 | 工时 | 截止 | 关键路径 |
|---|---|---|---|---|
| Phase 1 | D1.3.2.3 + D1.6 | 0.5 pw | 6/1 | 否 |
| Phase 2 | D2.1 + D2.2 + D2.4 + D2.5 | 3.0 pw | 6/5 | D2.5 tracker 是 M2 输入依赖 |
| Phase 3 | D3.4（IDL + M8 publisher） | 1.5 pw | **6/8** | 🔴 D3.1/D3.2/D3.3b 的 publisher 依赖 |
| Phase 3 | D3.1（M4 IvP） | 4.0 pw | **6/12** | 🔴 SAT-2 数据源 |
| Phase 3 | D3.2（M5 BC-MPC） | 4.0 pw | **6/12** | 🔴 SAT-3 数据源 |
| Phase 3 | D3.3a（M7 veto） | 2.5 pw | 6/12 | 🟡 不阻塞演示但影响可信度 |
| Phase 3 | D3.3b（M7 SOTIF） | 1.5 pw | 6/12 | 🟡 不阻塞演示但影响可信度 |
| **总计** | | **~17 pw** | **6/15** | |

**关键路径**：D3.4 IDL（6/8）→ D3.1 M4 IvP + D3.2 M5 BC-MPC（6/12）→ 端到端集成测试（6/13–14）→ DEMO-1（6/15）

---

## 6. 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|---|---|---|---|
| M5 BC-MPC Nomoto 参数不准导致轨迹不收敛 | 中 | 中 | 预先在 D1.3.1' 参考解中验证 Nomoto 参数；准备 3 组参数配置 |
| M4 IvP 接入后求解超时 >50ms | 低 | 高 | 预先 benchmark 1584 格求解时间；超时回退硬优先级 |
| M7 veto 误触发导致 M4/M5 频繁回退 | 中 | 中 | 初始阈值保守（CPA ±20% 容忍 / 3-miss watchdog）；演示前调参 |
| sil_msgs IDL 与前端 TypeScript 类型不匹配 | 低 | 中 | D3.4 先出 IDL，前端类型同步更新 |
| DEMO-1 当天环境问题（Docker/网络） | 低 | 高 | 提前 2 天在演示环境完整彩排 |

---

## 7. 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-05-25 | 初版：基于 GAP 评审 23 项 mock 清查 + 方案 A 全栈真实化设计 |
