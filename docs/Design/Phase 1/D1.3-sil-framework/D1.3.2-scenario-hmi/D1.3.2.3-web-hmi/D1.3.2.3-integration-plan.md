# D1.3.2.3 · Web HMI Captain 视图 · 集成补全计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 关闭 DEMO-1（2026-06-15）Web HMI Captain 视图端到端真数据联通的 5 项剩余 gap：ARPA BRG/RNG 实计算、demo fallback 显式标识、ENC raster tile fallback、Playwright e2e 自动化验证、demo 录屏脚本。

**Architecture:** 纯前端补全——不修改 protobuf schema、不修改 ROS2 topic。BRG/RNG 在 ArpaTargetTable.tsx 内从 ownShip/target pose 客户端计算（haversine + bearing 公式）。demo fallback 标识在 SimulationMonitor.tsx 底部栏追加红色标签。ENC raster 通过 MapLibre 已有 OSM raster 源 + OpenSeaMap overlay 扩展。Playwright e2e 测试沿用现有 `web/e2e/` 模式。

**Tech Stack:** TypeScript, React 18, Zustand (telemetryStore), MapLibre GL JS 4.x, Playwright (e2e), Bash (capture script), Vitest (unit test)

**依赖:** D1.3.2-integration（topic bridge 接通）必须先关闭。

**不在范围:** SAT-2 IvP、SAT-3 MPC、SOTIF、Apache Arrow replay、GIF batch、GSAP scrubber、S-57→MVT 真管线（全部 Phase 2）

---

## Task 分割

| Task | 主题 | 文件 | 提交 |
|---|---|---|---|
| T1 | BRG/RNG 客户端计算 + ArpaTargetTable 替换 | navMath.ts (new), bearingRange.test.ts (new), ArpaTargetTable.tsx | `b32e715` |
| T2 | DEMO MODE fallback 显式标识 | SimulationMonitor.tsx | `04c6710` |
| T3 | ENC raster tile fallback（OpenSeaMap overlay）| SilMapView.tsx | `bc26eac` |
| T4 | Playwright e2e — DEMO-1 R14 head-on | demo1-headon-r14.spec.ts (new) | `c92e3e5` |
| T5 | DEMO-1 录屏脚本 | scripts/demo1_capture.sh (new) | `2083142` |
| T6 | 集成验证 + DoD checklist | 本文件 | `（本 commit）` |

---

## Verification — DEMO-1 Captain View 全闭判据

### 自动化验证结果（2026-05-20 13:15 CST）

| 检查项 | 状态 | 证据 |
|---|---|---|
| `npx vitest run` | ✅ 19/20 files pass, 94/96 tests pass | 2 pre-existing failures (GateSequencer.test.tsx), zero new |
| `npx vitest run bearingRange.test.ts` | ✅ 8/8 pass | computeBearing + computeRangeNm all cases |
| `npx tsc --noEmit` | ✅ zero new type errors | Pre-existing errors in SimulationMonitor/SilMapView unchanged |
| `npx playwright test --list` | ✅ 8 demo1-headon tests listed | All 8 test cases visible |
| `git log --oneline` | ✅ 5 D1.3.2.3 commits on main | b32e715, 04c6710, 2083142, c92e3e5, bc26eac |
| `bash -n scripts/demo1_capture.sh` | ✅ zero syntax errors | Script passes bash syntax check |

### 人工验证（浏览器打开 — 需 docker compose up + Vite dev server）

1. `docker compose up` → 打开 `http://localhost:5173/#/monitor/imazu-08`
2. [ ] own_ship 在 MapLibre 上真 4-DOF MMG 运动
3. [ ] target_ship 按 scenario YAML 初值 + 简化运动学走
4. [ ] ConningBar 7 字段从 `/sil/own_ship_state` 真订阅
5. [ ] ThreatRibbon CPA chip 颜色动态变化（绿 >2nm / 琥珀 1-2 / 红 <1）
6. [ ] ModulePulseBar 8 格随 M1-M8 真心跳 GREEN/AMBER/RED
7. [ ] ARPA 表 BRG/RNG 列有真值（非 `—`）
8. [ ] ENC 底图可见（OSM raster + OpenSeaMap 航海标记 overlay）
9. [ ] ToR Modal → FSM TOR→OVERRIDE → ASDR Ledger 真追加 take_over event
10. [ ] 全程无 "DEMO MODE — dead-reckon fallback" 红色标签（WS 连通时）

### 录屏产物

- [ ] `bash scripts/demo1_capture.sh` 生成 ≤5 min webm 视频 + 1 帧 PNG 截图

### 回归验证

- [ ] `cd web && npx vitest run` 全部 PASS（零回归）
- [ ] `cd web && npx tsc --noEmit` 零类型错误（本 plan 引入的文件）

---

## 修订记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-05-20 | 初版。5 Task + DoD checklist |
| v1.1 | 2026-05-20 | 实施完成。5 commits on main，验证结果已追加。T2 commit 手动修复（子代理提交未入 main）|
