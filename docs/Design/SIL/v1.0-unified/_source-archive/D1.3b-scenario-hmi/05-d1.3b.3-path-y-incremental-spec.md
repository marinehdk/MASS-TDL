# D1.3b.3 Path Y — DEMO-1 HMI 增量补全 Spec

**版本**：v1.0
**日期**：2026-05-11
**D 编号**：D1.3b.3 (Path Y incremental)
**Owner**：M8 前端负责人 + 技术负责人
**完成日期**：2026-05-11 EOD
**工时预算**：~9h（5 项增量 Tasks）

---

## §0 设计决策

| 决策 | 内容 | Rationale |
|---|---|---|
| **sil_mock_node 扩展方案** | 方案 A：在原 `sil_mock_node.py` 内追加 publisher，不建新节点 | 单节点一键启动完整 DEMO-1，最简 |
| **状态管理** | App.tsx props drilling（selectedScenario, jobStatus），不引 React context | 5 个组件共享状态量少，context 过度设计 |
| **ScenarioSelector 样式** | `<select>` dropdown with night theme styling | 10 个场景名，select 原生可访问性最优，不比自定义 dropdown 差 |
| **PlaybackControls 轮询** | `setInterval` 1s 轮询 `/sil/scenario/status/{job_id}` | sil_router 已有 endpoint，不需要 WebSocket 双通道 |
| **ODDPanel 数据源** | 复用 `useSilDebug` 的 `odd` 字段（已有 SilODDPanel schema） | sil_ws.py 已广播 ODD 数据，不另起 hook |
| **SAT tabs** | 纯 React state `satTab` ∈ {1,2,3}，默认 1 | 纯前端切换，不需要路由 |

---

## §1 当前基线

### 1.1 已有组件

| 文件 | 作用 |
|---|---|
| `App.tsx` | 顶层 grid（map + 320px SidePanel），foxglove + silDebug 数据流，R14 fallback |
| `SidePanel.tsx` | 6 卡片：CPA/TCPA/Rule/Decision/Pulse/ASDR |
| `MapView.tsx` | MapLibre GL 地图，ENC 底图，own-ship/target sprites |
| `ChartLegend.tsx` | 水深色阶 + 符号图例 |
| `useSilDebug.ts` | WebSocket `/ws/sil_debug` → `SilDebugData` |
| `useFoxgloveBridge.ts` | foxglove_bridge :8765 → ownShip/targets/connected |
| `demoTarget.ts` | 静态 R14 fallback 数据 |

### 1.2 已有后端

| 端点 | 状态 |
|---|---|
| `GET /sil/scenario/list` | ✅ 返回 colregs/*.yaml 文件名 |
| `POST /sil/scenario/run` | ✅ 启动 batch job |
| `GET /sil/scenario/status/{id}` | ✅ 返回 {status, progress} |
| `/ws/sil_debug` | ✅ 广播 SilDebugSchema (sat1 + odd + job_status) |
| `sil_mock_node` | ✅ SAT/ODD stub @1Hz（**不**发 nav/tracks） |

---

## §2 新增组件设计

### 2.1 SidePanel 新布局

```
┌──────────────────────────────────┐
│ [SAT-1] [SAT-2] [SAT-3]          │  ← SATView tabs (Task 5)
├──────────────────────────────────┤
│ Scenario: [R14 Head-on     ▾]    │  ← ScenarioSelector (Task 1)
├──────────────────────────────────┤
│ [▶ Play] [⏸ Pause] [⏹ Reset]    │  ← PlaybackControls (Task 2)
│ Status: IDLE                     │
├──────────────────────────────────┤
│ ODD: A · NOMINAL · 0.87          │  ← ODDPanel (Task 4)
├──────────────────────────────────┤
│ [CPA 0.18 nm] [TCPA 520 s]       │  ← existing
├──────────────────────────────────┤
│ RulePanel · DecisionPanel · etc  │  ← existing
└──────────────────────────────────┘
```

### 2.2 Task 1: ScenarioSelector.tsx

**Files**: Create `web/src/components/ScenarioSelector.tsx`

```tsx
import { useEffect, useState, memo } from 'react';

interface Props {
  value: string;
  onChange: (id: string) => void;
}

export const ScenarioSelector = memo(function ScenarioSelector({ value, onChange }: Props) {
  const [scenarios, setScenarios] = useState<string[]>([]);

  useEffect(() => {
    fetch('/sil/scenario/list')
      .then(r => r.json())
      .then(setScenarios)
      .catch(() => setScenarios([]));
  }, []);

  return (
    <div style={{
      padding: '10px 14px',
      borderBottom: '1px solid var(--line-1)',
    }}>
      <label style={{
        fontFamily: 'var(--fnt-mono)',
        fontSize: '9px',
        color: 'var(--text-2)',
        textTransform: 'uppercase',
        letterSpacing: '1px',
        display: 'block',
        marginBottom: '6px',
      }}>
        Scenario
      </label>
      <select
        value={value}
        onChange={e => onChange(e.target.value)}
        style={{
          width: '100%',
          background: 'var(--bg-2)',
          color: 'var(--text-1)',
          border: '1px solid var(--line-1)',
          fontFamily: 'var(--fnt-mono)',
          fontSize: '11px',
          padding: '6px 8px',
          borderRadius: 'var(--radius-none)',
          outline: 'none',
        }}
      >
        {scenarios.map(s => (
          <option key={s} value={s}>{s.replace('.yaml', '')}</option>
        ))}
      </select>
    </div>
  );
});
```

**App.tsx 集成**：
```tsx
const [selectedScenario, setSelectedScenario] = useState('colreg-rule14-ho-001-seed42-v1.0.yaml');
// Pass to SidePanel as prop
<SidePanel data={effectiveSilData} scenarioId={selectedScenario} onScenarioChange={setSelectedScenario} />
```

### 2.3 Task 2: PlaybackControls.tsx

**Files**: Create `web/src/components/PlaybackControls.tsx`

```tsx
import { useState, useRef, useEffect, memo } from 'react';

type JobStatus = 'IDLE' | 'RUNNING' | 'DONE' | 'ERROR';

interface Props {
  scenarioId: string;
  disabled: boolean;
}

const STATUS_COLORS: Record<JobStatus, string> = {
  IDLE: 'var(--text-2)',
  RUNNING: '#f59e0b',
  DONE: 'var(--color-stbd)',
  ERROR: 'var(--color-port)',
};

export const PlaybackControls = memo(function PlaybackControls({ scenarioId, disabled }: Props) {
  const [jobStatus, setJobStatus] = useState<JobStatus>('IDLE');
  const [jobId, setJobId] = useState<string | null>(null);
  const pollRef = useRef<ReturnType<typeof setInterval> | null>(null);

  useEffect(() => {
    return () => { if (pollRef.current) clearInterval(pollRef.current); };
  }, []);

  const play = async () => {
    setJobStatus('RUNNING');
    const res = await fetch('/sil/scenario/run', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ scenario_ids: [scenarioId] }),
    });
    const { job_id } = await res.json();
    setJobId(job_id);
    pollRef.current = setInterval(async () => {
      const r = await fetch(`/sil/scenario/status/${job_id}`);
      const { status } = await r.json();
      if (status === 'done') { setJobStatus('DONE'); clearInterval(pollRef.current!); }
      else if (status === 'failed') { setJobStatus('ERROR'); clearInterval(pollRef.current!); }
    }, 1000);
  };

  const reset = () => {
    if (pollRef.current) clearInterval(pollRef.current);
    setJobStatus('IDLE');
    setJobId(null);
  };

  const btn = (label: string, action: () => void, enable: boolean) => (
    <button onClick={action} disabled={!enable} style={{
      fontFamily: 'var(--fnt-mono)',
      fontSize: '10px',
      padding: '4px 10px',
      background: enable ? 'var(--bg-2)' : 'var(--bg-1)',
      color: enable ? 'var(--text-1)' : 'var(--text-3)',
      border: '1px solid var(--line-1)',
      borderRadius: 'var(--radius-none)',
      cursor: enable ? 'pointer' : 'default',
    }}>{label}</button>
  );

  return (
    <div style={{
      padding: '10px 14px',
      borderBottom: '1px solid var(--line-1)',
      display: 'flex',
      alignItems: 'center',
      gap: '6px',
    }}>
      {btn('▶ Play', play, !disabled && jobStatus !== 'RUNNING')}
      {btn('⏹ Reset', reset, jobStatus !== 'IDLE')}
      <span style={{
        fontFamily: 'var(--fnt-mono)',
        fontSize: '9px',
        color: STATUS_COLORS[jobStatus],
        marginLeft: 'auto',
      }}>
        {jobStatus}
      </span>
    </div>
  );
});
```

### 2.4 Task 3: sil_mock_node 扩展

**Files**: Modify `src/sim_workbench/sil_mock_publisher/sil_mock_publisher/sil_mock_node.py`

在原 `SilMockNode` 类中追加：

```python
# 新增 import
from l3_external_msgs.msg import TrackedTargetArray
from l3_msgs.msg import TrackedTarget, EncounterClassification
from geographic_msgs.msg import GeoPoint
import math
import time

# 新增 __init__ 中的 publisher
self._pub_nav = self.create_publisher(FilteredOwnShipState, "/nav/filtered_state", 10)
self._pub_tracks = self.create_publisher(TrackedTargetArray, "/world_model/tracks", 10)
self._nav_timer = self.create_timer(0.02, self._publish_nav)  # 50 Hz
self._track_timer = self.create_timer(0.5, self._publish_tracks)  # 2 Hz
self._t0 = time.time()
self._own_lat0, self._own_lon0 = 63.435, 10.395  # Trondheim Fjord

# 新增方法
def _publish_nav(self) -> None:
    """Own-ship: heading 0° at 18 kn, moving north."""
    elapsed = time.time() - self._t0
    dist_nm = 18.0 * elapsed / 3600.0
    lat = self._own_lat0 + dist_nm / 60.0
    lon = self._own_lon0

    msg = FilteredOwnShipState()
    msg.schema_version = "v1.1.2"
    msg.stamp = self.get_clock().now().to_msg()
    msg.position.latitude = lat
    msg.position.longitude = lon
    msg.sog_kn = 18.0
    msg.cog_deg = 0.0
    msg.heading_deg = 5.0
    msg.confidence = 0.9
    self._pub_nav.publish(msg)


def _publish_tracks(self) -> None:
    """Target ship: heading 180° at 10 kn, starting ~2.5nm north, closing."""
    elapsed = time.time() - self._t0
    dist_nm = 10.0 * elapsed / 3600.0
    ts_lat = 63.458 - dist_nm / 60.0  # moving south
    ts_lon = 10.395
    dlat_nm = (ts_lat - (self._own_lat0 + (18.0 * elapsed / 3600.0) / 60.0)) * 60.0
    dlon_nm = 0.0
    cpa_nm = abs(dlat_nm)
    tcpa_s = max(0, cpa_nm / ((18.0 + 10.0) / 3600.0))

    t = TrackedTarget()
    t.schema_version = "v1.1.2"
    t.target_id = 1
    t.stamp = self.get_clock().now().to_msg()
    pos = GeoPoint()
    pos.latitude = ts_lat
    pos.longitude = ts_lon
    t.position = pos
    t.sog_kn = 10.0
    t.cog_deg = 180.0
    t.heading_deg = 180.0
    t.cpa_m = cpa_nm * 1852.0
    t.tcpa_s = tcpa_s
    t.classification = "vessel"
    t.classification_confidence = 0.7
    t.source_sensor = "ais"
    t.confidence = 0.7

    msg = TrackedTargetArray()
    msg.schema_version = "v1.1.2"
    msg.targets = [t]
    msg.confidence = 0.7
    msg.rationale = "R14 head-on mock trajectory"
    self._pub_tracks.publish(msg)
```

### 2.5 Task 4: ODDPanel.tsx

**Files**: Create `web/src/components/OddPanel.tsx`

```tsx
import { memo } from 'react';

interface OddData {
  zone: string;
  envelope_state: string;
  conformance_score: number;
  confidence: number;
}

interface Props {
  odd: OddData | null;
}

const ZONE_COLORS: Record<string, string> = {
  'A': 'var(--color-stbd)',
  'B': '#f59e0b',
  'C': 'var(--color-port)',
  'D': '#ef4444',
};

export const OddPanel = memo(function OddPanel({ odd }: Props) {
  if (!odd) return null;
  return (
    <div style={{
      padding: '10px 14px',
      borderBottom: '1px solid var(--line-1)',
      display: 'flex',
      alignItems: 'center',
      gap: '8px',
    }}>
      <span style={{
        fontFamily: 'var(--fnt-mono)',
        fontSize: '9px',
        color: 'var(--text-2)',
        textTransform: 'uppercase',
        letterSpacing: '1px',
      }}>ODD</span>
      <span style={{
        fontFamily: 'var(--fnt-mono)',
        fontSize: '12px',
        fontWeight: 600,
        color: ZONE_COLORS[odd.zone?.charAt(0)] ?? 'var(--text-1)',
        padding: '2px 6px',
        background: 'var(--bg-2)',
        border: '1px solid var(--line-1)',
        borderRadius: 'var(--radius-none)',
      }}>{odd.zone}</span>
      <span style={{
        fontFamily: 'var(--fnt-mono)',
        fontSize: '10px',
        color: odd.envelope_state === 'IN' ? 'var(--color-stbd)' : 'var(--color-port)',
      }}>{odd.envelope_state}</span>
      <span style={{
        fontFamily: 'var(--fnt-mono)',
        fontSize: '9px',
        color: 'var(--text-2)',
        marginLeft: 'auto',
      }}>{(odd.conformance_score * 100).toFixed(0)}%</span>
    </div>
  );
});
```

### 2.6 Task 5: SATView.tsx (tabs)

**Files**: Create `web/src/components/SatView.tsx`

```tsx
import { useState, memo, type ReactNode } from 'react';

interface Props {
  sat1Content: ReactNode;
}

type SatTab = 1 | 2 | 3;

export const SatView = memo(function SatView({ sat1Content }: Props) {
  const [tab, setTab] = useState<SatTab>(1);

  const tabStyle = (t: SatTab) => ({
    flex: 1,
    padding: '6px 0',
    textAlign: 'center' as const,
    fontFamily: 'var(--fnt-mono)',
    fontSize: '9px',
    textTransform: 'uppercase' as const,
    letterSpacing: '1px',
    color: tab === t ? 'var(--text-1)' : 'var(--text-2)',
    background: tab === t ? 'var(--bg-2)' : 'transparent',
    border: 'none',
    borderBottom: tab === t ? '2px solid var(--color-stbd)' : '2px solid transparent',
    cursor: 'pointer',
  });

  const placeholder = (label: string, phase: string) => (
    <div style={{
      padding: '20px 14px',
      fontFamily: 'var(--fnt-mono)',
      fontSize: '10px',
      color: 'var(--text-3)',
      textAlign: 'center',
      borderBottom: '1px solid var(--line-1)',
    }}>{label} — {phase}</div>
  );

  return (
    <div>
      <div style={{
        display: 'flex',
        borderBottom: '1px solid var(--line-1)',
      }}>
        <button style={tabStyle(1)} onClick={() => setTab(1)}>SAT-1</button>
        <button style={tabStyle(2)} onClick={() => setTab(2)}>SAT-2</button>
        <button style={tabStyle(3)} onClick={() => setTab(3)}>SAT-3</button>
      </div>
      <div style={{ display: tab === 1 ? 'block' : 'none' }}>{sat1Content}</div>
      {tab === 2 && placeholder('Comprehension subsystem', 'Phase 2 增项')}
      {tab === 3 && placeholder('Projection subsystem', 'D3.4')}
    </div>
  );
});
```

### 2.7 SidePanel 集成

修改 `SidePanel.tsx`，新 props + 新布局：

```tsx
interface Props {
  data: SilDebugData | null;
  scenarioId: string;
  onScenarioChange: (id: string) => void;
}

export const SidePanel = memo(function SidePanel({ data, scenarioId, onScenarioChange }: Props) {
  const d = data;

  const sat1Cards = (
    <>
      <div style={{ padding: '14px', borderBottom: '1px solid var(--line-1)' }}>
        <div style={{ display: 'flex', gap: '8px' }}>
          <CpaPanel cpaNm={d?.cpa_nm ?? 0} />
          <TcpaPanel tcpaS={d?.tcpa_s ?? 0} />
        </div>
      </div>
      <RulePanel ruleText={d?.rule_text ?? ''} />
      <DecisionPanel decisionText={d?.decision_text ?? ''} />
      <ModulePulseBar moduleStatus={d?.module_status ?? Array(8).fill(false)} />
      <AsdrLog events={d?.asdr_events ?? []} />
    </>
  );

  return (
    <div style={{ width: '320px', background: 'var(--bg-1)', borderLeft: '1px solid var(--line-1)', display: 'flex', flexDirection: 'column', overflowY: 'auto', flexShrink: 0 }}>
      <SatView sat1Content={sat1Cards} />
      <ScenarioSelector value={scenarioId} onChange={onScenarioChange} />
      <PlaybackControls scenarioId={scenarioId} disabled={!scenarioId} />
      <OddPanel odd={d?.odd ?? null} />
    </div>
  );
});
```

---

## §3 App.tsx 变更

```tsx
// 新增 state
const [selectedScenario, setSelectedScenario] = useState('colreg-rule14-ho-001-seed42-v1.0.yaml');

// 修改 SidePanel 调用
<SidePanel
  data={effectiveSilData}
  scenarioId={selectedScenario}
  onScenarioChange={setSelectedScenario}
/>
```

---

## §4 不改动文件（回归边界）

- `MapView.tsx` — 不改
- `ChartLegend.tsx` — 不改
- `useFoxgloveBridge.ts` — 不改
- `useSilDebug.ts` — 不改
- `sil_router.py` — 不改
- `sil_ws.py` — 不改
- `sil_schemas.py` — 不改
- `app.py` — 不改

---

## §5 DoD（5 项全部完成后）

- [ ] ScenarioSelector 渲染 10 个场景名，默认 R14，可切换
- [ ] PlaybackControls Play → 后端 batch_runner 启动 → 状态 RUNNING → 完成变 DONE
- [ ] sil_mock_node 发布 `/nav/filtered_state` @50Hz + `/world_model/tracks` @2Hz（R14 head-on 轨迹）
- [ ] ODDPanel 显示 zone=A · NOMINAL · 87%
- [ ] SAT-1/2/3 tab bar 可切换，SAT-2/3 显示占位，SAT-1 含全部 6 卡片
- [ ] 现有 30 个 pytest 零回归
- [ ] `web/` 零 TypeScript 编译错误
- [ ] 5 张截图 + 1 个 GIF 收集到 `demo-evidence/`

---

## §6 来源与置信度

| 断言 | 来源 | 置信度 |
|---|---|---|
| sil_router 4 端点已实现 | 仓库代码 sil_router.py | 🟢 High |
| sil_ws /ws/sil_debug 广播 SilDebugSchema | 仓库代码 sil_ws.py | 🟢 High |
| SidePanel 现有 6 卡片 | 仓库代码 SidePanel.tsx | 🟢 High |
| sil_mock_node 仅发 SAT/ODD | 仓库代码 sil_mock_node.py | 🟢 High |

---

*文档版本 v1.0 · 2026-05-11 · brainstorming 产出*
