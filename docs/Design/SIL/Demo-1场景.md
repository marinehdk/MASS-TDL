Viewed imazu-01-ho.yaml:1-70

根据对标准对遇场景文件 `imazu-01-ho.yaml` 的深入解析，该场景是经典 **Imazu (1987) Case 01 — Head-On（对遇避碰测试）**。

在符合 COLREGs（国际海上避碰规则）设计及系统理论设计的闭环闭口状态下，仿真执行的完整物理过程和状态演变如下。您可以对照前端 HMI 进行全流程的比对验证：

---

### 阶段一：初始直航阶段（Simulation Time: `T+ 0s` 至 `T+ 200s`）
*   **物理状态**：
    *   **本船 (OS)** 从 `(63.44°N, 10.38°E)` 出发，以速度 **`10.0 kn`** 沿着 **`0.0°` (正北)** 航向直线航行。
    *   **目标船 (TS1)** 从 `(63.557451°N, 10.38°E)` 出发，以速度 **`10.0 kn`** 沿着 **`180.0°` (正南)** 航向直线迎面驶来。
    *   初始时两船相距约 **7.05 海里**，处于同一经度线上，形成绝对的**正面对遇 (Head-On)** 态势。
*   **HMI 前端体现**：
    *   此时处于 `TRANSIT` (直航) 模式。
    *   本船与目标船均显示绿色航迹线（COG Leader Line）笔直指向对方。
    *   由于相距较远，本船四周的三个安全几何扇区（M7 警戒边界）为默认大小（观察区 2.2nm / 起作用区 1.2nm / 紧急区 0.3nm），无变色预警。

---

### 阶段二：探测分类与规则激活阶段（Simulation Time: `T+ 200s` 至 `T+ 300s`）
*   **后台与物理状态**：
    *   随着两船以约 **`20 kn`** 的相对速度对向接近，距离缩短至约 **5.0 海里** 内。
    *   **M2 World Model** 与 **M6 COLREGs Reasoner** 持续计算 CPA（最近相遇距离）和 TCPA（最近相遇时间）。CPA 趋近于零，发生碰撞危险。
    *   **M6 规则推理机** 判定此相遇状态符合 **Rule 14 (对遇局面/Head-On)**，激活规避规则。
*   **HMI 前端体现**：
    *   一旦检测到冲突，状态机会从 `TRANSIT` 转换为 `COLREG_AVOIDANCE` 状态。
    *   在 **G 键工程视角** 下，雷达 PPI 及主地图上会显示 **M6 决策溯源树** 激活了 **"Rule 14 Head-On"**，并且指示 `expected_own_action: turn_starboard` (期望自船向右转向避让)。

---

### 阶段三：避碰向右转向执行阶段（Simulation Time: `T+ 300s` 至 `T+ 450s`）
*   **后台与物理状态**：
    *   根据场景配置，在接近 `T+ 300s` 时，避碰决策强制输出。
    *   **M5 Tactical Planner (Mid-MPC)** 结合 M6 的 Rule 14 约束（禁止向左转向），将右舷可行航向的安全 utilities 权重放大，计算出一条**向右（东侧）**偏航的避让轨迹。
    *   本船执行右转动作，避碰偏角预计达到 **`0.61 rad` (~35°)**。
    *   两船将在本船的 **左舷** 星展驶过（Starboard-to-Starboard 避让，各船向右，左舷对左舷驶过）。
*   **HMI 前端体现**：
    *   本船的航向向右发生明显偏折（Heading 偏向 `35°` 左右）。
    *   在 **G 键工程视角** 下，会呈现出绿色的 **M5 MPC 多路径候选簇 (SAT-3)**，其中最粗的绿色路径弯向右侧，且本船四周的安全几何包络线中的左侧红区因排除了左转可能而高度强化。

---

### 阶段四：驶过与偏航回归阶段（Simulation Time: `T+ 450s` 至 `T+ 600s`）
*   **后台与物理状态**：
    *   避碰动作持续约 `90秒`（`avoidance_duration_s`）。
    *   在相对运动中，两船横距（CPA）保持在 **500米 (约 0.27 NM)** 以上安全驶过。
    *   一旦 TS1 越过本船的横向安全包络边界，**M2 世界模型** 解除碰撞警报（`conflict_detected = false`）。
    *   **M5 规划器** 自动将参考目标重新切换回 L2 标称航线段，开始修正方向偏角，平滑回归 nominal 航线。
*   **HMI 前端体现**：
    *   在地图上可以看到本船的历史轨迹（点状虚线）向右划出一道漂亮的避让弧线，随后船头转回正北（`0.0°`），本船图标重新完美归轨在标称直线上。
    *   FSM 状态机无缝切回 `TRANSIT`。

---

### 阶段五：仿真结束与自动评估（Simulation Time: `T+ 600s` 至 `T+ 700s`）
*   **物理状态**：
    *   仿真总时长达到 `700秒` 后，本船已完全脱离危险，重回稳定直航，模拟自动停止。
*   **HMI 前端体现**：
    *   页面自动完成第三屏跳转，切入 **B4 仿真报告 (Simulation Evaluator)**。
    *   报告中自动提取并判定评估指标：
        *   **最小避碰横距 (Min CPA)** 是否满足 `cpa_min_m_ge: 500.0` 限制（绿灯通过 ✅）。
        *   航行决策轨迹的合规性与平稳得分自动列出。

---

### 💡 您的两张截图现象解构（为什么截图里两船似乎是“同向平行”航行？）

*   **截图 1 (`T+ 43:27`)**：
    *   仔细看图，本船 SOG 4.2 kn，航向 `338°`；目标船 SOG 0.9 kn，航向 `331°`，目标船实际上已经运行到了本船的右侧（绿色 Overtaking 包络线和左侧边界）。
*   **截图 2 (`T+ 46:23`)**：
    *   由于之前后台对 AIS 和 Tracker Mock 进行了重构调试，测试数据和心跳 stubs 可能发生过局部拥堵重播，两船此时以极低速平行滑动。
*   **对比结论**：
    *   在全新合并代码后，仿真已完全还原为 **10节对向碰撞** 物理态势。您在刷新并启动仿真后，应当能观察到本船在相距约 `2.5nm - 3nm` 时，果断**向右舷转向**，并以超过 500 米的安全距离与目标船左舷相对驶过，随后切回正北方向的完美避碰全闭环。