在当前的 **“仿真运行”（Simulation Monitor）** 页面设计中，针对**本船（Own Ship）**及其周边的态势感知，系统设计了两个维度的主题图层展示：**普通监控视角（Captain/ROC 视角）** 和 **算法诊断视角（Engineer/God 视角）**。

结合您的截图与代码库中 `SilMapView.tsx` 和 `SimulationMonitor.tsx` 的实际实现，本船周边设计展示的内容如下：

---

### 一、 基础导航与本船状态层（所有视角均展示）

1. **本船图标与状态（Own Ship Marker）**
   - **样式**：高亮青色（Teal, `#2dd4bf`）的圆形图标，带有代表船首指向的三角形尖端。
   - **旋转**：根据实时的**船首向（Heading, HDG）**在地图上动态旋转。
   - **交互**：点击本船图标，可将其置为选中状态（Selected），并在上方弹出悬浮数据牌。

2. **悬浮数据牌（HUD Plaque & Leader Line）**
   - **展示内容**：
     - **HDG**：船首向（°）
     - **COG**：对地航向（°）
     - **SOG**：对地航速（节, kn）
     - **ROT**：转向率（°/分, °/m）
   - **设计细节**：数据牌下方有一条虚线引向船体中心。无论地图缩放或本船转向，数据牌均通过**反向旋转算法**保持水平直立，确保文字阅读的舒适度。

3. **对地航向矢量指示线（COG Leader Line）**
   - **样式**：从船首延伸出的青色实线（`#2dd4bf`）。
   - **长度**：基于当前对地航速（SOG）的 **6分钟预测距离**（Scaled Vector）动态伸缩，用于直观预判本船的短期运动趋势。

4. **历史航迹线（Historical Trail）**
   - **样式**：一条虚线青色轨迹（`#2dd4bf`），直观呈现本船在历史周期内的实际行驶轨迹。

---

### 二、 避碰评估与安全警戒层（普通监控视角 + 算法视角）

5. **避碰评估环（CPA / Danger Rings）**
   - **样式**：以本船为中心的红黄色虚线圆环（截图中的红色虚线圈）。
   - **设计半径**：分别为 **0.5 海里（0.5 nm）** 和 **1.0 海里（1.0 nm）**，作为避碰态势的距离基准线。

6. **搁浅风险实时高亮层（Grounding Hazard Highlight）**
   - **工作原理**：后台以 **10 Hz** 的高频扫描本船的位置与航向，并与海图数据源（S-57 矢量图层中的陆地、浅滩、未勘测区等）进行碰撞几何计算。
   - **设计样式**：一旦预测有搁浅风险，与本船可能发生碰撞的浅滩或陆地几何区域，将在海图上以**半透明亮红色（`#ff0000`，40% 不透明度）**的填充图层进行动态高亮闪烁，发出搁浅警示。

7. **PPI 雷达扫描圈与距离比例尺（PPI Rings & Scale）**
   - **PPI 圈**：以视口中心（普通视角下偏下方 `[50, 70]`）为圆心，渲染多层同心距离评估圈，半径分别为 40px、80px、160px、320px。
   - **距离比例尺**：底部正中展示每像素代表的海里单位（如 `0.01 nm/px`）。

---

### 三、 算法决策与诊断分析图层（仅在 工程师/全局 视角下展示，或避碰状态激活时展示）

当视角切换为 **工程师模式（Engineer）**、**全局模式（God）**，或本船状态机处于 **“主动避碰”（`COLREG_AVOIDANCE`）** 状态时，会额外叠加展示核心避碰算法的决策图层：

8. **安全避障边界层（Safety Domain Layer）**
   - **展示内容**：围绕本船几何中心实时解算并渲染多边形“船舶安全领域（Safety Domain）”边界线，清晰直观地展示避碰边界围栏。

9. **规划路径轨迹簇（MpcTrajectoryLayer）**
   - **展示内容**：将 **M5 战术规划器（Tactical Planner）** 实时计算生成的未来多条候选路径曲线（Trajectory Candidates）渲染在海图上，方便工程师评估自主规划决策的收敛状态。

10. **避碰风险梯度场（IvpRiskGradientLayer）**
    - **展示内容**：由 **M4 行为仲裁器（Behavior Arbiter）** 生成的网格化风险梯度场（Ivp Contributions），在海图上以伪彩色热力图呈现当前海域中的避碰风险强度分布。

11. **COLREGs 避碰规则扇区（ColregsSectors）**
    - **展示内容**：依据《国际海上避碰规则》，在本船周围以扇形切片（红/黄/绿等规则扇区）形式划分出交叉、追越、对遇等规则判定区间。

---

### 总结建议

当前的界面设计已经非常完整，**普通监控视角**（如您截图所示）重点提供**本船基础航仪指标（HDG/COG/SOG/ROT）、航速矢量线、0.5/1.0 nm 避碰安全圈、雷达 PPI 圈以及海图底图（ENC/SAT/OSM）**，这充分保障了监控的清晰与安全专注。

若您需要在调试时查看更深度的自主决策链，可在右上角将视角切换至 **“工程师视角”** 或 **“全局视角”**，从而直观观察安全保护圈、候选轨迹簇和避碰风险场等算法图层的协同运行。





