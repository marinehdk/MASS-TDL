# D2.8 · 架构 v1.1.3 stub — 规格文档

| 属性 | 值 |
|---|---|
| D 编号 | D2.8 |
| 标题 | 架构 v1.1.3 stub（4 缺失模块 + §17–§22 章节迁入/新建） |
| 阶段 | Phase 2 |
| 时间窗口 | 2026-06-16 → 2026-07-31（3.0 人周） |
| Affects | 架构主文件 `MASS_ADAS_L3_TDL_架构设计报告.md` v1.1.3-pre-stub → v1.1.3-stub（全模块） |
| Depends | D1.3.2.3（Web HMI base）/ D2.4（scoring weights stub）/ D2.5（SIL integration）/ D2.6（HF 访谈输出）|
| Owner | 架构师 + M2/M3/M5/M7/M8 模块负责人（各支援） |
| 状态 | 🔴 未启动（2026-05-22，spec 完成，待 6/16 启动）|

> **权威架构文件**：`docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md`（v1.1.3-pre-stub 当前权威）
> **决策记录**：`docs/Design/SIL/v1.0-unified/archive/SIL-root/00-architecture-revision-decisions-2026-05-09.md`（附录 F 全部 33 个外部来源 + 置信度）
> **完整 DoD 细则**：`docs/Design/Architecture Design/gantt/archive/v3.2_2026-05-20_archived.md` §D2.8

---

## §1 背景与目标

架构报告 v1.1.3-pre-stub 在 2026-05-09 完成了一次重要 patch：将 SIL 框架架构（选项 D 混合 + DNV 工具链 3 MUST + RL 隔离三层 + maritime-schema + Imazu-22 覆盖立方体 + 6D 评分 + Web HMI）以**附录 F（9 节）**形式锁定。

D2.8 的目标：
1. **迁移**：将附录 F 9 节内容升格为主章节体系（§17–§21），附录 F 退役
2. **新建**：§16 Cybersecurity Spec + §22 延期支持模块 Stub（ENC Manager / Parameter Store / Environment Cross-Source Validator / BNWAS-equivalent）
3. **更新**：若干现有子章节（§3.4 / §10.1 / §10.5 / §11.x / §12.3 / §12.5 / §15.0 / §15 IDL 表）
4. **版本**：v1.1.3-pre-stub → v1.1.3-stub，发出 CCS / DNV 联审邀请

**非目标**（推 D3.8）：
- 完整算法实现细节（MPC 参数 / RL 超参 / HAZID 132 [TBD] 回填）
- §22 四个模块的完整设计和代码
- Cybersecurity §16 的完整 IACS UR E26/E27 分析

---

## §2 "完成"判据（CLAUDE.md §6 强制 5 项）

| # | 判据 | 验证方式 |
|---|---|---|
| 追溯 | 每条架构断言指向仓库内文档章节或 `[Rx]` 引用 | PR reviewer 按 §16 References 列表核验 |
| 接口契约 | §22.1–§22.4 每节含 input topic / output topic / 频率 / 责任声明 | 对照本规格 §8 检查清单 |
| 降级路径 | §22.1–§22.4 每节含 DEGRADED/CRITICAL/OUT-of-ODD 行为 | PR 评审时确认 |
| CCS 映射 | §22 四模块各映射到 DMV-CG-0264 9 个子功能之一 | 对照架构 §14 CCS 覆盖矩阵 |
| 置信度标注 | §16/§17/§22 中网络/推断结论用 🟢🟡🔴⚫ 标注 | Linting 工具或人工 |

---

## §3 架构报告章节号重排总览

D2.8 完成后，架构报告章节变为：

| 重排前 | 重排后 | 动作 |
|---|---|---|
| §1–§15（M1-M8 + 多船型 + CCS + 接口契约）| §1–§15（不变）| — |
| §16 参考文献 | §23 参考文献 | 顺延 |
| 附录 F（9 节，F.1–F.9）| 删除 | 内容迁入 §17–§21 |
| *(无)* | §16 Cybersecurity Spec（新）| D2.8 新建 |
| *(无)* | §17 SIL 框架架构（F.1-F.3 迁入）| D2.8 迁入 |
| *(无)* | §18 RL 隔离架构（F.4 迁入）| D2.8 迁入 |
| *(无)* | §19 场景库与覆盖立方体方法论（F.5-F.7 迁入）| D2.8 迁入 |
| *(无)* | §20 结构化 COLREGs 评分（F.8 迁入）| D2.8 迁入 |
| *(无)* | §21 Web HMI 与 ENC 集成（F.9 迁入）| D2.8 迁入 |
| *(无)* | §22 延期支持模块 Stub（§22.1–§22.4）| D2.8 新建 |

附录 A/B/C/D/E 保留，附录 D''''' 修订记录合并新 D2.8 条目。

---

## §4 输入依赖

| 输入 | 来源 | 需要时间 |
|---|---|---|
| 附录 F 全文（9 节，F.1–F.9）| 架构主文件 v1.1.3-pre-stub | 启动即可用 |
| 决策记录（33 个外部来源 + 置信度）| `SIL/v1.0-unified/archive/...00-architecture-revision-decisions-2026-05-09.md` | 启动即可用 |
| D2.6 船长访谈结果（心智模型 + 培训矩阵）| D2.6 交付，目标 7/13 | 7/13 前（§5.5/§5.6 占位先写）|
| D2.4 6D 评分权重 stub | D2.4 进行中 | §20 仅写框架，权重引用 D2.4 |
| D2.5 SIL 集成 latency 实测数据 | D2.5 进行中 | §17 dds-fmu budget 先用估算值，D2.5 完成后更新 |
| D1.3.2.3 Web HMI 实现状态 | D1.3.2.3（Phase 1 末）| §21 阶段路线图 Phase 1 行已有，写时确认 |
| RFC-007 Cybersecurity 接口草案 | D3.9 尚未启动 | §16 仅写 stub，D3.9 完整化 |
| HAZID RUN-001 参数（132 [TBD-HAZID]）| 8/19 完成 | §16/§22 中用 [TBD-HAZID] 占位 |

---

## §5 现有章节更新规格

每节格式：**目标章节位置** / **新增内容摘要** / **D-task 联动** / **TBD 标注**

### §5.1 §3.4 ToR 自适应矩阵

**目标位置**：§3.4 TDL 工作模式（ODD 子域部分）末尾，新增一表。

**新增内容**：4 操作员状态（ROC-在岗 / 桥楼备岗 / 餐厅 / 睡舱）× 4 ODD 子域（ODD-A/B/C/D）交叉的 ToR 时限矩阵。单元格格式 `T_ToR (s) | 触发条件`。初始值：ROC=60s / 桥楼=30s / 餐厅=90s / 睡舱=120s（v3.1 基线）。

**D-task 联动**：D2.1 M1 ODD 实装时用此矩阵作输入参数；D2.6 HF 访谈结果可能修正初始值（7/13 前可能产生 patch）。

**[TBD-HAZID]**：具体秒数值在 HAZID RUN-001（8/19）校准后更新到 D3.5。

### §5.2 §10.1 算法选型矩阵

**目标位置**：§10 M5 Tactical Planner 引言后，新增 §10.1 小节。

**新增内容**：4 候选算法（Mid-MPC / RRT* / VO / MPPI）× 4 评估维度（实时性 / CCS 白盒可审计性 / COLREG 约束表达 / SIL 验证成本）的 4×4 矩阵，每格评分 1-5 + 简短理由。结论行：**Mid-MPC N=18/90s** 为默认路径（已锁定），其余三个在特定降级场景下的激活条件。

**D-task 联动**：D3.2 M5 完整实现时对照此矩阵验证选型合理性。

**置信度**：🟢 High（MPC 选型有 Phase 1 SIL 实测 + DNV-CG-0264 §9 白盒要求两个独立来源支持）。

### §5.3 §10.5 4-DOF 适用性边界

**目标位置**：§10 M5 末尾，新增 §10.5 小节。

**新增内容**：
- FCBPlugin 4-DOF（Yasukawa 2015 标准参数）适用条件：`SOG ≤ 12 kn` AND `hull_class ≠ SEMI_PLANING`
- 超出边界触发路径：`hull_class = SEMI_PLANING` → 6-DOF PlaningHull6DOF（Phase 2 实装，D2.1 stub → D3.2 完整）
- 高速段（SOG > 12 kn）：6-DOF 预留，CasADi/IPOPT 路径，Phase 4 实装

**[TBD-HAZID]**：精确 SOG 转换阈值（12 kn）在 HAZID RUN-001 校准。

### §5.4 §11.x L3 仲裁优先级矩阵

**目标位置**：§11 M7 Safety Supervisor 末尾（§11.8 之后），新增 §11.9 小节。

**新增内容**：3 横向轴（Reflex Arc / Hardware Override / Deterministic Checker）× 3 触发状态（正常 / CRITICAL / OUT-of-ODD）的仲裁优先级表，加锁定顺序：
1. Hardware Override（最高，零软件硬连线）
2. Y-axis Reflex Arc（< 500 ms，直达 L5）
3. X-axis Deterministic Checker VETO
4. M7 Doer-Checker 内部否决
5. M1 ODD 状态切换

**Finding 关闭**：D P1-D-08（Y-axis 通告机制）。

### §5.5 §12.3 船长心智模型 ground truth

**目标位置**：§12 M8 HMI Bridge §12.3 子节（扩充已有内容或新增）。

**新增内容**：
- 标题：`§12.3.1 心智模型 Ground Truth（D2.6 访谈结果）`
- 占位结构（D2.6 完成前）：`[TBD-D2.6]` 表格框架 — 5 行（船长 ID #1-#5）× 6 列（关注点 / 干预阈值 / 信任触发 / 不信任触发 / 信息优先级 / 关键 UX 引言）
- D2.6（7/13 交付）后填充；本 stub 只写表头和说明

**D-task 联动**：D2.6 船长访谈结果 → 填充此表；D3.4 M8 完整实装时以此为输入设计 SAT-1/2/3 接口细节。

### §5.6 §12.5 人员资质 / 培训 / 演练

**目标位置**：§12 M8 末尾，新增 §12.5 小节。

**新增内容**：
- `§12.5.1 ROC 操作员资质要求`（知识维度 + 操作能力维度 + 最低持证要求 stub）
- `§12.5.2 培训矩阵 stub`：`[TBD-D2.6]` — D2.6 培训矩阵占位（4 列：角色 / 培训科目 / 时长 / 验收标准）
- `§12.5.3 定期演练制度 stub`：ToR 演练频率 + 场景类型（至少含 DEGRADED + CRITICAL）

**D-task 联动**：D2.6（7/27 可用性测试报告）→ 填充 §12.5.2；D3.5' 培训课程大纲 → 完整化 §12.5.1/§12.5.3。

### §5.7 §15.0 时基与同步

**目标位置**：§15 接口契约之前，新增 §15.0 小节。

**新增内容**：

| 项目 | 规格 |
|---|---|
| PTP Grandmaster | IEEE 1588v2（PTPv2）；OT 侧部署于 GNSS disciplined master clock |
| 同步误差预算 | L3 节点间时钟偏差 ≤ 1 ms（从 `/time/ptp_status` 监控）|
| `stamp` 字段强制 | 所有 L3 消息 IDL 必须携带 `header.stamp`（ROS2 builtin `Time`）|
| 退化路径 | PTP 失锁（≥ 3 s）→ M1 发布 `ODD_EDGE`，系统降级至 ASSISTED；M8 显示"时钟失步"告警 |
| ASDR 时基 | ASDR JSONL 每行 `timestamp` 使用同一 PTP 时钟，不用系统时钟 |

**置信度**：🟢 High（IEEE 1588v2 是 IACS UR E26 工控网络基准要求 + ROS2 Time 接口现有支持）。

### §5.8 §13.x 测试策略章节（V&V Plan 引入）

**目标位置**：§13 多船型 Capability Manifest 章节末尾，新增 §13.x 小节（约 1 页）。

**新增内容**：前向指针节，以表格形式索引 L3 TDL 各验证层的策略文件：

| 验证层 | 策略文档 | 关联 D-task |
|---|---|---|
| SIL（软件在环）| [D1.5 V&V Plan](../Phase%201/D1.5-vv-plan-scenario-qual/V%26V_Plan/00-vv-strategy-v0.1.md) | D1.3c / D2.5 / D3.6 |
| 场景覆盖率 | §19 场景库与覆盖立方体 + D1.6/D1.7 | D3.6 |
| 模块级单元测试 | `src/l3_tdl_kernel/M*/test/` | 每个 D{2.x} 模块 |
| 集成测试（M1-M6 联调）| D2.5 集成测试 spec | D2.5 |
| HIL（硬件在环）| D4.1/D4.2 展望 | Phase 4 |
| 认证级实船试航 | D5.x（2027 Q1/Q2）| Phase 5 |

加一段说明："本架构报告不包含完整 V&V 规划，详见上表各策略文档。架构决策与 V&V 策略的双向联动通过 D-task 联动表维护，不在本章重复。"

**D-task 联动**：D1.5 V&V Plan（输入来源）；D3.6 SIL 1000+ 完成后可更新覆盖率行。

---

### §5.9 §15 IDL 接口契约表新增 4 列

**目标位置**：§15 中的接口契约总表（每个消息一行），所有行新增 4 列：

| 新增列 | 含义 | 示例值 |
|---|---|---|
| `auth_required` | 消息是否需要身份验证 | `none` / `hmac-sha256` / `dds-auth` |
| `integrity_mechanism` | 完整性保护机制 | `none` / `crc32` / `hmac-sha256` |
| `replay_protection` | 重放攻击防护 | `none` / `seq-counter` / `ptp-window` |
| `dds_security_profile` | DDS Security profile ID | `open` / `L3-internal` / `ROC-link` |

初始值：L3 内部消息统一 `L3-internal` profile（DDS-Security 细节 D3.9 RFC-007 完整化）；ROC-Link 消息 `ROC-link` profile。`schema_version` 字段已在 v1.1.3-pre-stub 中规定，本次仅确认已有列完整性。

---

## §6 新建章节规格

每节格式：**章节号** / **来源** / **迁入/新建内容** / **章节引言模板** / **D-task 联动表** / **Finding 关闭** / **DNV/CCS 注** / **[TBD] 标注**

---

### §6.1 §16 Cybersecurity Spec（新章节）

**来源**：D2.8 v3.0 scope（RFC-007 D3.9 的 stub）。

**章节引言**：本章为 L3 TDL 网络安全框架的 stub 规格，定义 L3 DDS 信任域边界、接口认证机制占位、ASDR 签名升级路径，完整实现于 D3.9 RFC-007。

**新建内容**：

```
§16.1 信任域划分（Trust Zone）
  - Zone 1: L3 DDS 内部网络（M1-M8 节点，OT 隔离，DDS-Security profile L3-internal）
  - Zone 2: ROC Shore Link（DMZ，data diode + TLS 1.3，DDS-Security profile ROC-link）
  - Zone 3: ENC / Chart Server（只读 ingress，验证签名）
  - Zone 4: ASDR Evidence Store（append-only，HMAC-SHA256 签名链）
  架构图：4 zone 方框 + 箭头 + DDS domain 边界线（ASCII 或 Mermaid）

§16.2 L3 接口安全矩阵
  - 引用 §15 IDL 表新增的 4 列（auth_required / integrity_mechanism / replay_protection / dds_security_profile）
  - 高风险接口标注：M1→M8（ToR trigger）/ M7→M1（VETO）/ ROC-Link（遥控接管）

§16.3 ASDR HMAC 升级 stub
  - 当前：ASDR JSONL 无签名
  - D2.8 stub：每行追加 `"hmac_sha256": "<hex>"` 字段，key 从 Parameter Store 取（§22.2）
  - 完整实现：D3.9 RFC-007（密钥管理 + 轮换 + IACS UR E27 符合性）
  - 代码占位：`asdr_writer.cpp` 中 `TODO[D3.9]` 注释

§16.4 前向引用
  - D3.9 RFC-007：完整 IACS UR E26/E27 分析 + DDS-Security policy 文件
  - Zone 2 ROC-Link 细节：`docs/Design/Cross-Team Alignment/RFC-decisions.md`
```

**D-task 联动**：

| D-task | 联动内容 | 方向 |
|---|---|---|
| D3.9 RFC-007 Cybersecurity | §16 完整化，IACS UR E26/E27 填充 | 填充方 |
| D2.1 M1 ODD | 读取 §16.1 Zone 1 profile 实现 DDS-Security | 消费方 |
| D2.5 SIL Integration | §16.3 ASDR HMAC 字段 stub 实现 | 实现方 |

**Finding 关闭**：F P0-F-01（cyber 架构位置占位）stub 级别关闭。

**[TBD-D3.9]**：§16.1 Zone 边界防火墙规则细节 / §16.3 密钥管理 / IACS UR E26/E27 完整映射。

---

### §6.2 §17 SIL 框架架构（Appendix F.1-F.3 迁入）

**来源**：附录 F §F.1-F.3 全文（约 800 词，含架构图 + 三层模拟策略表 + 禁止反模式 + DNV 工具链表 + M7 边界规则 + FCBPlugin 代码片段 + F.3 ROS2/OS/RT 锁定）。

**章节引言**（新写，置于迁移内容之前）：本章定义 L3 TDL SIL（Software-in-the-Loop）测试框架的顶层架构，确保"测试目标即部署目标"（production C++/MISRA ROS2 节点直接运行于 SIL 内核）。本章由 v1.1.3-pre-stub 附录 F §F.1-F.3 迁入，为架构报告永久章节。内容填充路径：D1.3c（FMI 桥实装）→ D2.5（50 场景 SIL 集成）→ D3.6（1000 场景扩展）。

**迁入内容**：F.1 SIL harness 架构图 + 三层模拟策略表 + 反模式 + F.2 DNV 工具链 3 MUST + 2 NICE-deferred + dds-fmu latency budget + M7 边界规则 + FCBPlugin C++ 代码片段 + F.3 ROS2 Humble + Ubuntu 22.04 + PREEMPT_RT + Python 3.11 风险注。

**D-task 联动表**：

| D-task | 联动内容 | 方向 | 目标时间 |
|---|---|---|---|
| D1.3c FMI 桥 | dds-fmu latency 实测值更新 §17.2 预算表 | 填充 | Phase 1 末 |
| D2.5 SIL Integration | 50 场景验证 §17.1 架构图各连接真实通 | 验证 | 7/31 |
| D3.6 SIL 1000+ | 三层模拟策略 1000-cell 扩展，§17.1 路径验证 | 扩展 | Phase 3 |
| D4.6 RL 对抗 | F.2 中 NICE-deferred mlfmu 实装，§17.2 更新 | 激活 | Phase 4 |

**Finding 关闭**：SIL P0 SIL-1（SIL 框架架构决策锁定）/ SIL-2（DNV 工具链 MUST 列表）/ SIL-3（ROS2 Humble + RT 锁定）。

**[DNV-FEEDBACK-HOOK] §17.A**：以下内容依赖 DNV 验证官对 maritime-schema acceptance 的反馈（预计 7/31 后收到）：
- `maritime-schema TrafficSituation` 作为 evidence container 的 CCS 可接受性（§17.1 架构图中的 Scenario YAML 部分）
- 若 CCS 不接受：回退路径 = maritime-schema 作内部表示 + 导出器至 CCS 要求格式（需 D3.8 patch）
- 本 hook 标注在 D3.8 完整化时根据 feedback 决议删除或替换

**[TBD-D3.8]**：dds-fmu latency budget 最终实测值（D2.5 完成后更新）/ Phase 4 RL FMU 路径激活条件细化。

---

### §6.3 §18 RL 隔离架构（Appendix F.4 迁入）

**来源**：附录 F §F.4 全文（约 400 词，含三层边界表 + Phase 4 启动条件）。

**章节引言**（新写）：本章定义 RL（强化学习）对抗生成器与认证核心（M1-M8）的三层强制隔离边界。即使 B2 RL 推 Phase 4 启动，本章在 v1.1.3 stub 中锁定边界规则，确保 Phase 1-3 仓库结构和 CI lint 提前合规，避免 Phase 4 启动时回退污染已审认证内核。

**迁入内容**：F.4 三层边界表（L1 Repo / L2 Process / L3 Artefact）+ Phase 4 启动条件 + git history audit 要求 + docker-compose 命名空间隔离规则。

**补充内容**（D2.8 新写，F.4 中未涉及）：

```
§18.4 技术选型占位（[TBD-D4.6]）
  - L1 Repo 隔离：单体 monorepo + .gitignore 重规则 vs git submodule
    → 当前决策：单体 monorepo（`/src/rl_workbench/` 独立 colcon 包）
    → 具体 CI lint rule 在 D1.3a/D1.3b 仓库结构中已落地，本章引用
  - L2 Process 隔离：Docker compose 独立 namespace
    → K8s pod 隔离为 Phase 4 升级选项（D4.6 启动时评估）
  - L3 Artefact：mlfmu build → target_policy.fmu → evidence store
    → DNV-RP-0671 鉴定流程 [TBD-D4.6]（本章给出引用占位）
```

**D-task 联动表**：

| D-task | 联动内容 | 方向 |
|---|---|---|
| D1.3a / D1.3b | L1 Repo 边界 CI lint rule 已落地 → §18 引用 | 现状确认 |
| D1.5 V&V Plan | §18.3 RL artefact rebasing 进 V&V 门 | 输入 |
| D4.6 RL 对抗（Phase 4）| §18.4 全部 [TBD] 填充 + mlfmu 鉴定 | 激活 |

**Finding 关闭**：SIL P0 SIL-4（RL 隔离三层边界锁定）。

**DNV 引用**：DNV-RP-0671 [R30]（ML qualification）/ DNV-RP-0510 [R31]（autonomous systems）/ DNV-RP-0513 [R25]（assurance of ML）。

> **置信度注**：🟡 Medium — DNV-RP-0671/0510/0513 直接内容未在 safety_verification NLM 笔记本（NLM 查询 2026-05-22：未命中具体 RP 号；覆盖 DNV-CG-0264 + DNVGL-RP-A203 框架）；架构报告 §F.4 引用 [R25][R30][R31] 对应此三文件，应在 D3.8 完整化时验证具体条款引用准确性。

**[TBD-D4.6]**：L2/L3 隔离完整技术规格 / mlfmu 鉴定流程 / DNV-RP-0671 条款映射。

---

### §6.4 §19 场景库与覆盖立方体方法论（Appendix F.5-F.7 迁入）

**来源**：附录 F §F.5（maritime-schema 扩展）+ F.6（覆盖立方体）+ F.7（AIS-driven scenario 工具）全文（约 700 词）。

**章节引言**（新写）：本章定义 L3 TDL 的场景库工程体系：从 maritime-schema 场景语言到 Imazu-22 强制基线，再到 1100-cell 覆盖立方体和 AIS-driven authoring 工具链。本章为 V&V Plan（D1.5）的场景层补充，由 D1.3b.2（AIS tool）、D1.6（schema 规约）和 D3.6（1000 场景）持续填充。

**迁入内容**：F.5 TrafficSituation 扩展字段表 + 双语言验证 + CCS 接受度未解项 + F.6 Imazu-22 基线 + 1100-cell 立方体 + 60/25/15 比例 + Monte Carlo LHS/Sobol + F.7 AIS-driven 5 阶段管线 + 3 target 模式表 + 数据源决策。

**D-task 联动表**：

| D-task | 联动内容 | 方向 | 目标时间 |
|---|---|---|---|
| D1.6 场景 schema | maritime-schema 扩展字段 + 双语言验证实装 | 填充 §19.1 | Phase 1 |
| D1.3b.2 AIS tool | 5 阶段管线实装 + 数据源连通验证 | 填充 §19.3 | Phase 1/2 |
| D3.6 SIL 1000+ | 1100-cell 覆盖立方体 full run + LHS/Sobol 实测 | 填充 §19.2 | Phase 3 |
| D4.6 RL 对抗 | adversarial scenario 回路激活，§19.2 adversarial 30% | 激活 §19.2 | Phase 4 |

**Finding 关闭**：SIL P0 SIL-5（场景 schema 替代决策）/ SIL-6（Imazu-22 基线锁定）/ MV-7~11 stub（多船型场景覆盖）。

**[DNV-FEEDBACK-HOOK] §19.A**：maritime-schema `TrafficSituation` 作为 evidence container 的 CCS 接受度（F P0-F-04 stub），与 §17.A 同一 hook，7/31 后 DNV 反馈收到后 D3.8 patch。

**[TBD-D3.8]**：monte Carlo LHS/Sobol 实测分布 / adversarial 30% 实际生成路径 / 60/25/15 比例最终辩护文件（现为"内部启发式"）。

---

### §6.5 §20 结构化 COLREGs 评分（Appendix F.8 迁入）

**来源**：附录 F §F.8 全文（约 500 词，含 6 维度表 + 权重公式 + per-rule 准则说明）。

**章节引言**（新写）：本章定义 L3 TDL 的结构化 COLREGs 评分方法：PASS/FAIL 二元判定保留基础上，增加 6 维度连续评分作为 CCS surveyor 论据。评分框架由 D1.7 和 D2.4 共同填充，本章为框架宿主。

**迁入内容**：F.8 6D 维度表（Safety / Rule compliance / Delay penalty / Action magnitude / Phase score / Trajectory implausibility）+ 权重公式 `total_score = w_s·safety + w_r·rule − p_delay − p_mag + w_p·phase` + 置信度注。

**补充内容（D2.8 新写）**：

```
§20.3 权重参数版本控制
  当前状态（D2.8 stub）：w_s / w_r / w_p / λ_1 = [TBD-D1.7]（待 D1.7 Hagen 2022 原文细节）
  D2.4 实装后：weights 由 D2.4 report 提交，§20 只引用，不 override
  D3.6 1000 场景后：用实测 score distribution 回检权重合理性（推翻信号）

§20.4 与 D2.4 联动协议
  §20 定义框架（维度 + 公式结构），D2.4 填充参数（权重 + per-rule threshold）
  D2.4 完成后：发 PR 仅更新 §20.3 权重参数表，不修改维度或公式
  避免循环返工：§20 框架在 D2.4 期间不得修改；若 D2.4 发现维度结构需调整 → D2.4 architect review 再改 §20
```

**D-task 联动表**：

| D-task | 联动内容 | 方向 |
|---|---|---|
| D1.7 覆盖率方法论 | Hagen 2022 [R33] / Woerner 2019 [R34] 权重原文细节 → §20.3 填充 | 填充 §20.3 |
| D2.4 M6 COLREGs 实装 | 权重 stub + per-rule threshold → §20.3 patch | 填充 §20.3 |
| D3.6 SIL 1000+ | 实测 score distribution → 权重合理性验证 | 验证 §20.3 |

**Finding 关闭**：SIL P0 SIL-7（6D 评分框架锁定）/ F P1-F-03（COLREGs scoring 方法明确）。

**置信度**：🟡 Medium（6D 维度结构学术圈公认；具体权重 w_s / w_r / w_p 需 D1.7 引 Hagen 2022 / Woerner 2019 原文确认，推翻信号：若维度与 CCS《智能船舶规范 2024》§9.1 不可对齐则重构）。

**[TBD-D1.7+D2.4]**：权重数值 / per-rule threshold / λ_1 delay penalty 系数。

---

### §6.6 §21 Web HMI 与 ENC 集成（Appendix F.9 迁入）

**来源**：附录 F §F.9 全文（约 600 词，含技术选型表 + XAI overlay 模式 + 3 阶段路线）。

**章节引言**（新写）：本章定义 L3 TDL 操作员 HMI 的参考架构，替代原型 `L3_TDL_SIL_Interactive.html`，以 MapLibre GL JS + S-57 MVT + foxglove_bridge 构建 production-grade web HMI。本章为 D1.3.2.3（Phase 1 基础）、D2.5/D2.6（Phase 2 增项）、D3.4（Phase 3 完整化）的宿主设计文档。

**迁入内容**：F.9 技术选型表（MapLibre / GDAL-Tippecanoe-MVT / foxglove_bridge / IEC 62288 SA subset / Apache Arrow / GSAP / Puppeteer / React 选型及理由）+ XAI overlay 模式（现有 5 项 + 新增 7 项）+ 3 阶段路线表。

**补充内容（D2.8 新写）**：

```
§21.4 D-task 产出物映射（三任务统一引用）

| 产出物 | 来源 D-task | 进入 §21 时间 |
|---|---|---|
| MapLibre 骨架 + S-57 MVT + foxglove_bridge | D1.3.2.3 | Phase 1 末 |
| IEC 62288 SA subset 显示标准落地 | D1.3.2.3 | Phase 1 末 |
| Apache Arrow replay + scrubber + GIF evidence | D2.5 | 7/31 |
| 多 target 同屏 + grounding 高亮 + TLS/WSS | D2.5 | 7/31 |
| Figma 船长可用性 + ToR countdown panel | D2.6 | 7/13 |
| Trajectory ghosting + M7 verdict badge | D3.4 M8 完整 | Phase 3 |
| 1000 场景 evidence pack 一键导出 | D3.6 SIL 1000+ | Phase 3 |

§21.5 IEC 62288 SA subset 范围界定
  采用：SA Presentation Level 2（关键导航数据 + 告警 + 预测轨迹）
  不采用：完整 ECDIS（S-52 complete grammar + TYPE_APPROVED 认证）
  理由：CCS credibility 需求通过 SA subset 满足；完整 ECDIS 认证成本 > 收益
  NLM 证据：maritime_human_factors notebook（BAM compliance + MSC.302(87) + 等效感知）🟢 High

§21.6 IMO S-Mode 占位
  S-Mode 标准化状态（[TBD-DNV]）：
  目前 IMO S-Mode 仅有非强制指南状态；MASS Code Tier IV 强制时间未定
  D2.8 stub：§21 引用 S-Mode 概念 + "本 HMI 设计为 S-Mode 就绪（S-Mode ready）"
  D3.4 + D3.8：根据 IMO S-Mode 最终版本完善
```

**D-task 联动表**：

| D-task | 联动内容 | 方向 |
|---|---|---|
| D1.3.2.3 Web HMI + ENC | Phase 1 基础 3 项产出 → §21.4 Phase 1 行确认 | 填充 §21.4 |
| D2.5 SIL M1-M6 集成 | Apache Arrow + scrubber + GIF → §21.4 Phase 2 行 | 填充 §21.4 |
| D2.6 船长 HF | Figma 原型 + ToR countdown → §21.4 Phase 2 行 + §21.5 输入 | 填充 §21.4 |
| D3.4 M8 完整 | Trajectory ghosting + M7 verdict badge → §21.4 Phase 3 行 | 填充 §21.4 |

**Finding 关闭**：SIL P0 SIL-7（Web HMI 架构锁定，与 §20 同一 finding）/ B P1-B-06（foxglove_bridge 选型理由）。

---

### §6.7 §22 延期支持模块 Stub（§22.1–§22.4）

**章节引言**：以下 4 个模块为 L3 TDL 架构中识别的必要支持模块，因 Phase 1-3 资源约束（B4 standby）延至 Phase 4 D4.7 完整实现。本章以"接口契约 stub"形式记录，使 M1-M8 模块设计可引用其接口位，CCS 审计员可评估接口完整性，而不需要实现细节。

**格式**：每个子节遵循 M1-M8 接口契约最小模板（不写算法实现）。

---

#### §22.1 ENC Manager

**职责**：将电子海图（S-57/S-101）数据转化为 L3 可消费的障碍物多边形、水深轮廓线和交通规则区域，作为 M2 World Model 和 M5 Tactical Planner 的地理约束输入源。

| 属性 | 值 |
|---|---|
| 订阅模块 | M2 World Model / M5 Tactical Planner |
| 触发方 | 自船位置更新（M2 发布）|
| 实现推迟至 | Phase 4 D4.7（B4 条件触发）|

**接口契约（stub）**：

| 消息 | 话题 | 方向 | 频率 | 内容摘要 |
|---|---|---|---|---|
| `ENCHazardMap` | `/enc/hazard_polygons` | → M2/M5 | 0.1 Hz + 位置事件 | 当前位置 ±5 nm 内障碍物 + 浅水多边形列表 |
| `ENCDepthContours` | `/enc/depth_contours` | → M5 | 0.1 Hz | 规划路径相关水深轮廓线 |
| `ENCTrafficZones` | `/enc/traffic_zones` | → M6 | 0.1 Hz | TSS / VTS / Port 通航规则区域（M6 Rule 10 输入）|

**降级行为**：
- DEGRADED（ENC 数据 > 24h 未更新）：M1 发布 `ODD_EDGE`；M5 使用保守静水障碍物假设
- CRITICAL（ENC 完全不可用）：M1 切换 ODD_OUT；系统发出 MRC，禁止自主决策
- Phase 4 前（stub 期）：M5 使用硬编码 no-go zone 占位（测试场景 Trondheim Fjord 基线）

**CCS 功能映射**：DMV-CG-0264 Sub-function 4（Route Planning）+ Sub-function 5（Collision Avoidance）。

**[TBD-D4.7]**：S-57 vs S-101 格式支持策略 / ENC 来源认证 / 动态障碍物更新机制 / GDAL pipeline 集成细节。

---

#### §22.2 Parameter Store

**职责**：L3 TDL 唯一权威参数源，存储船舶能力清单（Capability Manifest）、水动力系数（MMG 4-DOF/6-DOF）、停船距离、风流补偿表、推进配置和降级回退值。确保"零船型常量"原则（CLAUDE.md §4 顶层决策 4）。

| 属性 | 值 |
|---|---|
| 订阅模块 | M1 / M4 / M5 / M7 |
| 触发方 | 系统启动 + 船型切换事件 |
| 实现推迟至 | Phase 4 D4.7 |

**接口契约（stub）**：

| 消息 | 话题 | 方向 | 频率 | 内容摘要 |
|---|---|---|---|---|
| `CapabilityManifest` | `/params/capability_manifest` | → M1/M4/M5 | 0.1 Hz 心跳 + 启动 | 船型 + 推进配置 + ROT_max 曲线 + 停船距离 |
| `VesselDynamics` | `/params/vessel_dynamics` | → M5 | 0.1 Hz 心跳 + 启动 | MMG 系数集（Yasukawa 2015 或外部提供）|
| `SafetyMargins` | `/params/safety_margins` | → M7 | 0.1 Hz 心跳 + 启动 | CPA_min / TCPA_min / ODD 边界阈值（含 HAZID 初值）|
| `ParamUpdateEvent` | `/params/update_event` | → M1 | 事件 | 参数热更新通知（需 M7 VETO 验证）|

**降级行为**：
- DEGRADED（心跳 > 5 s 中断）：M1 发布 `ODD_EDGE`；使用上次缓存值运行 ≤ 60 s
- CRITICAL（参数完整性校验失败）：M1 切换 ODD_OUT；禁止自主决策
- Phase 4 前（stub 期）：YAML 配置文件 `/config/fcb_params.yaml` 直接由 M5/M7 读取（无 ROS2 topic 封装）

**CCS 功能映射**：跨切所有 9 个子功能（参数是全局约束输入）+ DMV-CG-0264 §4.2 Configuration Management。

**[TBD-D4.7]**：参数热更新 M7 VETO 验证协议 / HAZID 132 [TBD-HAZID] 参数回填（D3.5 8/19）/ 多船型 PVA 适配路径。

---

#### §22.3 Environment Cross-Source Validator

**职责**：验证来自多传感器融合路径（AIS / Radar ARPA / AIS-R / Fusion Pipeline）的环境数据一致性，检测欺骗、陈旧数据、跨源 MMSI 冲突和位置不一致，为 M2 World Model 提供带验证标志的目标状态。

| 属性 | 值 |
|---|---|
| 订阅模块 | M2 World Model |
| 上游来源 | AIS / Radar ARPA / Multimodal Fusion（L3 边界外）|
| 实现推迟至 | Phase 4 D4.7 |

**接口契约（stub）**：

| 消息 | 话题 | 方向 | 频率 | 内容摘要 |
|---|---|---|---|---|
| `ValidatedTrackSet` | `/env/validated_tracks` | → M2 | 2-5 Hz | 目标列表 + per-target `cross_source_agreement ∈ [0,1]` + `staleness_flag` |
| `ValidationAlert` | `/env/validation_alerts` | → M1 | 事件 | 欺骗疑似 / MMSI 冲突 / 传感器降质告警 |

**降级行为**：
- DEGRADED（单一传感器源不可用）：降低 `cross_source_agreement`；M2 提高 TrackedTarget 不确定度
- CRITICAL（所有融合路径中断 > 10 s）：M1 发布 `ODD_CRITICAL`；系统降级至最低 MRC
- Phase 4 前（stub 期）：M2 直接消费 `/ais/contacts` + `/fusion/tracks`（无跨源验证）

**CCS 功能映射**：DMV-CG-0264 Sub-function 2（Situational Awareness）+ 网络安全 §16 Zone 1 完整性保护。

**[TBD-D4.7]**：具体跨源不一致检测算法 / MMSI 冲突解决策略 / 欺骗检测阈值（HAZID 校准）。

---

#### §22.4 BNWAS-equivalent

**职责**：L3 TDL 软件级桥梁航行值班报警系统等价物（类 SOLAS CH V Reg 19-1）。在自主运行期间监测 ROC 操作员活跃度，对非响应超时按三级阶梯触发接管请求。

| 属性 | 值 |
|---|---|
| 监测对象 | ROC 操作员（主 + 备）|
| 上游 | `/operator/heartbeat`（ROC 交互事件）+ `/m1/odd_state`（自主等级）|
| 下游触发 | M1 ODD 状态切换 + M8 告警显示 |
| 实现推迟至 | Phase 4 D4.7（B4 条件触发）+ 部分功能 D3.4 M8 实装时预留 |

**接口契约（stub）**：

| 消息 | 话题 | 方向 | 频率 | 内容摘要 |
|---|---|---|---|---|
| `OperatorHeartbeat` | `/operator/heartbeat` | ← ROC 前端 | 事件（操作员交互）| 操作员 ID + 时间戳 + 交互类型 |
| `BNWASAlertLevel` | `/bnwas/alert_level` | → M8 | 1 Hz 心跳 + 事件 | enum: INACTIVE / WARNING_1(30s) / WARNING_2(60s) / ESCALATED(90s) |
| `BNWASTakeoverRequest` | `/bnwas/takeover_request` | → M1 | 事件 | 触发 ToR，含当前操作员状态和 elapsed_time |

**三级阶梯**（基线初值，[TBD-HAZID] HAZID RUN-001 校准）：
- **T1 = 30 s**：ROC 操作员无交互 → 闪灯 WARNING_1（M8 显示）
- **T2 = 60 s**：持续无响应 → WARNING_2，通知备用操作员（二副相当）
- **T3 = 90 s（ROC 场景）/ 120 s（睡舱场景）**：ESCALATED，发出 ToR 自适应矩阵（§3.4）驱动的接管请求

**降级行为**：
- DEGRADED（`/operator/heartbeat` 话题不可用）：立即触发 WARNING_1 并通知 M1
- CRITICAL（ESCALATED 后无响应 > T3 + 30 s）：M1 发布 `ODD_OUT`；系统执行自主 MRC
- Phase 4 前（stub 期）：ToR 协议由 M1 定时器直接实现（无独立 BNWAS 节点）

**CCS 功能映射**：DMV-CG-0264 Sub-function 9（Communication and Control）+ SOLAS CH V Reg 19-1 类比（非 Type-Approved，stub 目的为 CCS 接口评审）。

**Finding 关闭（D2.8 stub 级别）**：D P1-D-04（BNWAS stub 设计位置确认）。

**[TBD-D4.7]**：SOLAS Reg 19-1 合规性完整分析 / 多 ROC 操作员同时在线时的主备切换协议 / T1/T2/T3 HAZID 最终值（D3.5）。

---

## §7 Appendix F 退役规格

D2.8 完成后：
1. 从附录列表中删除 `附录 F SIL 框架架构 [v1.1.3-pre-stub 新增 — 2026-05-09]`
2. 在附录 D''''' 修订记录末尾新增 D2.8 条目：
   > `附录 F（§F.1–F.9）迁入正章 §17–§21；附录 F 退役（2026-07-31，D2.8）`
3. 附录 A/B/C/D/E 保留不变

---

## §8 版本提升规格

| 属性 | 旧值 | 新值 |
|---|---|---|
| 文件头版本号 | `v1.1.3-pre-stub` | `v1.1.3-stub` |
| 文件头日期 | 2026-05-09 | 2026-07-31 |
| 文件头摘要新增条目 | — | D2.8 完成：§16-§22 新建/迁入，附录 F 退役，4 缺失模块 stub，CCS/DNV 邀请发出 |

版本提升 commit 格式：
```
arch(D2.8): v1.1.3-pre-stub → v1.1.3-stub

- Migrate Appendix F §F.1-F.9 to main chapters §17-§21
- Add §16 Cybersecurity Spec (stub, D3.9 pending)
- Add §22 Deferred Support Modules (§22.1-§22.4 interface contracts)
- Update §3.4/§10.1/§10.5/§11.x/§12.3/§12.5/§15.0/§15-IDL
- Retire Appendix F
- Issue CCS/DNV review invitations (D1.8 follow-up)

Closes: SIL P0 SIL-1~7, F P0-F-01/F-02/F-04 stub, F P1-F-03,
        D P1-D-04/D-08, B P1-B-06, MV-7~11
Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

---

## §9 Finding 闭环映射

| Finding ID | 角度 | D2.8 关闭程度 | 备注 |
|---|---|---|---|
| SIL P0 SIL-1 | SIL | ✅ stub 关闭 | §17 SIL 框架决策锁定迁入正章 |
| SIL P0 SIL-2 | SIL | ✅ stub 关闭 | §17 DNV 工具链 3 MUST 锁定 |
| SIL P0 SIL-3 | SIL | ✅ stub 关闭 | §17 ROS2 Humble + RT 锁定 |
| SIL P0 SIL-4 | SIL | ✅ stub 关闭 | §18 RL 隔离三层边界锁定 |
| SIL P0 SIL-5 | SIL | ✅ stub 关闭 | §19 maritime-schema 替代决策 |
| SIL P0 SIL-6 | SIL | ✅ stub 关闭 | §19 Imazu-22 + 1100-cell 方法论 |
| SIL P0 SIL-7 | SIL | ✅ stub 关闭 | §20 6D 评分框架 + §21 Web HMI 架构 |
| F P0-F-01 | F(M3/Field) | 🟡 partial stub | §16 cyber 架构位置占位；D3.9 完整 |
| F P0-F-02 | F | 🟡 partial stub | §19 场景库位置确认；D3.6 完整 |
| F P0-F-04 | F | 🟡 partial stub + [DNV-hook] | §19 maritime-schema CCS 接受度 hook 置入 |
| F P1-F-03 | F | ✅ stub 关闭 | §20 COLREGs scoring 方法明确 |
| C P1-C-3 | C(Cert) | 🟡 partial | §16 + §22 接口位确认；D2.7 HARA 完整关闭 |
| C P1-C-5 | C | 🟡 partial | §22 CCS 映射表；D3.3a 完整关闭 |
| B P1-B-06 | B(M2) | ✅ stub 关闭 | §21 foxglove_bridge 选型理由落入正章 |
| D P1-D-08 | D(Decision) | ✅ 关闭 | §11.x Y-axis 仲裁优先级矩阵 |
| D P1-D-04 | D | ✅ stub 关闭 | §22.4 BNWAS 接口位确认 |
| MV-7 ~ MV-11 | MV(Multi-vessel) | 🟡 stub scope | §19 多船型场景 3 target 模式框架；D3.6 完整 |

---

## §10 DEMO-2 Charter（D2.8 视角）

**DEMO-2 日期**：2026-07-31

**D2.8 在 DEMO-2 中的展示内容**：

1. **架构 v1.1.3-stub 全文 walkthrough**（≤ 15 min）
   - §17 SIL 框架架构图（选项 D + 3 MUST 工具链确认）
   - §18 RL 隔离三层（CI lint 验证 Phase 1 已合规）
   - §19 场景库（Imazu-22 + 1100-cell + AIS-driven 工具）
   - §20 6D 评分框架（维度 + 公式，权重由 D2.4 填充）
   - §21 Web HMI 阶段路线（D1.3.2.3 Phase 1 行已完成）
   - §22 延期支持模块接口图（4 模块话题名 + 频率可视化）

2. **CCS / DNV 邀请确认**（纸面证据）
   - CCS 中期意见会议邀请函（含 v1.1.3-stub PDF 附件）
   - DNV 验证官 maritime-schema acceptance 联审邀请发出确认

**Visible Success for D2.8**：
- [ ] v1.1.3-stub 架构主文件 commit 已入 `main`
- [ ] §17–§22 全文现场可阅读（无空白章节，stub 标注清晰）
- [ ] §22.1–§22.4 接口契约表完整（话题名 + 频率 + 降级行为）
- [ ] CCS 中期意见邀请函已发出（邮件截图证据）
- [ ] DNV 验证官联审邀请已发出

---

## §11 外部依赖 + 时间线

| 依赖 | 负责方 | 预计时间 | D2.8 影响 |
|---|---|---|---|
| D2.6 船长访谈结果 | HF 咨询 | 7/13 | §5.5/§5.6 从 [TBD-D2.6] 填充 |
| D2.4 6D 权重 stub | M6 负责人 | 7/15（阶段性）| §20.3 权重表 patch |
| D2.5 dds-fmu latency 实测 | V&V 工程师 | 7/20（估）| §17 latency budget 更新 |
| DNV 验证官联审反馈 | PM + Architect | 7/31 后 | [DNV-FEEDBACK-HOOK] §17.A / §19.A 留待 D3.8 |
| CCS 中期意见邮件回执 | PM | 8月中旬（估）| DEMO-2 可见成功项 |
| HAZID RUN-001 完成 | 安全工程师 | 8/19 | §3.4/§22.x [TBD-HAZID] → D3.5 回填 |

---

## §12 DoD 检查清单

```
基础（v3.2 verified — 28 章已在）
[x] 架构 v1.1.3-pre-stub commit（已完成，此为前置条件）

D2.8 新增（本任务目标）
[ ] §3.4 ToR 自适应矩阵新增（4×4 表格已入正文）
[ ] §10.1 算法选型矩阵新增（4×4 评分表 + 结论行）
[ ] §10.5 4-DOF 边界声明新增（hull_class + SOG 阈值）
[ ] §11.x L3 仲裁优先级矩阵新增（3 横向轴 × 3 触发状态）
[ ] §12.3 心智模型 stub 占位（[TBD-D2.6] 表框架）
[ ] §12.5 培训矩阵 stub 占位（[TBD-D2.6] 三子节框架）
[ ] §13.x 测试策略章节新增（V&V Plan 前向索引表）
[ ] §15.0 时基与同步新增（PTPv2 + budget + 退化路径）
[ ] §15 IDL 表 4 新列（auth / integrity / replay / dds-security）
[ ] §16 Cybersecurity Spec 新建（4 子节，stub 级）
[ ] §17 SIL 框架架构新建（F.1-F.3 迁入 + 引言 + D-task 联动 + [DNV-hook]）
[ ] §18 RL 隔离架构新建（F.4 迁入 + 引言 + §18.4 技术选型占位）
[ ] §19 场景库方法论新建（F.5-F.7 迁入 + 引言 + D-task 联动 + [DNV-hook]）
[ ] §20 6D 评分新建（F.8 迁入 + 引言 + §20.3 权重版本控制 + §20.4 联动协议）
[ ] §21 Web HMI 新建（F.9 迁入 + 引言 + §21.4 三任务映射 + §21.5/§21.6）
[ ] §22 延期支持模块 stub 新建（§22.1–§22.4 全部接口契约表 + 降级行为 + CCS 映射）
[ ] §23 参考文献（原 §16 顺延，确认 [R25]-[R38] 14 条已入）
[ ] 附录 F 退役（从附录列表删除，修订记录新增条目）
[ ] 版本号 v1.1.3-pre-stub → v1.1.3-stub，修订记录补全
[ ] CCS / DNV review 邀请发出（邮件证据）
[ ] CCS / DNV review 加入 maritime-schema acceptance 反馈请求

Finding 关闭（见 §9）
[ ] SIL P0 SIL-1 ~ SIL-7（7 项）
[ ] F P0-F-01/F-02/F-04 stub + F P1-F-03
[ ] B P1-B-06 + D P1-D-04/D-08
[ ] C P1-C-3/C-5（partial）
[ ] MV-7 ~ MV-11（stub scope）
```

---

## 修订

| 日期 | 变更 |
|---|---|
| 2026-05-22 | 初版（brainstorming 产出；spec 设计决策：方案 B 迁移+连接层 + §22 单章四节 + §17 全内容+DNV hook + 按目标章节组织）|
