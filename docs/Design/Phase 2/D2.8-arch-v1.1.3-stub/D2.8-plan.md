# D2.8 架构 v1.1.3-stub Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Upgrade the architecture report from v1.1.3-pre-stub to v1.1.3-stub: migrate Appendix F §F.1-F.9 into main chapters §17-§21, add §16 Cybersecurity Spec and §22 Deferred Support Modules stub, update 9 existing sections (§3.4 / §10.1 / §10.5 / §11 / §12.3 / §12.5 / §13 / §15.0 / §15.2 IDL), renumber §16 References → §23, retire Appendix F, and bump version to v1.1.3-stub.

**Architecture:** Single Markdown file edit throughout: `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` (2299 lines at plan-writing time, grows during execution). New chapters §16-§22 all insert before the current `## 第十六章 参考文献` heading (which becomes §23 in Task B8). Group A edits existing sections in-place; Group B inserts new chapters sequentially; Group C finalizes structure.

**Tech Stack:** Markdown editing only. Spec: `docs/Design/Phase 2/D2.8-arch-v1.1.3-stub/D2.8-spec.md`. Time window: 2026-06-16 → 2026-07-31 (3.0 人周).

**Timing constraints:**
- §12.3.1 / §12.6 stubs can be written immediately; fill content only after D2.6 completes (7/13)
- §20.3 weights: write `[TBD-D1.7+D2.4]` placeholder now; D2.4 patches after 7/15
- §17 dds-fmu latency: use estimated values now; D2.5 patches after 7/20

---

## File Map

**Single target file:** `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md`

Key anchors (verified at plan-writing time):

| Anchor text | Line |
|---|---|
| `### 3.4 TMR / TDL 量化模型` | ~260 |
| `### 3.5 ODD 状态机設計` | ~283 |
| `## 第十章 M5 — Tactical Planner` | ~806 |
| `### 10.1 決策原因` | ~808 → becomes §10.2 after Task A2 |
| `### 10.5 FCB 高速船型修正` | ~884 → becomes §10.6 after Task A2 |
| `### 11.9 人工接管時 L3 内部行為` | ~1063 |
| `#### 11.9.2 接管解除（回切）順序化` | ~1088 |
| `## 第十二章 M8` | ~1114 |
| `### 12.3 差異化視圖設計` | ~1156 |
| `### 12.4 責任移交協議` | ~1177 |
| `### 12.5 決策依據` | ~1242 (already exists — training matrix goes to §12.6) |
| `## 第十三章 多船型参数化設計` | ~1252 |
| `### 13.5 典型船型扩展路径` | ~1323 (last §13 subsection) |
| `## 第十四章 CCS 入级路径映射` | ~1334 |
| `## 第十五章 接口契约总表` | ~1391 |
| `### 15.1` | ~1392 (IDL definitions) |
| `### 15.2 接口矩阵总覧` | ~1611 |
| `## 第十六章 参考文献` | 1651 ← ALL new §16-§22 insert BEFORE this |
| `## 附录 F SIL 框架架構` | 2038 ← delete in Task C1 |
| `## 附录 D'''''` | 2261 (Appendix F ends here) |

**Correction vs. spec** (discovered during plan-writing):
- Spec §5.4 says "新增 §11.9" but §11.9–§11.9.2 already exist (v1.1.1 patch) → new section is **§11.10**
- Spec §5.6 says "新增 §12.5" but §12.5 "決策依據" already exists → training matrix goes to **§12.6**
- Spec §5.8 says "§13.x" → last §13 subsection is §13.5, so test strategy pointer goes to **§13.6**

---

## Task 1: Branch Setup

**Files:** None (git only)

- [ ] **Step 1: Create feature branch**

```bash
git checkout -b feat/d2.8-arch-v1.1.3-stub
```
Expected: `Switched to a new branch 'feat/d2.8-arch-v1.1.3-stub'`

- [ ] **Step 2: Verify anchor lines**

```bash
grep -n "## 第十六章 参考文献\|## 附录 F SIL\|### 3\.4\|### 10\.1\|### 10\.5\|### 11\.9 人\|### 12\.5 決\|### 13\.5\|## 第十五章" \
  "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Record actual line numbers. Confirm §11.9 exists, §12.5 "決策依據" exists, §13.5 is the last §13 section, §16 参考文献 is at line 1651.

---

## Task A1: §3.4 ToR 自适应矩阵

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` at §3.4

- [ ] **Step 1: Locate §3.4 / §3.5 boundary**

```bash
grep -n "### 3\.4\|### 3\.5" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
The table inserts at the end of §3.4 content, immediately before `### 3.5`.

- [ ] **Step 2: Insert ToR 自适应矩阵 before `### 3.5`**

Using the Edit tool, find the blank line immediately before `### 3.5 ODD 状态机设计` and insert:

```markdown

**ToR 自适应矩阵 [D2.8 新增 — D2.1 实装参数输入]**

操作员状态 × ODD 子域的 ToR 时限矩阵。格式：`T_ToR (s) | 触发条件`。所有初始值为 v3.1 基线；**[TBD-HAZID]** 在 HAZID RUN-001（8/19）校准后由 D3.5 更新。

| 操作员状态 | ODD-A（正常运营）| ODD-B（受限运营）| ODD-C（边缘）| ODD-D（降级）|
|---|---|---|---|---|
| **ROC 在岗** | 60 s \| TDL ≤ TMR 触发 | 60 s \| TDL ≤ TMR 触发 | 45 s \| TDL ≤ 80 s | 30 s \| 立即 |
| **桥楼备岗** | 90 s \| ODD EDGE + 通知 | 75 s \| TDL ≤ 1.5×TMR | 60 s \| TDL ≤ 100 s | 45 s \| 立即 |
| **餐厅** | 120 s \| 计划切换点 | 90 s \| EDGE + 广播 | 75 s \| OUT 预警 | 60 s \| 立即 |
| **睡舱** | 150 s \| D4→D3 预通知 | 120 s \| EDGE 强通知 | 90 s \| 叫醒 + ToR | 75 s \| MRC 准备 |

> **[TBD-HAZID]**：秒数在 HAZID RUN-001（8/19）以 FCB 实际接管数据校准，D3.5 回填。D2.1 M1 ODD 实装时以此矩阵作参数配置输入；D2.6 HF 访谈（7/13）可能产生 patch。

```

- [ ] **Step 3: Verify**

```bash
grep -A 8 "ToR 自适应矩阵" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md" | head -10
```
Expected: table header row visible.

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A1): add §3.4 ToR adaptive matrix (4 operator × 4 ODD)"
```

---

## Task A2: §10.1 算法选型矩阵（新增 + §10.1-§10.8 重排 → §10.2-§10.9）

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` at §10

- [ ] **Step 1: Confirm current §10 subsection list**

```bash
grep -n "^### 10\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: §10.1 決策原因 through §10.8 決策依据（8 sections).

- [ ] **Step 2: Renumber §10.8 → §10.9（reverse order to avoid conflicts）**

```bash
ARCH="docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
sed -i '' 's/^### 10\.8 /### 10.9 /' "$ARCH"
sed -i '' 's/^### 10\.7 /### 10.8 /' "$ARCH"
sed -i '' 's/^### 10\.6 /### 10.7 /' "$ARCH"
sed -i '' 's/^### 10\.5 /### 10.6 /' "$ARCH"
sed -i '' 's/^### 10\.4 /### 10.5 /' "$ARCH"
sed -i '' 's/^### 10\.3 /### 10.4 /' "$ARCH"
sed -i '' 's/^### 10\.2 /### 10.3 /' "$ARCH"
sed -i '' 's/^### 10\.1 /### 10.2 /' "$ARCH"
```

- [ ] **Step 3: Verify renumbering**

```bash
grep -n "^### 10\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: §10.2 決策原因 through §10.9（no §10.1）.

- [ ] **Step 4: Insert new §10.1 before `### 10.2 決策原因`**

Using the Edit tool, find `### 10.2 決策原因` and insert the following block immediately before it:

```markdown
### 10.1 算法选型矩阵 [D2.8 新增]

M5 Tactical Planner 候选算法评估。评分 1-5（5 最优）。当前默认路径已锁定（Mid-MPC），其余算法仅在特定降级场景激活。

**置信度**：🟢 High（MPC 选型有 Phase 1 SIL 实测 + DNV-CG-0264 §9 白盒要求两个独立来源支持）

| 候选算法 | 实时性（≤1 Hz 帧内求解）| CCS 白盒可审计性 | COLREG 约束表达 | SIL 验证成本 | 总评 |
|---|---|---|---|---|---|
| **Mid-MPC N=18/90s** ✅ | 5（CasADi 25ms 实测）| 5（状态轨迹全可视）| 5（约束直接入 QP）| 5（D2.5 pipeline 已建）| **25 — 默认路径** |
| RRT* | 3（树扩展不确定）| 3（路径采样难审计）| 2（COLREGs 需后处理）| 2（场景 coverage 难量化）| 10 — 障碍物密集 + MPC 超时时备用 |
| VO（Velocity Obstacle）| 4（反应快）| 4（几何直观）| 3（Rule 8 大动作不自然）| 3（VO + COLREGs patch 验证量大）| 14 — BC-MPC 降级时辅助 |
| MPPI | 2（GPU 依赖，OT 兼容差）| 1（采样轨迹难审计）| 3（代价函数可编码）| 1（OT 实机部署风险高）| 7 — Phase 4 研究选项，不入认证路径 |

> **降级激活**：RRT* 在障碍物密度 > 8 目标/nm² 且 Mid-MPC 求解连续超时（> 30ms × 3 次）时激活；VO 在 BC-MPC 求解失败时辅助；MPPI 不在 D1-D3 认证路径中。
> **D-task 联动**：D3.2 M5 完整实现时以此矩阵验证选型；D4.6 RL 对抗启动时 MPPI 路径重评。

```

- [ ] **Step 5: Verify §10 structure**

```bash
grep -n "^### 10\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: §10.1 算法选型矩阵 / §10.2 決策原因 / §10.3 双层MPC ... / §10.9 決策依据.

- [ ] **Step 6: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A2): add §10.1 algorithm selection matrix; renumber §10.1-8 → §10.2-9"
```

---

## Task A3: §10.6 4-DOF 适用性边界扩充

> §10.5 "FCB 高速船型修正" became §10.6 after Task A2.

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` at §10.6

- [ ] **Step 1: Find §10.6 / §10.7 boundary**

```bash
grep -n "^### 10\.6\|^### 10\.7" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 2: Append 4-DOF boundary table at end of §10.6 content（before §10.7）**

```markdown

**FCBPlugin 4-DOF 适用性边界 [D2.8 新增 — 架构权威声明]**

| 条件 | 规则 | 实现路径 |
|---|---|---|
| `SOG ≤ 12 kn` AND `hull_class ≠ SEMI_PLANING` | **4-DOF 适用**（Yasukawa 2015 [R7]）| `FCBPlugin::DisplacementMMG::step()` |
| `hull_class == SEMI_PLANING` | **6-DOF 激活**（Savitsky / semi-planing empirical）| D2.1 stub → D3.2 完整 |
| `SOG > 12 kn` | **6-DOF 预留**（CasADi/IPOPT 高速流体动力学）| Phase 4 实装 D4.7 |

> **[TBD-HAZID]**：12 kn 阈值在 HAZID RUN-001（8/19）以 FCB 实测操纵数据校准。当前值为 Yasukawa 2015 MMG 标准方法适用范围上限估算。
> **D-task 联动**：D1.3a（FCBPlugin 实装）/ D2.1 M1 ODD（hull_class 判断接口）/ D3.2 M5 完整（6-DOF 实装）。

```

- [ ] **Step 3: Verify**

```bash
grep -A 8 "FCBPlugin 4-DOF 适用性边界" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md" | head -10
```

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A3): extend §10.6 with 4-DOF applicability boundary table"
```

---

## Task A4: §11.10 L3 仲裁优先级矩阵

> Spec says "§11.9" but §11.9–§11.9.2 already exist（v1.1.1）. New section is §11.10.

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — insert after §11.9.2

- [ ] **Step 1: Find end of §11.9.2 / start of §12**

```bash
grep -n "#### 11\.9\.2\|## 第十二章" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 2: Insert §11.10 block before `## 第十二章`**

Find the blank line immediately before `## 第十二章 M8` and insert:

```markdown

### 11.10 L3 内部仲裁优先级矩阵 [D2.8 新增 — Finding D P1-D-08 关闭]

L3 TDL 三类横向干预通道的仲裁优先级。高优先级通道可覆盖低优先级通道；被覆盖通道冻结输出。

**优先级顺序（1 = 最高）**：

1. **Hardware Override**（零软件硬连线 Z-BOTTOM）— L3 完全冻结
2. **Y-axis Reflex Arc**（< 500 ms，直达 L5）— L3 输出被覆盖，M7 告警保留
3. **X-axis Deterministic Checker VETO** — M7 接收 `CheckerVetoNotification` 触发 MRM
4. **M7 Doer-Checker 内部否决** — 发布 `Safety_AlertMsg` + `recommended_mrm`，M1 执行 MRM
5. **M1 ODD 状态切换**（软件决策，最低）— 广播 `ODD_StateMsg`，M4/M5 切换行为集

**触发状态 × 横向通道矩阵**：

| 横向通道 | ODD IN（D4 正常）| CRITICAL / ODD_OUT | 人工接管激活 |
|---|---|---|---|
| Hardware Override | 不活跃 | 优先级 1，立即接管 | 优先级 1，L3 完全冻结 |
| Y-axis Reflex Arc | 监控中（CPA < 200m 触发）| 优先级 2，与 MRC 并行 | 继续运行（不受 L3 冻结约束）|
| X-axis Det. Checker | 低频 VETO 接收 | 优先级 3，强制 MRM | Override 期间暂停（§11.8）|
| M7 Doer-Checker | 连续监控，SOTIF 双轨 | 优先级 4，MRM 触发 | 暂停主仲裁，保留降级告警（§11.9.1）|
| M1 ODD 状态 | 1 Hz 周期 + 事件 | 优先级 5，MRC 序列 | OVERRIDDEN 模式（§11.8）|

> **独立性保证**：Hardware Override 和 Y-axis Reflex Arc 的触发路径不经过任何 L3 软件（含 M7）。L3 仅接收通知（`ReflexActivationNotification` / `OverrideActiveSignal`），不参与触发决策。
> **D-task 联动**：D2.1 M1（实装矩阵中 M1 行）/ D2.5 SIL 集成（验证优先级通道在 SIL 可测试）/ D3.3b SOTIF（M7 行详细场景分析）。

```

- [ ] **Step 3: Verify**

```bash
grep -n "### 11\.10\|L3 内部仲裁优先级" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A4): add §11.10 L3 arbitration priority matrix (closes D P1-D-08)"
```

---

## Task A5: §12.3.1 船长心智模型 stub

> §12.3 "差異化視圖設計" already exists. Add §12.3.1 as a new sub-subsection.

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` at §12.3

- [ ] **Step 1: Find §12.3 / §12.4 boundary**

```bash
grep -n "^### 12\.3\|^### 12\.4" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 2: Insert §12.3.1 before `### 12.4`**

```markdown

#### 12.3.1 船长心智模型 Ground Truth（D2.6 访谈结果）[D2.8 stub — TBD-D2.6]

5 位 FCB 类场景经验船长的 HF 访谈结果接收位。D2.6 完成（目标 7/13）后填充此表。D3.4 M8 完整实装时以此表为 SAT-1/2/3 接口细节输入。

**[TBD-D2.6]** — 填充时间：D2.6 交付后（7/13）

| 船长 ID | 关注信息优先级 | 干预阈值（CPA/TCPA）| 信任触发 | 不信任触发 | 关键 UX 引言 |
|---|---|---|---|---|---|
| Captain #1 | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |
| Captain #2 | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |
| Captain #3 | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |
| Captain #4 | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |
| Captain #5 | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |

> **D-task 联动**：D2.6（7/13 填充此表）→ D3.4 M8 完整（以此表为 SAT 接口细节输入）。

```

- [ ] **Step 3: Verify**

```bash
grep -n "12\.3\.1\|心智模型 Ground Truth" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A5): add §12.3.1 captain mental model stub (TBD-D2.6)"
```

---

## Task A6: §12.6 培训矩阵 stub

> §12.5 "決策依據" already exists. Training matrix goes to §12.6.

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — insert after §12.5

- [ ] **Step 1: Find end of §12 / start of §13**

```bash
grep -n "^### 12\.5\|## 第十三章" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 2: Insert §12.6 before `## 第十三章`**

```markdown

### 12.6 人员资质 / 培训 / 演练 [D2.8 新增]

本节定义 L3 TDL 操作员资质要求框架。完整矩阵由 D2.6（7/27 可用性测试）和 D3.5' 培训课程大纲填充。

#### 12.6.1 ROC 操作员资质要求 stub

**知识维度**（最低要求，**[TBD-D2.6]** 最终确认）：
- COLREG Rules 5-19 在 D2-D4 自主等级下的适用边界
- L3 TDL ODD 子域概念 + M1-M8 功能职责
- ToR 协议（§12.4）操作流程 + 60-150 s 接管窗口矩阵（§3.4）
- SAT-1/2/3 界面（§12.2）读法 + M7 Doer-Checker verdict 解读

**最低持证要求**：**[TBD-D2.6]**（STCW 证书等级 + L3 专项证书路径，待 HF 咨询 + CCS 确认）

#### 12.6.2 培训矩阵 stub

**[TBD-D2.6]** — 填充时间：D2.6 可用性测试报告（目标 7/27）

| 角色 | 培训科目 | 时长 (h) | 验收标准 |
|---|---|---|---|
| ROC 主操作员 | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |
| ROC 备用操作员 | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |
| 船长（监督角色）| [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |
| 维护工程师 | [TBD-D2.6] | [TBD-D2.6] | [TBD-D2.6] |

#### 12.6.3 定期演练制度 stub

**演练频率**（初始建议，**[TBD-HAZID]** 校准）：
- 月度：ToR 演练（≥ 2 次 / 月，覆盖 ROC 在岗 + 桥楼备岗场景）
- 季度：DEGRADED 场景演练（传感器降质 + ODD EDGE）
- 年度：CRITICAL 场景预演（ODD_OUT + MRC 启动 + 完整接管，SIL 仿真）

> **D-task 联动**：D2.6（7/27 填充 §12.6.2）/ D3.5' 培训课程大纲（完整化 §12.6.1/§12.6.3）/ D4.5' 船长/ROC 模拟器认证（最终版本输入）。

```

- [ ] **Step 3: Verify**

```bash
grep -n "^### 12\.6\|培训矩阵 stub" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A6): add §12.6 training matrix stub (TBD-D2.6)"
```

---

## Task A7: §13.6 验证与确认策略前向指针

> §13.5 "典型船型扩展路径" is the last §13 subsection. New section is §13.6.

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — insert after §13.5

- [ ] **Step 1: Find §13.5 / §14 boundary**

```bash
grep -n "^### 13\.5\|## 第十四章" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 2: Insert §13.6 before `## 第十四章`**

```markdown

### 13.6 验证与确认策略前向指针 [D2.8 新增]

本节为 L3 TDL 各验证层的前向导航表。架构报告不重复 V&V 规划细节；完整规划见各策略文档。架构决策与 V&V 的双向联动通过 D-task 联动表维护。

| 验证层 | 策略文档 | 关联 D-task | 状态 |
|---|---|---|---|
| **SIL（软件在环）** | [D1.5 V&V Plan](../Phase%201/D1.5-vv-plan-scenario-qual/V%26V_Plan/00-vv-strategy-v0.1.md) | D1.3c / D2.5 / D3.6 | ✅ v0.1 完成 2026-05-12 |
| **场景覆盖率** | §19 场景库与覆盖立方体（本报告）+ D1.6/D1.7 | D1.6 / D1.7 / D3.6 | 🔴 D1.6/D1.7 stub 待 Phase 1 末 |
| **模块级单元测试** | `src/l3_tdl_kernel/M*/test/` | 每 D{2.x} 模块 D-task | ⏳ 随 Phase 2 推进 |
| **集成测试（M1-M6 联调）** | [D2.5 spec](../D2.5-sil-m1-m6-integration/D2.5-spec.md) | D2.5 | ⏳ 7/31 目标 |
| **HIL（硬件在环）** | D4.1/D4.2（Phase 4 启动后建立）| D4.1 / D4.2 | ⏳ Phase 4（9 月起）|
| **认证级实船试航** | D5.x（2027 Q1/Q2，AIP 受理后）| D5.x | ⏳ Phase 5 |

```

- [ ] **Step 3: Verify**

```bash
grep -n "^### 13\.6\|验证与确认策略前向指针" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A7): add §13.6 V&V strategy forward pointer table"
```

---

## Task A8: §15.0 时基与同步

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — insert as first subsection of §15

- [ ] **Step 1: Find §15.1 heading**

```bash
grep -n "^### 15\.1\|## 第十五章" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
§15.0 inserts between `## 第十五章 接口契约总表` chapter intro (if any) and `### 15.1`.

- [ ] **Step 2: Insert §15.0 immediately before `### 15.1`**

```markdown
### 15.0 时基与同步 [D2.8 新增]

所有 L3 消息 `header.stamp` 必须使用同一 PTP 时钟源。任何节点使用系统墙钟（`std::chrono::system_clock`）记录 ASDR 时间戳均为错误。

| 项目 | 规格 |
|---|---|
| **PTP Grandmaster** | IEEE 1588v2（PTPv2）；OT 侧部署于 GNSS disciplined master clock |
| **同步误差预算** | L3 节点间时钟偏差 ≤ 1 ms（`/time/ptp_status` 话题监控）|
| **`stamp` 字段强制** | 所有 L3 消息 IDL 必须携带 `header.stamp`（ROS2 builtin `Time`，含 sec + nanosec）|
| **退化路径** | PTP 失锁（≥ 3 s）→ M1 发布 `ODD_EDGE`；系统降级至 ASSISTED；M8 显示"时钟失步"红色告警 |
| **ASDR 时基** | ASDR JSONL 每行 `timestamp` 使用同一 PTP 时钟；禁止系统墙钟 |

**置信度**：🟢 High（IEEE 1588v2 是 IACS UR E26 工控网络基准要求；ROS2 `rclcpp::Time` 内建 PTP 源绑定支持）

> **D-task 联动**：D1.3c FMI 桥（dds-fmu latency 对照 ≤ 1 ms 预算）/ D2.5 SIL 集成（SIL 内 PTP 模拟路径）/ D3.9 RFC-007（PTP 时基安全性 + replay protection 接口）。

```

- [ ] **Step 3: Verify**

```bash
grep -n "^### 15\.0\|时基与同步" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A8): add §15.0 time base & PTP sync spec"
```

---

## Task A9: §15.2 接口矩阵新增 4 列安全字段

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` at §15.2

The §15.2 table currently has 4 columns（发布者→订阅者 / 消息类型 / 频率 / 关键内容）. Add 4 security columns to every row.

Profile assignment:
- **L3 内部 M-to-M**：`hmac-sha256 | hmac-sha256 | seq-counter | L3-internal`
- **ROC-Link**（M8→ROC/Captain）：`dds-auth | hmac-sha256 | ptp-window | ROC-link`
- **外部上游**（L1/L2/Fusion → L3）：`none | crc32 | none | open`
- **跨系统通道**（X-axis/Y-axis/HW Override）：`none | crc32 | none | open`（安全性由物理隔离保证，见 §16.1）
- **L3 → 下游**（M5→L4）：`none | crc32 | none | open`

- [ ] **Step 1: Read current §15.2 table header**

```bash
grep -n "### 15\.2\|发布者 → 订阅者" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 2: Replace §15.2 table header line with extended header**

Using the Edit tool, find the exact table header line:
```
| 发布者 → 订阅者 | 消息类型 | 频率 | 关键内容 |
```
and replace with:
```
| 发布者 → 订阅者 | 消息类型 | 频率 | 关键内容 | auth_required | integrity_mechanism | replay_protection | dds_security_profile |
```
Also replace the separator line:
```
|---|---|---|---|
```
with:
```
|---|---|---|---|---|---|---|---|
```

- [ ] **Step 3: Add 4 columns to each data row**

For each row in the table, append the security columns. Use the Edit tool row by row, or use a sed-based batch approach. The exact values per row:

```
L1 Voyage Order → M3       → none | crc32 | none | open
L2 WP_Generator → M3,M5   → none | crc32 | none | open
L2 Speed_Profiler → M3,M5 → none | crc32 | none | open
Multimodal Fusion → M2 (×3)→ none | crc32 | none | open
M1 → M2,M3,M4,M5,M6,M7,M8→ hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
M1 → M4                   → hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
M2 → M3,M4,M5,M6          → hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
M3 → M4                   → hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
M3 → L2 Voyage Planner    → none | crc32 | none | open
L2 Voyage Planner → M3    → none | crc32 | none | open
M4 → M5                   → hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
M6 → M5                   → hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
M5 Mid-MPC → L4           → none | crc32 | none | open
M5 BC-MPC → L4            → none | crc32 | none | open
M7 → M1                   → hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
X-axis Checker → M7       → none | crc32 | none | open
Y-axis Reflex Arc → L5    → none | crc32 | none | open
Y-axis Reflex Arc → M1    → none | crc32 | none | open
Hardware Override → M1    → none | crc32 | none | open
M1..M7 → ASDR            → hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
M1,M2-M7 → M8            → hmac-sha256 | hmac-sha256 | seq-counter | L3-internal
M8 → ROC/Captain          → dds-auth | hmac-sha256 | ptp-window | ROC-link
M8 → ROC (ToR)            → dds-auth | hmac-sha256 | ptp-window | ROC-link
```

- [ ] **Step 4: Add footnote after table**

Append immediately after the table's last row:

```markdown
> **[D2.8 security columns 注]**：列值为 v1.1.3-stub 初始设计值。L3-internal 和 ROC-link DDS-Security policy XML 在 D3.9 RFC-007 交付。跨系统通道（X-axis / Y-axis / Hardware Override）安全性由物理隔离实现（§16.1 Zone 划分），不依赖 DDS-Security。完整 IACS UR E27 符合性分析见 D3.9。
```

- [ ] **Step 5: Verify column count in header**

```bash
grep "dds_security_profile" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: header line + separator + footnote (3 lines containing `dds_security_profile`).

```bash
grep "^|.*L3-internal" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md" | wc -l
```
Expected: ≥ 9 rows.

- [ ] **Step 6: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/A9): add 4 security columns to §15.2 interface matrix"
```

---

## Task B1: §16 Cybersecurity Spec（新章节）

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — insert before `## 第十六章 参考文献`

> All B tasks insert before this anchor. The old §16 heading is NOT yet renamed（happens in B8）.

- [ ] **Step 1: Confirm anchor**

```bash
grep -n "## 第十六章 参考文献" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 2: Insert full §16 chapter before `## 第十六章 参考文献`**

```markdown
---

## 第十六章 Cybersecurity Spec（Stub）[D2.8 新增 — D3.9 RFC-007 完整化]

本章为 L3 TDL 网络安全框架的 stub 规格，定义 L3 DDS 信任域边界、接口认证机制占位、ASDR 签名升级路径。完整实现（IACS UR E26/E27 分析 + DDS-Security policy XML）在 D3.9 RFC-007 中交付。

**Finding 关闭**：F P0-F-01（cyber 架构位置占位）stub 级别关闭。

### 16.1 信任域划分（Trust Zone）

| Zone | 边界 | 内部协议 | 认证机制 |
|---|---|---|---|
| **Zone 1**（L3 DDS 内部）| OT 网络 + Data Diode | ROS2 DDS Humble | DDS-Security `L3-internal`（D3.9 完整化）|
| **Zone 2**（ROC Shore Link）| DMZ + Data Diode + TLS | TLS 1.3 over DDS | DDS-Security `ROC-link` + mTLS |
| **Zone 3**（ENC / Chart Server）| 只读 ingress，验证签名 | HTTPS | ENC 来源数字签名（CCS 认可来源）|
| **Zone 4**（ASDR Evidence Store）| append-only，无删除接口 | JSONL + HMAC | HMAC-SHA256 签名链（§16.3）|

信任域边界：Zone 1 ↔ Zone 2 通过 DMZ 上的 data diode 单向隔离；Zone 3 和 Zone 4 为 L3 发出方向（只写）。

> **[TBD-D3.9]**：Zone 边界防火墙规则 / Zone 1 ↔ Zone 2 data diode 配置 / IACS UR E26 §4 网络分段完整映射。

### 16.2 L3 接口安全矩阵

接口安全分类引用 §15.2 新增 4 列（`auth_required` / `integrity_mechanism` / `replay_protection` / `dds_security_profile`）。

**高风险接口**（D3.9 优先审查）：

| 接口 | 风险原因 | D3.9 审查项 |
|---|---|---|
| M8 → ROC（ToR_RequestMsg）| 接管请求伪造 → 操作员错误接管 | ROC-link mTLS + 消息签名 |
| M7 → M1（Safety_AlertMsg）| VETO 伪造 / 抑制 → 安全监控失效 | L3-internal HMAC + replay counter |
| X-axis Checker → M7 | 跨系统接口，物理隔离为主保障 | Zone 1 物理路径审查 |
| M8 → ROC（UI_StateMsg 50Hz）| 高频注入攻击面 | ROC-link rate limiting + auth |

> **[TBD-D3.9]**：完整 IACS UR E27 §3 威胁模型 + 缓解措施映射。

### 16.3 ASDR HMAC 升级 Stub

**当前状态**：ASDR JSONL 每行携带 `signature` 字段（SHA-256 防篡改，§15.1 IDL）但密钥管理机制未定义。

**D2.8 stub 升级路径**：
- 目标：每行追加 `"hmac_sha256": "<64-char hex>"` — 将 SHA-256 升级为 HMAC-SHA256（密钥化）
- 签名密钥来源：§22.2 Parameter Store `SafetyMargins` 话题中的 `asdr_hmac_key` 字段（Phase 4 D4.7 实装）
- 代码占位：ASDR write 路径加注释 `// TODO[D3.9]: replace SHA256 with HMAC-SHA256, key from §22.2 Parameter Store`

**完整实现**：D3.9 RFC-007（密钥管理 + 轮换 + IACS UR E27 §5 符合性）

**[TBD-D3.9]**：密钥管理协议 / 密钥轮换机制 / Zone 4 append-only 强制机制。

### 16.4 D-task 联动

| D-task | 联动内容 | 方向 |
|---|---|---|
| D3.9 RFC-007 Cybersecurity | §16 完整化，IACS UR E26/E27 | 填充方 |
| D2.1 M1 ODD | Zone 1 DDS-Security `L3-internal` 实装 | 消费方 |
| D2.5 SIL 集成 | §16.3 ASDR TODO 注释实装 | 实现方 |

### 16.5 前向引用

| 话题 | 位置 |
|---|---|
| D3.9 完整 IACS UR E26/E27 分析 | `docs/Design/Phase 3/D3.9-rfc007-cybersecurity/`（D3.9 启动时建立）|
| Zone 2 ROC-Link 接口细节 | `docs/Design/Cross-Team Alignment/RFC-decisions.md` RFC-005 |
| DDS-Security policy XML | `src/l3_tdl_kernel/config/dds_security/`（D3.9 产出）|

**置信度**：🟡 Medium — Zone 划分参照 IACS UR E26（2022）§4 概念；DDS-Security profile 细节需 D3.9 工程验证。

```

- [ ] **Step 3: Verify**

```bash
grep -n "## 第十六章 Cybersecurity\|### 16\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: new §16 chapter with §16.1-§16.5, and old §16 参考文献 further down.

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/B1): add §16 Cybersecurity Spec stub (closes F P0-F-01 stub)"
```

---

## Task B2: §17 SIL 框架架构（Appendix F.1-F.3 迁入）

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — insert before `## 第十六章 参考文献`

Appendix F §F.1-F.3 source: `## 附録 F SIL 框架架構` → first three sub-sections（`### F.1`/`### F.2`/`### F.3`，lines ~2038-2165）. Read them then compose the new chapter.

- [ ] **Step 1: Read Appendix F §F.1-F.3**

```bash
grep -n "^### F\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Note exact line numbers for F.1 / F.2 / F.3 / F.4 to know the range to copy.

- [ ] **Step 2: Insert §17 chapter before `## 第十六章 参考文献`**

Insert the following header + intro + D-task table, then copy verbatim the content of Appendix F §F.1 / §F.2 / §F.3 (sub-headings become `### 17.1` / `### 17.2` / `### 17.3`):

```markdown
---

## 第十七章 SIL 框架架构 [D2.8 新增 — 原附录 F §F.1-F.3 迁入]

本章定义 L3 TDL SIL（Software-in-the-Loop）测试框架的顶层架构，确保"测试目标即部署目标"——production C++/MISRA ROS2 节点直接运行于 SIL 内核。本章由 v1.1.3-pre-stub 附录 F §F.1-F.3 迁入，为架构报告永久章节。填充路径：D1.3c（FMI 桥实装）→ D2.5（50 场景 SIL 集成）→ D3.6（1000 场景扩展）。

**Finding 关闭**：SIL P0 SIL-1（SIL 框架架构决策锁定）/ SIL-2（DNV 工具链 3 MUST）/ SIL-3（ROS2 Humble + RT 锁定）

**[DNV-FEEDBACK-HOOK] §17.A**：以下内容依赖 DNV 验证官对 maritime-schema acceptance 的反馈（预计 7/31 后收到）：`maritime-schema TrafficSituation` 作为 evidence container 的 CCS 可接受性。若 CCS 不接受：回退路径 = maritime-schema 作内部表示 + 导出器（需 D3.8 patch）。本 hook 在 D3.8 完整化时根据反馈决议删除或替换。

### 17.1 SIL Harness 架构（原 F.1）

[从附录 F §F.1 "### F.1 选項 D 混合架構 — SIL Harness 设計決策" 整节内容原文迁入，包括架構圖、三层模擬策略表、禁止反模式列表]

> **执行说明**：读取 `## 附録 F SIL 框架架構` 下的 `### F.1` 全部内容（至 `### F.2` 之前），改标题为 `### 17.1 SIL Harness 架构`，原文粘贴。

### 17.2 DNV 工具链锁定（原 F.2）

[从附录 F §F.2 整节内容原文迁入，包括工具链 MUST/NICE 表、ROS2↔FMI 桥接边界、FCBPlugin C++ 代码片段]

> **执行说明**：读取 `### F.2` 全部内容（至 `### F.3` 之前），改标题为 `### 17.2 DNV 工具链锁定`，原文粘贴。

**[TBD-D2.5]**：dds-fmu latency budget 实测值（D2.5 完成后更新 §17.2 预算表）。

### 17.3 ROS2 + OS + RT 锁定（原 F.3）

[从附录 F §F.3 整节内容原文迁入]

> **执行说明**：读取 `### F.3` 全部内容（至 `### F.4` 之前），改标题为 `### 17.3 ROS2 + OS + RT 锁定`，原文粘贴。

### 17.4 D-task 联动

| D-task | 联动内容 | 方向 | 目标时间 |
|---|---|---|---|
| D1.3c FMI 桥 | dds-fmu latency 实测值更新 §17.2 预算表 | 填充 | Phase 1 末 |
| D2.5 SIL 集成 | 50 场景验证 §17.1 架构图各连接真实通 | 验证 | 7/31 |
| D3.6 SIL 1000+ | 三层模拟策略 1000-cell 扩展验证 | 扩展 | Phase 3 |
| D4.6 RL 对抗 | NICE-deferred mlfmu 实装，§17.2 更新 | 激活 | Phase 4 |

```

- [ ] **Step 3: Verify**

```bash
grep -n "## 第十七章\|### 17\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: §17 chapter with §17.1-§17.4.

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/B2): add §17 SIL framework (migrate Appendix F.1-F.3); closes SIL-1/2/3"
```

---

## Task B3: §18 RL 隔离架构（Appendix F.4 迁入）

**Files:** Modify at `## 第十六章 参考文献` anchor.

- [ ] **Step 1: Insert §18 chapter before `## 第十六章 参考文献`**

```markdown
---

## 第十八章 RL 隔离架构 [D2.8 新增 — 原附录 F §F.4 迁入]

本章定义 RL（强化学习）对抗生成器与认证核心（M1-M8）的三层强制隔离边界。即使 B2 RL 推 Phase 4 启动，本章在 v1.1.3 stub 中锁定边界规则，确保 Phase 1-3 仓库结构和 CI lint 提前合规，避免 Phase 4 启动时回退污染已审认证内核。

**Finding 关闭**：SIL P0 SIL-4（RL 隔离三层边界锁定）
**置信度**：🟡 Medium — DNV-RP-0671/0510/0513 直接条款引用在 D3.8 完整化时验证；架构报告 §F.4 对应 [R25][R30][R31]。

### 18.1 三层隔离边界（原 F.4）

[从附录 F §F.4 "### F.4 RL 隔离架构 — 三层强制边界" 整节内容原文迁入，包括 L1 Repo / L2 Process / L3 Artefact 三层边界表和 Phase 4 启动条件]

> **执行说明**：读取 `### F.4` 全部内容（至 `### F.5` 之前），改标题为 `### 18.1 三层隔离边界`，原文粘贴。

### 18.2 技术选型占位 [TBD-D4.6]

- **L1 Repo 隔离**：单体 monorepo（`/src/rl_workbench/` 独立 colcon 包）；具体 CI lint rule 已在 D1.3a/D1.3b 仓库结构落地（§18.1 引用）
- **L2 Process 隔离**：Docker compose 独立 namespace；K8s pod 隔离为 Phase 4 升级选项（D4.6 启动时评估）
- **L3 Artefact**：`mlfmu build` → `target_policy.fmu` → evidence store；DNV-RP-0671 鉴定流程 **[TBD-D4.6]**

### 18.3 D-task 联动

| D-task | 联动内容 | 方向 |
|---|---|---|
| D1.3a / D1.3b | L1 Repo CI lint rule 已落地 → §18 引用确认 | 现状确认 |
| D1.5 V&V Plan | §18.3 RL artefact 进 V&V 门条件 | 输入 |
| D4.6 RL 对抗（Phase 4）| §18.2 全部 [TBD] 填充 + mlfmu 鉴定 | 激活 |

**[TBD-D4.6]**：L2/L3 隔离完整技术规格 / mlfmu 鉴定流程 / DNV-RP-0671 条款映射。

```

- [ ] **Step 2: Verify**

```bash
grep -n "## 第十八章\|### 18\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 3: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/B3): add §18 RL isolation architecture (migrate F.4); closes SIL-4"
```

---

## Task B4: §19 场景库与覆盖立方体方法论（Appendix F.5-F.7 迁入）

**Files:** Modify at `## 第十六章 参考文献` anchor.

- [ ] **Step 1: Insert §19 chapter before `## 第十六章 参考文献`**

```markdown
---

## 第十九章 场景库与覆盖立方体方法论 [D2.8 新增 — 原附录 F §F.5-F.7 迁入]

本章定义 L3 TDL 场景库工程体系：从 maritime-schema 场景语言到 Imazu-22 强制基线，再到 1100-cell 覆盖立方体和 AIS-driven authoring 工具链。本章为 V&V Plan（D1.5）的场景层补充，由 D1.3b.2（AIS tool）、D1.6（schema 规约）和 D3.6（1000 场景）持续填充。

**Finding 关闭**：SIL P0 SIL-5（maritime-schema 替代决策）/ SIL-6（Imazu-22 + 1100-cell 方法论）/ F P0-F-02 + F P0-F-04 stub + MV-7~11 stub

**[DNV-FEEDBACK-HOOK] §19.A**：`maritime-schema TrafficSituation` 作为 evidence container 的 CCS 接受度（同 §17.A，同一 hook）。7/31 后 DNV 反馈收到后 D3.8 patch。

### 19.1 场景 Schema（原 F.5）

[从附录 F §F.5 "### F.5 场景 schema — maritime-schema TrafficSituation 扩展" 整节内容原文迁入]

> **执行说明**：读取 `### F.5` 全部内容（至 `### F.6` 之前），改标题为 `### 19.1 场景 Schema`，原文粘贴。

### 19.2 场景库与覆盖立方体（原 F.6）

[从附录 F §F.6 "### F.6 场景库与覆盖立方体方法论" 整节内容原文迁入]

> **执行说明**：读取 `### F.6` 全部内容（至 `### F.7` 之前），改标题为 `### 19.2 场景库与覆盖立方体`，原文粘贴。

### 19.3 AIS-driven Scenario Authoring 工具（原 F.7）

[从附录 F §F.7 "### F.7 AIS-driven scenario authoring 工具（D1.3b.2 NEW）" 整节内容原文迁入]

> **执行说明**：读取 `### F.7` 全部内容（至 `### F.8` 之前），改标题为 `### 19.3 AIS-driven 工具`，原文粘贴。

### 19.4 D-task 联动

| D-task | 联动内容 | 方向 | 目标时间 |
|---|---|---|---|
| D1.6 场景 schema | maritime-schema 扩展字段 + 双语言验证实装 | 填充 §19.1 | Phase 1 |
| D1.3b.2 AIS tool | 5 阶段管线实装 + 数据源连通验证 | 填充 §19.3 | Phase 1/2 |
| D3.6 SIL 1000+ | 1100-cell 覆盖立方体 full run + LHS/Sobol | 填充 §19.2 | Phase 3 |
| D4.6 RL 对抗 | adversarial scenario 回路激活（§19.2 60%）| 激活 §19.2 | Phase 4 |

**[TBD-D3.8]**：Monte Carlo 实测分布 / adversarial 60% 生成路径 / 60/25/15 比例最终辩护文件。

```

- [ ] **Step 2: Verify**

```bash
grep -n "## 第十九章\|### 19\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 3: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/B4): add §19 scenario library (migrate F.5-F.7); closes SIL-5/6, F P0-F-02/F-04 stub"
```

---

## Task B5: §20 结构化 COLREGs 评分（Appendix F.8 迁入）

**Files:** Modify at `## 第十六章 参考文献` anchor.

- [ ] **Step 1: Insert §20 chapter before `## 第十六章 参考文献`**

```markdown
---

## 第二十章 结构化 COLREGs 评分 [D2.8 新增 — 原附录 F §F.8 迁入]

本章定义 L3 TDL 结构化 COLREGs 评分方法：PASS/FAIL 二元判定保留基础上，增加 6 维度连续评分作为 CCS surveyor 论据。评分框架由 D1.7 和 D2.4 共同填充，本章为框架宿主。

**Finding 关闭**：SIL P0 SIL-7（6D 评分框架锁定）/ F P1-F-03（COLREGs scoring 方法明确）
**置信度**：🟡 Medium（6D 维度结构学术圈公认；具体权重需 D1.7 引 Hagen 2022 / Woerner 2019 原文确认）

### 20.1 6 维度评分框架（原 F.8）

[从附录 F §F.8 "### F.8 结构化 COLREGs 评分（Hagen 2022 / Woerner 2019）" 整节内容原文迁入，包括 6 维度表 + 权重公式 `total_score = w_s·safety + w_r·rule − p_delay − p_mag + w_p·phase`]

> **执行说明**：读取 `### F.8` 全部内容（至 `### F.9` 之前），改标题为 `### 20.1 6 维度评分框架`，原文粘贴。

### 20.2 权重参数版本控制 [D2.8 新增]

| 时间点 | 状态 |
|---|---|
| D2.8 stub（当前）| w_s / w_r / w_p / λ_1 = **[TBD-D1.7]**（待 D1.7 Hagen 2022 原文细节）|
| D2.4 实装后 | D2.4 report 提交权重 stub；§20 只引用，不 override |
| D3.6 1000 场景后 | 用实测 score distribution 回检权重合理性（推翻信号）|

### 20.3 与 D2.4 联动协议

§20 定义框架（维度 + 公式结构），D2.4 填充参数（权重 + per-rule threshold）。

- D2.4 完成后：PR 仅更新 §20.2 权重参数表，不修改维度或公式
- 避免循环返工：§20 框架在 D2.4 期间不得修改；若 D2.4 发现维度结构需调整 → D2.4 architect review 再改 §20

### 20.4 D-task 联动

| D-task | 联动内容 | 方向 |
|---|---|---|
| D1.7 覆盖率方法论 | Hagen 2022 [R33] / Woerner 2019 [R34] 权重原文细节 → §20.2 | 填充 §20.2 |
| D2.4 M6 COLREGs 实装 | 权重 stub + per-rule threshold → §20.2 patch | 填充 §20.2 |
| D3.6 SIL 1000+ | 实测 score distribution → 权重合理性验证 | 验证 §20.2 |

**推翻信号**：若维度与 CCS《智能船舶规范 2024》§9.1 性能验证条款不可对齐 → 需 D2.4 architect review 重构。

```

- [ ] **Step 2: Verify**

```bash
grep -n "## 第二十章\|### 20\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 3: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/B5): add §20 structured COLREGs scoring (migrate F.8); closes SIL-7, F P1-F-03"
```

---

## Task B6: §21 Web HMI 与 ENC 集成（Appendix F.9 迁入）

**Files:** Modify at `## 第十六章 参考文献` anchor.

- [ ] **Step 1: Insert §21 chapter before `## 第十六章 参考文献`**

```markdown
---

## 第二十一章 Web HMI 与 ENC 集成 [D2.8 新增 — 原附录 F §F.9 迁入]

本章定义 L3 TDL 操作员 HMI 的参考架构，替代原型 `L3_TDL_SIL_Interactive.html`，以 MapLibre GL JS + S-57 MVT + foxglove_bridge 构建 production-grade web HMI。本章为 D1.3.2.3（Phase 1 基础）、D2.5/D2.6（Phase 2 增项）、D3.4（Phase 3 完整化）的宿主设计文档。

**Finding 关闭**：SIL P0 SIL-7（Web HMI 架构锁定）/ B P1-B-06（foxglove_bridge 选型理由）

### 21.1 技术选型与 XAI overlay（原 F.9）

[从附录 F §F.9 "### F.9 Web HMI + ENC 集成" 整节内容原文迁入，包括技术选型表和 XAI overlay 7 新增项]

> **执行说明**：读取 `### F.9` 全部内容（至 `---` 分隔线之前），改标题为 `### 21.1 技术选型与 XAI overlay`，原文粘贴。

### 21.2 D-task 产出物映射 [D2.8 新增]

| 产出物 | 来源 D-task | 进入 §21 时间 |
|---|---|---|
| MapLibre 骨架 + S-57 MVT + foxglove_bridge | D1.3.2.3 | Phase 1 末 |
| IEC 62288 SA subset 显示标准落地 | D1.3.2.3 | Phase 1 末 |
| Apache Arrow replay + scrubber + GIF evidence | D2.5 | 7/31 |
| 多 target 同屏 + grounding 高亮 + TLS/WSS | D2.5 | 7/31 |
| Figma 船长可用性 + ToR countdown panel | D2.6 | 7/13 |
| Trajectory ghosting + M7 verdict badge | D3.4 M8 完整 | Phase 3 |
| 1000 场景 evidence pack 一键导出 | D3.6 SIL 1000+ | Phase 3 |

### 21.3 IEC 62288 SA subset 范围界定 [D2.8 新增]

- **采用**：SA Presentation Level 2（关键导航数据 + 告警 + 预测轨迹）
- **不采用**：完整 ECDIS（S-52 complete grammar + TYPE_APPROVED 认证）
- **理由**：CCS credibility 需求通过 SA subset 满足；完整 ECDIS 认证成本 > 收益
- **NLM 证据**：maritime_human_factors notebook 🟢 High

### 21.4 IMO S-Mode 占位 [TBD-DNV]

S-Mode 目前仅有非强制指南状态；MASS Code Tier IV 强制时间未定。

**D2.8 stub**：§21 引用 S-Mode 概念 + "本 HMI 设计为 S-Mode ready"。
**D3.4 + D3.8**：根据 IMO S-Mode 最终版本完善。

### 21.5 D-task 联动

| D-task | 联动内容 | 方向 |
|---|---|---|
| D1.3.2.3 Web HMI + ENC | Phase 1 基础 2 项产出 → §21.2 Phase 1 行确认 | 填充 §21.2 |
| D2.5 SIL 集成 | Apache Arrow + scrubber + GIF → §21.2 Phase 2 行 | 填充 §21.2 |
| D2.6 船长 HF | Figma 原型 + ToR countdown → §21.2 + §21.3 输入 | 填充 §21.2 |
| D3.4 M8 完整 | Trajectory ghosting + M7 verdict badge → §21.2 Phase 3 | 填充 §21.2 |

```

- [ ] **Step 2: Verify**

```bash
grep -n "## 第二十一章\|### 21\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 3: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/B6): add §21 Web HMI & ENC (migrate F.9); closes B P1-B-06"
```

---

## Task B7: §22 延期支持模块 Stub（§22.1-§22.4）

**Files:** Modify at `## 第十六章 参考文献` anchor.

The content for §22.1-§22.4 comes directly from spec §6.7（D2.8-spec.md §§22.1-§22.4 sections）.

- [ ] **Step 1: Insert §22 chapter before `## 第十六章 参考文献`**

Insert the following header + §22.1-§22.4 content. The full spec content for each subsection is in `docs/Design/Phase 2/D2.8-arch-v1.1.3-stub/D2.8-spec.md` under `### §6.7`（§§22.1-22.4）— copy verbatim into the architecture report with these headings:

```markdown
---

## 第二十二章 延期支持模块 Stub [D2.8 新增]

以下 4 个模块为 L3 TDL 架构中识别的必要支持模块，因 Phase 1-3 资源约束（B4 standby）延至 Phase 4 D4.7 完整实现。本章以"接口契约 stub"形式记录，使 M1-M8 模块设计可引用其接口位，CCS 审计员可评估接口完整性，而不需要实现细节。

**格式**：每个子节遵循 M1-M8 接口契约最小模板（不写算法实现）。
**Finding 关闭**：D P1-D-04（BNWAS stub 设计位置）/ C P1-C-3 partial（§22 接口位确认）/ C P1-C-5 partial（§22 CCS 映射表）

### 22.1 ENC Manager
```

Then copy the full §22.1 / §22.2 / §22.3 / §22.4 content exactly from spec §6.7 (lines starting from `#### §22.1 ENC Manager` in the spec file). The spec already has all interface contract tables, degradation behavior, CCS mapping, and [TBD] markers.

- [ ] **Step 2: Read §22.x content from spec**

```bash
grep -n "#### §22\." "docs/Design/Phase 2/D2.8-arch-v1.1.3-stub/D2.8-spec.md"
```
Read the lines for each §22.x sub-section. Copy and paste into the architecture report, changing headings from `#### §22.1` to `### 22.1` etc.

- [ ] **Step 3: Verify §22 structure**

```bash
grep -n "## 第二十二章\|^### 22\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: §22 chapter with §22.1 ENC Manager / §22.2 Parameter Store / §22.3 Environment Cross-Source Validator / §22.4 BNWAS-equivalent.

```bash
grep "TBD-D4.7\|CCS 功能映射\|降级行为" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md" | grep -c "."
```
Expected: ≥ 12 lines (3 per module × 4 modules).

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/B7): add §22 deferred support modules stub (§22.1-§22.4); closes D P1-D-04"
```

---

## Task B8: §23 参考文献（§16 重排 + TOC 更新）

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — rename heading + update TOC

- [ ] **Step 1: Rename §16 参考文献 → §23**

Using the Edit tool, find:
```
## 第十六章 参考文献
```
Replace with:
```
## 第二十三章 参考文献
```

- [ ] **Step 2: Update TOC（lines 55-74 in original file）**

Find the current TOC block:
```
16. 参考文献
```
Replace the entire TOC numbered list with the updated version adding §16-§23:

```markdown
1. 背景与设计约束
2. 核心架构决策
3. ODD / Operational Envelope 框架
4. 系统架构总览
5. M1 — ODD / Envelope Manager
6. M2 — World Model
7. M3 — Mission Manager
8. M4 — Behavior Arbiter
9. M6 — COLREGs Reasoner
10. M5 — Tactical Planner
11. M7 — Safety Supervisor
12. M8 — HMI / Transparency Bridge
13. 多船型参数化设计
14. CCS 入级路径映射
15. 接口契约总表
16. Cybersecurity Spec（Stub）[D2.8]
17. SIL 框架架构 [D2.8]
18. RL 隔离架构 [D2.8]
19. 场景库与覆盖立方体方法论 [D2.8]
20. 结构化 COLREGs 评分 [D2.8]
21. Web HMI 与 ENC 集成 [D2.8]
22. 延期支持模块 Stub [D2.8]
23. 参考文献
```

- [ ] **Step 3: Verify TOC and §23 heading**

```bash
grep -n "## 第二十三章 参考文献\|23\. 参考文献" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: both lines present.

```bash
grep -n "## 第十六章 参考文献" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: no output (old heading gone).

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/B8): rename §16 References → §23; update TOC to include §16-§23"
```

---

## Task C1: Appendix F 退役

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — delete Appendix F content + update Appendix D'''''

- [ ] **Step 1: Confirm Appendix F boundaries**

```bash
grep -n "## 附录 F SIL 框架架构\|## 附录 D'''''" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Note exact line numbers. Appendix F runs from `## 附录 F SIL 框架架構` to the line before `## 附录 D'''''`.

- [ ] **Step 2: Delete Appendix F content**

Using the Edit tool, find the entire block from:
```
## 附录 F SIL 框架架構 [v1.1.3-pre-stub 新增 — 2026-05-09]
```
to the blank line immediately before:
```
## 附录 D''''' 修订记录
```
and replace it with the retirement notice:

```markdown
## 附录 F ~~SIL 框架架構~~ [D2.8 退役 — 2026-07-31]

> **附录 F 已退役**：原 §F.1-F.9 全部内容已迁入正章主体（§17-§21）。本附录保留此占位条目用于修订历史追溯。
> 迁入记录：F.1-F.3 → §17 / F.4 → §18 / F.5-F.7 → §19 / F.8 → §20 / F.9 → §21。

```

- [ ] **Step 3: Add D2.8 entry to Appendix D''''' revision record**

Find `## 附录 D''''' 修订记录（v1.1.2-patch2 → v1.1.3-pre-stub）` and add a D2.8 entry to its revision table:

```markdown
## 附录 D'''''' 修订记录（v1.1.3-pre-stub → v1.1.3-stub）[D2.8 — 2026-07-31]

| 决策 / 变更 | 影响章节 | 依据 |
|---|---|---|
| 附录 F §F.1-F.9 迁入正章 §17-§21 | §17/§18/§19/§20/§21（新建）| D2.8 spec §6.2-§6.6 |
| §16 Cybersecurity Spec 新建（stub）| §16 | D2.8 spec §6.1 |
| §22 延期支持模块 stub 新建（§22.1-§22.4）| §22 | D2.8 spec §6.7 |
| §16 参考文献 顺延为 §23 | §23 | 章节重排 |
| 附录 F 退役 | 附录 F（占位）| 内容已入正章 |
| §3.4 ToR 自适应矩阵新增 | §3.4 | D2.8 spec §5.1 |
| §10.1 算法选型矩阵新增 + §10.1-§10.8 重排 | §10.1-§10.9 | D2.8 spec §5.2 |
| §10.6 4-DOF 边界声明新增 | §10.6（原 §10.5）| D2.8 spec §5.3 |
| §11.10 L3 仲裁优先级矩阵新增 | §11.10 | D2.8 spec §5.4 |
| §12.3.1 心智模型 Ground Truth stub | §12.3.1 | D2.8 spec §5.5 |
| §12.6 培训矩阵 stub 新增 | §12.6 | D2.8 spec §5.6 |
| §13.6 验证策略前向指针新增 | §13.6 | D2.8 spec §5.8 |
| §15.0 时基与同步新增 | §15.0 | D2.8 spec §5.7 |
| §15.2 接口矩阵 4 新列（安全字段）| §15.2 | D2.8 spec §5.9 |
| 版本 v1.1.3-pre-stub → v1.1.3-stub | 文件头 | D2.8 完成 |

Finding 关闭（D2.8）：SIL P0 SIL-1~7 / F P0-F-01/F-02/F-04 stub / F P1-F-03 / C P1-C-3/C-5 partial / B P1-B-06 / D P1-D-04/D-08 / MV-7~11 stub

```

- [ ] **Step 4: Verify Appendix F is retired**

```bash
grep -c "^### F\." "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: `0` (all F.x sub-sections deleted).

```bash
grep -n "## 附录 F" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: retirement notice line only (no ### F.x sub-sections).

- [ ] **Step 5: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/C1): retire Appendix F (content migrated to §17-§21); add revision record D''''''"
```

---

## Task C2: 版本号提升 + 文件头更新

**Files:** Modify `docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md` — file header table

- [ ] **Step 1: Read current file header（lines 1-16）**

```bash
head -16 "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```

- [ ] **Step 2: Update header table fields**

Using the Edit tool, make these changes in the header table:

| Field | Old value | New value |
|---|---|---|
| 版本 | `v1.1.3-pre-stub（...）` | `**v1.1.3-stub**（基于 v1.1.3-pre-stub + D2.8 2026-07-31：§16-§22 新建/迁入，附录 F 退役，4 缺失模块 stub，CCS/DNV 邀请发出）` |
| 日期 | `2026-05-09` | `2026-07-31` |
| 状态 | current value | append `；v1.1.3-stub 已完成（2026-07-31，D2.8）；§16-§22 已入；D3.8 2026-08-31 v1.1.3 完整化` |

- [ ] **Step 3: Verify**

```bash
head -12 "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md" | grep "版本\|日期\|v1.1.3-stub"
```
Expected: `v1.1.3-stub` in version field, `2026-07-31` in date field.

- [ ] **Step 4: Commit**

```bash
git add "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
git commit -m "arch(D2.8/C2): bump version v1.1.3-pre-stub → v1.1.3-stub; update header date 2026-07-31"
```

---

## Task C3: DoD 检查 + Finding 闭环确认 + CCS/DNV 邀请

**Files:** Verify only (no new file edits except evidence)

- [ ] **Step 1: Run full DoD checklist**

```bash
ARCH="docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"

echo "=== 版本号 ===" && grep "v1.1.3-stub" "$ARCH" | head -1
echo "=== §3.4 ToR 矩阵 ===" && grep -c "ToR 自适应矩阵" "$ARCH"
echo "=== §10.1 算法选型 ===" && grep -c "算法选型矩阵" "$ARCH"
echo "=== §10.6 4-DOF 边界 ===" && grep -c "4-DOF 适用性边界" "$ARCH"
echo "=== §11.10 仲裁优先级 ===" && grep -c "### 11.10" "$ARCH"
echo "=== §12.3.1 心智模型 ===" && grep -c "12\.3\.1" "$ARCH"
echo "=== §12.6 培训矩阵 ===" && grep -c "### 12\.6" "$ARCH"
echo "=== §13.6 测试策略 ===" && grep -c "### 13\.6" "$ARCH"
echo "=== §15.0 时基 ===" && grep -c "### 15\.0" "$ARCH"
echo "=== §15.2 安全列 ===" && grep -c "dds_security_profile" "$ARCH"
echo "=== §16 Cybersecurity ===" && grep -c "## 第十六章 Cybersecurity" "$ARCH"
echo "=== §17 SIL ===" && grep -c "## 第十七章 SIL" "$ARCH"
echo "=== §18 RL ===" && grep -c "## 第十八章 RL" "$ARCH"
echo "=== §19 场景库 ===" && grep -c "## 第十九章" "$ARCH"
echo "=== §20 COLREGs 评分 ===" && grep -c "## 第二十章" "$ARCH"
echo "=== §21 Web HMI ===" && grep -c "## 第二十一章" "$ARCH"
echo "=== §22 延期模块 ===" && grep -c "## 第二十二章" "$ARCH"
echo "=== §22.1-§22.4 接口契约 ===" && grep -c "^### 22\." "$ARCH"
echo "=== §23 参考文献 ===" && grep -c "## 第二十三章 参考文献" "$ARCH"
echo "=== 附录 F 退役 ===" && grep -c "^### F\." "$ARCH"
echo "=== [R25]-[R38] 引用完整性 ===" && grep -c "\[R3[0-9]\]\|\[R25\]\|\[R26\]\|\[R27\]\|\[R28\]\|\[R29\]" "$ARCH"
```

All `echo` after `=== ... ===` should output `1` (or ≥1 for counts), except `附录 F 退役` which should output `0`.

- [ ] **Step 2: Verify §22.1-§22.4 each has required elements**

```bash
ARCH="docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
for section in "ENC Manager" "Parameter Store" "Environment Cross-Source Validator" "BNWAS-equivalent"; do
  echo "--- $section ---"
  grep -A 5 "$section" "$ARCH" | grep -c "CCS 功能映射\|降级行为\|接口契约\|TBD-D4.7"
done
```
Expected: each section has ≥ 3 of the 4 required elements.

- [ ] **Step 3: Verify置信度 标注存在**

```bash
grep -c "🟢\|🟡\|🔴\|⚫" "docs/Design/Architecture Design/MASS_ADAS_L3_TDL_架构设计报告.md"
```
Expected: ≥ 10 confidence markers in the document.

- [ ] **Step 4: CCS 中期意见邀请**

Draft and send CCS 联审邀请函 (email with v1.1.3-stub PDF attachment):

```
收件人：CCS 技术中心（智能船舶审核组）
主题：MASS ADAS L3 战术决策层架构 v1.1.3-stub 中期意见征询
附件：MASS_ADAS_L3_TDL_架构设计报告.md（v1.1.3-stub，2026-07-31）

正文要点：
1. 征询 §16 Cybersecurity Spec 信任域划分思路的可接受性意见
2. 征询 §19 maritime-schema 作为 evidence container 的 CCS 接受度
3. 征询 §22 延期支持模块接口位设计的合理性
4. 请求 CCS 中期意见会议时间（建议 2026-08 月内）
```

保存邮件截图至 `docs/Design/Phase 2/D2.8-arch-v1.1.3-stub/evidence/ccs-review-invite-YYYYMMDD.png`

- [ ] **Step 5: DNV 验证官联审邀请**

Draft and send DNV 联审邀请（含 maritime-schema acceptance 反馈请求）:

```
收件人：DNV 验证官（maritime autonomy team）
主题：MASS ADAS L3 - maritime-schema acceptance confirmation request + v1.1.3-stub review
内容：
1. 请求 DNV 对 maritime-schema TrafficSituation 作为 evidence container 的 CCS 可接受性意见
2. [DNV-FEEDBACK-HOOK] §17.A / §19.A 两处 hook 说明
3. 邀请 DNV 验证官参与 v1.1.3-stub 联审
```

保存邮件截图至 `docs/Design/Phase 2/D2.8-arch-v1.1.3-stub/evidence/dnv-review-invite-YYYYMMDD.png`

- [ ] **Step 6: Final commit**

```bash
git add "docs/Design/Phase 2/D2.8-arch-v1.1.3-stub/evidence/"
git commit -m "$(cat <<'EOF'
arch(D2.8): v1.1.3-pre-stub → v1.1.3-stub

- Migrate Appendix F §F.1-F.9 to main chapters §17-§21
- Add §16 Cybersecurity Spec (stub, D3.9 pending)
- Add §22 Deferred Support Modules (§22.1-§22.4 interface contracts)
- Update §3.4/§10.1/§10.5/§11/§12.3/§12.5/§13/§15.0/§15.2-IDL
- Retire Appendix F (content migrated)
- Renumber §16 References → §23; update TOC §16-§23
- Issue CCS/DNV review invitations (evidence in D2.8/evidence/)

Closes: SIL P0 SIL-1~7, F P0-F-01/F-02/F-04 stub, F P1-F-03,
        D P1-D-04/D-08, B P1-B-06, C P1-C-3/C-5 partial,
        MV-7~11 stub scope

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 7: Update TDL-Kernel M-module progress files**

For each module affected by D2.8:

```bash
# M1: §11.10 仲裁优先级 + §15.0 时基
grep -l "D2.8\|Currently Implementing" docs/Design/TDL-Kernel/M1-*/M1-progress.md 2>/dev/null

# M5: §10.1 算法选型 + §10.6 4-DOF
# M8: §12.3.1 + §12.6
```

Add `Closed in: D2.8` entries to the D-task linkage tables in:
- `docs/Design/TDL-Kernel/M1-odd-envelope-manager/M1-progress.md`
- `docs/Design/TDL-Kernel/M5-tactical-planner/M5-progress.md`
- `docs/Design/TDL-Kernel/M7-safety-supervisor/M7-progress.md`
- `docs/Design/TDL-Kernel/M8-hmi-transparency-bridge/M8-progress.md`

Then:
```bash
git add docs/Design/TDL-Kernel/
git commit -m "arch(D2.8/C3): update M1/M5/M7/M8 progress.md - Closed in D2.8"
```

---

## 执行顺序约束

```
Task 1 (branch setup)
  ↓
A1-A9（任意顺序，独立；A3 须在 A2 之后以获得正确节号）
  ↓ commit A group
B1 → B2 → B3 → B4 → B5 → B6 → B7 → B8（顺序执行，均插入 §16 References 锚点前）
  ↓ commit B group
C1（Appendix F 退役）→ C2（版本提升）→ C3（DoD + 邀请）
```

**时间约束**（任务内标注）：
- A5（§12.3.1）/ A6（§12.6）：stub 先写；D2.6 完成（7/13）后内容 patch
- B5（§20 权重）：`[TBD-D1.7+D2.4]` 占位；D2.4 完成（7/15 后）patch §20.2
- B2（§17 latency）：估算值先写；D2.5 完成（7/20 估）后 patch §17.2

---

## 修订

| 日期 | 变更 |
|---|---|
| 2026-05-22 | 初版（writing-plans 产出；关键修正：§11.10/§12.6/§13.6 替代 spec 中 §11.9/§12.5/§13.x；完整 21 任务，含所有 Appendix F 迁入 + §22.1-§22.4 全接口契约）|
