# 核心提示词 — M5 NLP v2.2 完整验证 + 测试 + push

## 任务

**V2 rule14-ho probe + V3 OrbStack gate + push**。v2.2 根治实施已落（20 commits），待端到端 probe 验证后 push。

预期产出：probe evidence（NLP CONVERGED % + CPA min）+ OrbStack gate evidence + push to GitHub main + GitLab l3-tdl。

## 严禁

1. 不调参（w_colreg/w_dist/w_vel/k_asym/scenario 几何/scorer gate/任何阈值）
2. 不补丁让 probe 绿（不加 mock/skip/forced-pass/vessel-specific 分支）
3. 不归咎单一模块（COLREGs 全链路视角）
4. 不在 V2+V3 gate pass 前 push
5. 若 probe RED，先 trace 全链路定位 stage，不 tune

## 仓库与分支

- 仓库：`/Users/marine/Code/MASS-L3-Tactical Layer`
- **worktree**：`/Users/marine/Code/MASS-L3-Tactical Layer/.worktrees/colregs-12probe-debug`
- **branch**：`codex/colregs-12probe-debug`，HEAD `77ccac0f`（20 commits on top of `55f507b6` plan）
- 容器：`codex-gnc-validation-sil-nodes-1`（M5 stack）+ `codex-gnc-validation-gnc-gnc-nodes-1`（GNC stack）
- mempalace wing：`MASS-L3`（会话开头 `mempalace wake-up --wing MASS-L3`）

## 现状（必读：本次验证的事实基础）

### v2.2 根治 20 commits 全落（不 push，待验证）

spec v2.2 + plan + 4 D-task + Codex 整体 fix：

**α (decel_max + speed contract)**：
- GNC `ship_config.yaml` + `fast10` + `docker/gnc-ship-config-overlay.yaml` decel_max 0.08→0.20
- MidMpcInput.speed_gap_infeasible flag + compute_speed_gap_infeasible()
- assemble_input_ wiring + boundary tests
- gnc_avoidance_preflight.hpp default + mid_mpc_node fallback ODD 同步 0.20

**β (M4↔M5 reachability 合约，direction-aware)**：
- BehaviorPlan.msg schema 112→113（4 numeric + 1 rationale）
- ConstraintInputs 扩合约字段
- assemble_input_ 解析
- RowBoundConfig.minalt_box_infeasible + derive over {ROT ∩ box}（bimodal：box<min_alt → flag+全soft；box≥min_alt → k_minalt_rot；sentinel 0 → v2.1 fallback）
- M4 publish（compute_heading_box_reachability direction-aware：Starboard→d_max, Port→|d_min|；earliest_min_alt_k 公式；epsilon tolerance 0.005rad）

**γ (BC-MPC Phase E2 wiring + KeepLast policy)**：
- LifecycleState BcMpcFollow（8→9 态）
- BC-MPC 真读 consecutive_failures（MidMpcNode publish `/l3/m5/mid_mpc/consecutive_failures` std_msgs/UInt64 reliable QoS；BC-MPC subscribe + atomic）
- dispatch BC-MPC take-over：`consecutive>=3 OR minalt_box_infeasible OR speed_gap_infeasible`（compute_bc_mpc_take_over free fn，testable）
- committed_route KeepLast policy 修订：takeover→BcMpcFollow；否则 DegradedHold + MRM log critical
- **BcMpcFollow publish semantics**：publish_keep_last_ 入口 guard，emit empty BcMpcFollow-status heartbeat + return early（不 republish stale corridor）

**δ (tail-gate 文档化)**：types.hpp:770 注释 + 架构 §10.4 措辞 + spec v2 §13.4 指针。tail-gate = NLP publish gate（非 SIL2 checker），SIL2 责任归 M7 X-axis

### 测试状态

- m5: **449/449 PASS**（含 ~40 新 v2.2 测）
- m4: 155/155（**3 pre-existing D-3 speed-box failures**，与 v2.2 正交，handoff 有 stash 验证证据）
- 4 Codex 评审 gate 全通过（α task-mr6bj23a + β task-mr6d2jyi + γ ZCode sonnet + 整体 task-mr6eqycu）

### 已经验证 OK 的部分（避免重新怀疑）

- v2.1 reachable schedules（min_alt/CPA/terminal/direction）正确激活
- ROT 4.7°/s 端到端到达 NLP（`/gnc/execution_odd` cruise_max_yaw_rate_deg_s=4.7）
- v2.1 28/28 ctest 在 v2.2 代码下仍 PASS（sentinel=0 fallback 保 v2.1 行为）
- M4↔M5 合约 direction-aware 正确（starboard [23,53] own=0 min_alt=30 → reach=53° not infeasible）
- BcMpcFollow 不再 republish stale corridor

## 验证流程

### 1. 会话开头

```
mempalace wake-up --wing MASS-L3
```

### 2. Phase 1：GNC rebuild（decel_max 0.20 sync 到 GNC 容器）

**关键**：GNC 容器是独立 `/opt/gnc_ws`（非 worktree mount）。decel_max 0.20 改动在 worktree 的 `third_party/gnc_ws/...`，需 sync 到容器 + rebuild。

```bash
# 确认 worktree 改动
grep max_decel_mps2 third_party/gnc_ws/src/platform/ship_bringup/config/ship_config.yaml
# 应为 0.20

# sync 到 GNC 容器（narrow sync，不 --delete）
docker cp third_party/gnc_ws/src/platform/ship_bringup/config/ship_config.yaml \
         codex-gnc-validation-gnc-gnc-nodes-1:/opt/gnc_ws/src/platform/ship_bringup/config/ship_config.yaml
docker cp third_party/gnc_ws/src/platform/ship_bringup/config/ship_config_fast10.yaml \
         codex-gnc-validation-gnc-gnc-nodes-1:/opt/gnc_ws/src/platform/ship_bringup/config/ship_config_fast10.yaml

# GNC rebuild（per handoff runbook：cmake-clean-cache + touch cpp 强制重编，ccache 命中旧 obj）
docker exec codex-gnc-validation-gnc-gnc-nodes-1 bash -c \
  "source /opt/ros/humble/setup.bash && cd /opt/gnc_ws && \
   colcon build --packages-select ship_guidance --cmake-clean-cache && \
   touch src/gnc/ship_guidance/src/active_route_manager_node.cpp && \
   colcon build --packages-select ship_guidance"

# 重启 GNC 容器让新 ODD 生效
docker restart codex-gnc-validation-gnc-gnc-nodes-1

# 验证 decel_max 真到 runtime
ros2 topic echo /gnc/execution_odd --once | grep -E "max_decel|cruise_max_yaw"
# 应为 max_decel_mps2: 0.20, cruise_max_yaw_rate_deg_s: 4.7
```

若 GNC rebuild 失败，查 handoff runbook（`scripts/gnc-profile-start.sh` + ccache 处理）。

### 3. Phase 2：M5 rebuild（确保 v2.2 binary 在容器）

```bash
docker exec codex-gnc-validation-sil-nodes-1 bash -c \
  "source /opt/ros/humble/setup.bash && source /opt/ws/install/setup.bash && \
   cd /opt/ws && colcon build --packages-select m5_tactical_planner m4_behavior_arbiter l3_msgs"
```

### 4. Phase 3：V2 rule14-ho probe（spec §7.2 通过条件）

```bash
cd "/Users/marine/Code/MASS-L3-Tactical Layer/.worktrees/colregs-12probe-debug"
mkdir -p runs/v2.2_root_cause_rule14ho

# rule14-ho probe（sim-rate 5, restart between runs）
./scripts/colregs-cli probe --scenario rule14-ho --sim-rate 5 --restart-between-runs \
  | tee runs/v2.2_root_cause_rule14ho/probe_$(date +%Y%m%d_%H%M%S).json
```

**通过条件**（spec §7.2）：
- NLP SOLVER_CONVERGED 占比 > 30%（v2.1 当前 0%）
- CPA min ≥ 180m（v2.1 当前 2.4m）

**若 probe RED**：
1. 不 tune。先 trace 全链路：L2 route/speed → M2 CPA → M4 heading box + reachability 合约 → M5 NLP derive + dispatch → L4 → M7 → M8
2. 查 M5 NLP status 分布（EMPTY/VALID/CONVERGED/INFEASIBLE）
3. 查 minalt_box_infeasible / speed_gap_infeasible flag 是否触发
4. 查 BcMpcFollow lifecycle state 是否激活
5. 查 M4 BehaviorPlan reachability 合约字段是否非 0（M4 publish 正确）
6. 分类 stage contract broke，再决定修哪里

### 5. Phase 4：V3 OrbStack gate

```bash
cd "/Users/marine/Code/MASS-L3-Tactical Layer/.worktrees/colregs-12probe-debug"
source scripts/local-a4000-env.sh
./scripts/local-a4000-acceptance.sh
# evidence 写 runs/local_a4000_container_probe_*.json + runs/local_runtime_probe_*.json
```

**若 local-a4000-acceptance.sh 报 stale project**：
```bash
RECLAIM_STALE_LOCAL_PROJECT=1 ./scripts/local-a4000-acceptance.sh
```

### 6. Phase 5：Codex 验证评审 gate（per AGENTS.md「兼听则明」）

V2+V3 通过后，push 前：

```
/codex:review
```
或 dispatch `Agent(subagent_type: "codex:codex-rescue")` with `--model gpt-5.5 --effort high`，针对 probe + gate evidence + 全 20 commits 做 final verification review。

- 🔴 级问题：回到实现循环
- 🟡/informational：记 mempalace 后放行

### 7. Phase 6：push（gate 全 pass 后）

```bash
cd "/Users/marine/Code/MASS-L3-Tactical Layer/.worktrees/colregs-12probe-debug"

# 确认全测通过
docker exec codex-gnc-validation-sil-nodes-1 bash -c \
  "source /opt/ros/humble/setup.bash && source /opt/ws/install/setup.bash && \
   cd /opt/ws && colcon test --packages-select m5_tactical_planner m4_behavior_arbiter"

# integration branch（从 worktree branch fast-forward main）
cd "/Users/marine/Code/MASS-L3-Tactical Layer"
git checkout main
git merge --ff-only codex/colregs-12probe-debug

# push GitHub main + GitLab l3-tdl
git push origin main
git push gitlab l3-tdl
```

## 关键文件 + 坐标（验证时按需查阅）

- spec v2.2：`docs/superpowers/specs/2026-07-04-m5-nlp-constraint-restructure-design-v2.1.md`（§4.2/§4.6/§4.7/§13.1/§13.2/§13.4 是 v2.2 核心）
- plan：`docs/superpowers/plans/2026-07-04-m5-nlp-v2.2-root-cause-fix.md`
- handoff：`handoff/workspace_log.md` 末 4 段（v2.2 决策 + α/β/γ/δ 实施 + Codex 评审 + 整体 fix）
- mempalace drawers（`mempalace search "<keywords>"`）：
  - `m5-nlp-architecture-decisions`（4 用户决策）
  - `m5-nlp-v2.2-impl-alpha-beta`（α+β 实施 + Codex 评审）
  - `m5-nlp-v2.2-integration-review`（整体评审 + fix）
- NLP 约束实现：`src/l3_tdl_kernel/m5_tactical_planner/src/mid_mpc/mid_mpc_nlp_formulation.cpp:410-602`
- NLP solve + derive：`src/l3_tdl_kernel/m5_tactical_planner/src/mid_mpc/mid_mpc_solver.cpp`（solve :106-284, derive :286-406, last_minalt_box_infeasible_ 新加）
- dispatch BC-MPC take-over：`src/l3_tdl_kernel/m5_tactical_planner/src/mid_mpc/mid_mpc_node.cpp:~847`（compute_bc_mpc_take_over OR 3 conditions）
- BcMpcFollow publish guard：`src/l3_tdl_kernel/m5_tactical_planner/src/mid_mpc/mid_mpc_node.cpp` publish_keep_last_（grep BcMpcFollow）
- M4 reachability 合约：`src/l3_tdl_kernel/m4_behavior_arbiter/src/colregs_directive.cpp` compute_heading_box_reachability（direction-aware）
- tail-gate 6 check：`src/l3_tdl_kernel/m5_tactical_planner/include/m5_tactical_planner/common/types.hpp:930-1006`
- GNC decel 源：`third_party/gnc_ws/src/platform/ship_bringup/config/ship_config.yaml:640`（0.20）+ `docker/gnc-ship-config-overlay.yaml:640`（0.20）+ `ship_config_fast10.yaml`（0.20）

## 评估流程建议

1. **会话开头**：`mempalace wake-up --wing MASS-L3` + 读 handoff 末段 + 读本提示词「现状」段
2. **Phase 1 GNC rebuild**：sync decel_max + rebuild + 验证 runtime topic
3. **Phase 2 M5 rebuild**：确保 v2.2 binary 在容器
4. **Phase 3 V2 probe**：rule14-ho，spec §7.2 通过条件
5. **Phase 4 V3 OrbStack gate**：local-a4000-acceptance.sh
6. **Phase 5 Codex 验证评审**：probe + gate evidence final review
7. **Phase 6 push**：fast-forward main + push GitHub/GitLab

## 关键约束（严格遵守）

1. **V2+V3 gate pass 前不 push**（AGENTS.md promotion rule）
2. **不调参让 probe 绿**（COLREGs 全链路 debug 规则）
3. **probe RED 时先 trace 全链路**，不归各单一模块
4. **GNC rebuild 用 `--cmake-clean-cache` + touch cpp**（ccache 命中旧 obj，handoff runbook）
5. **local-a4000-acceptance.sh 从 worktree 跑**（若 stale project 用 RECLAIM_STALE_LOCAL_PROJECT=1）
6. **push 前 Codex 验证评审**（兼听则明）
7. **诚实停下**：若 probe 持续 RED 且无法定位，停下报告，不强行 push

## 已知 deferred 项（不阻塞 V2/V3，但 follow-up）

1. **BC-MPC self-activation**：is_bc_active_ 仍 key on CPA validity，未 gating on consecutive_failures（γ2 只 wire 数据管道，activation logic 在 γ3 仅 committed_route 侧）
2. **BcMpcFollow publish 4-WP 翻译**：当前 minimal viable = suppress stale + BC-MPC override drives（架构 §L4 ReactiveOverrideCmd）；full 4-WP translation from ReactiveOverrideCmd 是 follow-up
3. **β ROT fallback 不对称**：M4 hardcode 4.7 vs M5 fallback 1.2（文档化 known limitation，defer 到 behavior_arbiter 订阅 GNC ODD）
4. **m4 pre-existing 3 failures**：D-3 speed-box widening（behavior_arbiter_node.cpp:1303-1319），与 v2.2 正交，需另立 task

## 预期产出

- V2 probe evidence（`runs/v2.2_root_cause_rule14ho/*.json`）：NLP CONVERGED % + CPA min
- V3 OrbStack gate evidence（`runs/local_a4000_container_probe_*.json`）
- Codex 验证评审 output
- push to GitHub main + GitLab l3-tdl（gate 全 pass 后）
- mempalace diary + handoff append

---

**开工**：先 `mempalace wake-up --wing MASS-L3` + 读 handoff 末段 + 读本提示词「现状」段，然后 Phase 1 GNC rebuild。
