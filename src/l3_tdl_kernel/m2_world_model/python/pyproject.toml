[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "m2-enc-loader"
version = "0.1.0"
requires-python = ">=3.10"

[project.scripts]
enc-loader = "enc_loader.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
