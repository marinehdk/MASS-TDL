# M4 审计证据片（来自 2026-06-08 系统审计 @158bba9d；当前代码请用 codegraph 复核）

role: IvP multi-objective behavior arbitration: consumes M1/M2/M3/M6 inputs, selects the dominant behavior from a dictionary, runs a weighted-sum grid-search IvP solver over (heading, speed) domain, and publishes a heading+speed window to M5 at 4 Hz.

## 实际订阅 (subscriptions)
- /l3/m1/odd_state [USED] <l3_msgs/ODDState> src=M1 :: behavior_arbiter_node.cpp:50-52 — sub_odd_ subscribed, on_odd_state stores latest_odd_ and sets odd_received_=true; odd_zone read in build_inputs():130
- /l3/m2/world_state [USED] <l3_msgs/WorldState> src=M2 :: behavior_arbiter_node.cpp:53-55 — sub_world_ subscribed; own_ship.heading_deg, sog_kn, targets, position used in arbitration_timer_callback() for TRANSIT bearing and COLREGs geometry
- /l3/m1/mode_cmd [USED] <l3_msgs/ModeCmd> src=M1 :: behavior_arbiter_node.cpp:56-58 — on_mode_cmd; mode==MODE_EMERGENCY sets mode_mrc_triggered (build_inputs():142)
- /l3/m3/mission_goal [USED] <l3_msgs/MissionGoal> src=M3 :: behavior_arbiter_node.cpp:59-61 — on_mission_goal; fsm_state+task_validity used for m3_active_latch_ (line 159-161); current_target_wp for bearing computation (lines 237-244)
- /l3/m6/colregs_constraint [USED] <l3_msgs/COLREGsConstraint> src=M6 :: behavior_arbiter_node.cpp:62-64 — on_colregs_constraint; conflict_detected and constraints[].numeric_value consumed to build IvP avoid function (lines 301-398) and hard constraints (lines 409-416)
- /l3/m6/rule_assessment [USED] <l3_msgs/RuleAssessment> src=M6 :: behavior_arbiter_node.cpp:65-67 — sub_rule_assessment_ subscribed; on_rule_assessment() boosts COLREG_AVOID weight to 0.85 on Rule 14, else 0.70 (lines 113-123). NOT listed in M4-spec.md interface contract.

## 实际发布 (publications)
- /l3/m4/behavior_plan [REAL] unpop=[] :: behavior_arbiter_node.cpp:69, 539-549, 174-184, 195-205 — pub_plan_ publishes in all code paths; schema_version=113, stamp=now(), confidence and rationale set in every branch
- /sil/sat2_data [PARTIAL_FIELDS] unpop=[ivp_contributions[6] truncates 8 computed directions to 6 (270deg and 315deg dropped),reasoning_latency_ms] :: behavior_arbiter_node.cpp:551-576 — schema_version=113, stamp, confidence, rationale, trigger_reason, reasoning_chain, system_confidence, colregs_chain all set. SAT2Data.msg:17 declares float32[6] but compute_ivp_contributions() computes 8 directions (line 616); loop at line 567 is for i<6 so directions 270deg and 315deg are silently dropped. reasoning_latency_ms (SAT2Data.msg:14) is never set.
- /l3/asdr/record [REAL] unpop=[] :: behavior_arbiter_node.cpp:648-657 — publish_asdr_event() sets schema_version=113, stamp, source_module, decision_type, decision_json; called on behavior_switch, odd_transition, ivp_infeasible, fallback_anchor events
- /l3/safety/concern [PARTIAL_FIELDS] unpop=[] :: behavior_arbiter_node.cpp:487-494 — published only on fallback_anchor latch; concern_type, anchor_hdg, suggested_action, severity, stamp set. Not listed in M4-spec.md interface contract.
- /l3/m4/reactive_override_cmd [MISSING] unpop=[] :: M4-spec.md:26 lists '/l3/m4/reactive_override_cmd' as DEMO-2 P0 NEW publication. No publisher for this topic in src/l3_tdl_kernel/m4_behavior_arbiter/src/ or include/. BehaviorArbiterNode constructor (lines 69-73) creates pub_plan_, pub_sat2_, pub_asdr_, concern_pub_ only.

## 设计职责实现状态 (responsibilities)
- [REAL] Behavior dictionary load from YAML :: behavior_arbiter_node.cpp:82-95 — dictionary_.load(config_dir+'/behavior_definitions.yaml'); fallback minimal TRANSIT rule added if empty // config_dir param defaults to '' so dictionary load is conditional on runtime param being set
- [REAL] IvP multi-objective arbitration (weighted-sum grid search over heading x speed) :: ivp_solver.cpp:166-183 — solve_with_fallback() with cascading relaxation levels 0-3; WeightedSumCombination strategy; heading domain 1deg resolution, speed domain [0,22]kn at 0.5kn per m4_params.yaml:3-9 // Genuine grid search, not a stub. Timeout=50ms per param.
- [PARTIAL] 5-behavior weight table per spec (Transit 0.3, COLREGs_Avoidance 0.7, Restricted_Vis 0.6, DP_Hold 0.8, MRC_Drift 1.0) :: m4_params.yaml:11-26 defines weights correctly. However arbitration_timer_callback() hardcodes TRANSIT weight=1.0 (line 298) and COLREG_AVOID weight=10.0 (line 398) in weighted_fns — YAML weights are not applied to IvP function construction. // DP_HOLD spec weight=0.8 never appears in any IvP function construction path
- [PARTIAL] ODD-aware behavior activation (behavior enabled/disabled per ODD zone) :: behavior_activation.cpp:26-57 — is_transit_applicable checks odd_zone<=2, is_colreg_avoid_applicable checks odd_zone!=2, is_restricted_vis_applicable checks odd_zone==3, is_channel_follow_applicable checks odd_zone==1. world_visibility_nm is always 999.0 (build_inputs():136 comment), so is_restricted_vis_applicable() always returns false at runtime. // Restricted_Visibility branch is permanently dead
- [MISSING] M6 COLREGs primary_preferred_direction drives turn direction selection :: COLREGsConstraint.msg:8 defines primary_preferred_direction (STARBOARD|PORT|REDUCE_SPEED|HOLD). Zero references in behavior_arbiter_node.cpp. IvP avoid function (lines 301-398) hardcodes starboard-only by always treating numeric_value as positive starboard deviation and penalizing all port turns (penalty piece at nominal_hdg-180 to nominal_hdg+colregs_dev). // M6 PORT or REDUCE_SPEED directives silently discarded every cycle
- [REAL] M6 numeric_value used for avoidance magnitude :: behavior_arbiter_node.cpp:304-308 — iterates constraints[], reads c.numeric_value for constraint_type=='colregs' and unit=='deg' to derive colregs_dev; also injected into hard constraint heading_allowed_ranges_deg (lines 409-416). // Magnitude consumed correctly; only direction selection is missing
- [PARTIAL] SAT-2 ivp_contributions 8-direction IvP risk gradient at 4Hz :: behavior_arbiter_node.cpp:566-575 — compute_ivp_contributions returns 8 directions (kDirections[8] at line 616) but SAT2Data.msg:17 is float32[6] and loop is for i<6. Directions 270deg and 315deg always dropped. Published at 4Hz (interval_ms=250, m4_params.yaml:3). // Silent truncation — frontend IvpRiskGradientLayer receives only 6 of 8 directions
- [MISSING] CMM 3-interface: current_state() / rationale() / forecast(delta_t)+uncertainty() :: behavior_arbiter_node.hpp — full class definition shows no ROS2 service server members. No create_service() calls in behavior_arbiter_node.cpp. Architecture ADR-3 requires callable CMM interface per module queryable by M8. // BehaviorPlan.rationale string field is not a substitute for the CMM query interface
- [PARTIAL] M1 ODD as sole authority for behavior switching (ADR-1) :: behavior_activation.cpp uses ODD zone as primary switch for all behavior predicates. However on_rule_assessment():115-122 directly mutates dictionary_ priority weights based on M6 rule string comparison, bypassing M1 ODD as sole context authority. // ADR-1 weakened: M6 rule type modifies behavior weights without M1 sanction
- [MISSING] Publish /l3/m4/reactive_override_cmd (DEMO-2 P0 per spec) :: M4-spec.md:26 lists reactive_override_cmd. No pub_reactive_override_ publisher in BehaviorArbiterNode constructor (lines 69-73). Topic name absent from all src/ and include/ in non-salvage code. // Progress doc claims SAT-2 done but reactive_override_cmd is silently absent
- [REAL] Stale-input confidence reduction (freshness tracking) :: behavior_activation.cpp:7-24 — compute_health_state() uses age_*_ms vs timeout thresholds. IvP confidence reduced to 0.75 on relax_level>0 (line 432). Input timeout ASDR event published at lines 597-601. // Implemented correctly
- [STUB] Restricted_Visibility behavior (ODD zone 3, visibility < 2nm) :: behavior_activation.cpp:42-48 — is_restricted_vis_applicable() exists but world_visibility_nm always 999.0 (build_inputs():136). Condition inputs.world_visibility_nm >= 2.0 always true. Additionally compute_active_set():84-85 maps restricted_vis trigger to DP_HOLD BehaviorType (wrong mapping). // Dead code path; cannot activate at runtime; wrong enum if it did
- [STUB] Channel_Follow / BERTH behavior (ODD zone 1 VTS zone) :: behavior_activation.cpp:51-56 — is_channel_follow_applicable() checks world_in_vts_zone which is always false (never set in build_inputs(); field absent from WorldState schema). compute_active_set():87-88 maps channel_follow to BERTH BehaviorType. No IvP function constructed for BERTH in arbitration_timer_callback(). // Dead code path; no IvP function even if it activated

## 设计-实现偏离 (deltas)
- [HIGH] primary_preferred_direction from M6 ignored — starboard hardcoded for all encounters
    设计: COLREGsConstraint.msg:8 defines primary_preferred_direction as STARBOARD|PORT|REDUCE_SPEED|HOLD. M4 is expected to consume M6 directional guidance for avoidance vector selection.
    实现: behavior_arbiter_node.cpp:301-398 never reads primary_preferred_direction. IvP avoid function always constructs a starboard-biased window: penalty piece covers [nominal_hdg-180, nominal_hdg+colregs_dev) (line 362-367), optimal piece covers starboard window (lines 371-376). PORT and REDUCE_SPEED M6 directives silently dropped.
    证据: COLREGsConstraint.msg:8 — field exists and is populated by M6; grep of behavior_arbiter_node.cpp for 'primary_preferred_direction' returns zero matches
- [HIGH] /l3/m4/reactive_override_cmd publication missing — DEMO-2 P0 item absent
    设计: M4-spec.md:26 lists '/l3/m4/reactive_override_cmd' as DEMO-2 P0 NEW publication downstream to M5.
    实现: No publisher for this topic anywhere in current m4_behavior_arbiter/src/ or include/. BehaviorArbiterNode constructor (lines 69-73) creates only pub_plan_, pub_sat2_, pub_asdr_, concern_pub_.
    证据: behavior_arbiter_node.cpp:69-73; grep of src/ for 'reactive_override' returns zero matches in non-salvage code
- [MEDIUM] IvP weighted_fns use hardcoded weights (1.0 TRANSIT, 10.0 COLREG_AVOID) ignoring YAML behavior_weights
    设计: M4-spec.md behavior dictionary: Transit=0.3, COLREGs_Avoidance=0.7. m4_params.yaml:11-17 configures these weights. IvP combination should respect dictionary priority weights.
    实现: behavior_arbiter_node.cpp:298 — weighted_fns.push_back({1.0, transit_fn}); line 398 — weighted_fns.push_back({10.0, avoid_fn}). Magic numbers bypass YAML-configured weights. dictionary_.set_priority_weight() from on_rule_assessment() mutates dictionary but arbitration_timer_callback() never queries dictionary weights when building weighted_fns.
    证据: behavior_arbiter_node.cpp:298, 398; m4_params.yaml:11-17; on_rule_assessment():117,121 mutates dictionary_ but weighted_fns construction never reads it
- [MEDIUM] Restricted_Visibility behavior permanently disabled — world_visibility_nm removed from WorldState
    设计: M4-spec.md behavior dictionary: Restricted_Vis weight=0.6, activated when visibility < 2nm in ODD zone 3.
    实现: build_inputs():136 — comment states 'visibility_nm removed from WorldState (v1.1.2); keep default 999.0'. ArbitrationInputs::world_visibility_nm always 999.0. is_restricted_vis_applicable():46 checks world_visibility_nm >= 2.0 — always true, behavior never activates.
    证据: behavior_arbiter_node.cpp:136; behavior_activation.cpp:42-49
- [MEDIUM] compute_active_set maps Restricted_Visibility trigger to DP_HOLD BehaviorType (wrong enum)
    设计: Spec lists Restricted_Visibility as a distinct behavior with weight 0.6 and different IvP semantics from DP_Hold (weight 0.8).
    实现: behavior_activation.cpp:84-85 — if (is_restricted_vis_applicable(inputs)) { active.push_back(BehaviorType::DP_HOLD); }. The restricted visibility trigger activates DP_HOLD enum value.
    证据: behavior_activation.cpp:84-85
- [MEDIUM] CMM 3-interface (current_state/rationale/forecast) absent from M4
    设计: Architecture ADR-3: every module implements CMM current_state() / rationale() / forecast(delta_t)+uncertainty(), queryable by M8.
    实现: BehaviorArbiterNode has no ROS2 service servers and no methods named current_state or forecast. No create_service() calls in behavior_arbiter_node.cpp.
    证据: behavior_arbiter_node.hpp — full class definition shows no service server members; behavior_arbiter_node.cpp has no create_service() calls
- [LOW] SAT2Data ivp_contributions silently truncated from 8 computed directions to 6 published slots
    设计: M4-spec.md:26 and M4-progress.md:18 reference 8-direction IvP risk gradient for IvpRiskGradientLayer frontend.
    实现: compute_ivp_contributions() at line 616 defines kDirections[8]={0,45,90,135,180,225,270,315}. SAT2Data.msg:17 declares float32[6]. Loop at line 567 is for i<6 — directions 270deg and 315deg always silently dropped.
    证据: behavior_arbiter_node.cpp:566-574; SAT2Data.msg:17; compute_ivp_contributions():616
- [LOW] M4 directly couples to M6 rule_assessment to mutate behavior weights — bypasses M1 ODD authority (ADR-1 violation)
    设计: ADR-1: M1 ODD state is the sole source for behavior switching. Modules must not independently maintain safety context.
    实现: on_rule_assessment():113-123 — on receiving '/l3/m6/rule_assessment', M4 directly sets colreg_avoidance_weight_ and calls dictionary_.set_priority_weight() based on M6 rule string. Direct M6 to M4 coupling outside M1 mediation.
    证据: behavior_arbiter_node.cpp:113-123; M4-spec.md:19-23 subscription list omits /l3/m6/rule_assessment

## MOCK / STUB
- [MASQ-伪装真实] Restricted_Visibility behavior — dead code, world_visibility_nm hardcoded 999.0 @ src/l3_tdl_kernel/m4_behavior_arbiter/src/behavior_arbiter_node.cpp:136 and behavior_activation.cpp:42-48 // Predicate and activation logic exist but can never fire at runtime. No warning is logged. The comment 'keep default 999.0 (good vis)' silently disables the entire Restricted_Visibility branch.
- [MASQ-伪装真实] Channel_Follow / BERTH behavior — world_in_vts_zone never set, no IvP function built @ src/l3_tdl_kernel/m4_behavior_arbiter/src/behavior_arbiter_node.cpp build_inputs() and behavior_activation.cpp:51-56 // world_in_vts_zone is always false (ArbitrationInputs default). Even if it fired, arbitration_timer_callback() builds no IvP function for BERTH — only TRANSIT and COLREG_AVOID have IvP function construction blocks.
- [MASQ-伪装真实] dictionary_.set_priority_weight() called from on_rule_assessment() but weights never read in IvP function construction @ src/l3_tdl_kernel/m4_behavior_arbiter/src/behavior_arbiter_node.cpp:117,121 vs 298,398 // Code appears to dynamically adjust behavior weights via dictionary, but arbitration_timer_callback() hardcodes IvP weights (1.0, 10.0) and never queries dictionary weights for weighted_fns construction. The weight mutation is a no-op for actual IvP arbitration.
- [stub] IvP infeasible fallback emitting +/-90 degree symmetric window when no COLREGs deviation @ src/l3_tdl_kernel/m4_behavior_arbiter/src/behavior_arbiter_node.cpp:513-516 // Correctly labelled 'IvP infeasible fallback' with confidence=0.30. Not masquerading as a real solution.

## progress.md OVERCLAIM 修正
- 声称: M4-progress.md:13 — D3.1 'Closed in 2026-05-25: M4 BehaviorArbiter IvP complete implementation (813 LOC src + 916 LOC test)'
    实际: IvP solver is real and grid search works. However: (a) primary_preferred_direction from M6 never consumed; (b) YAML behavior weights not applied to IvP weighted_fns (hardcoded 1.0 and 10.0); (c) Restricted_Visibility behavior permanently dead; (d) only TRANSIT and COLREG_AVOID have IvP function construction — DP_HOLD, BERTH, MRC_DRIFT have none. 'Complete' overclaims actual coverage.
    证据: behavior_arbiter_node.cpp:298 (weight=1.0 hardcoded), 398 (weight=10.0 hardcoded); behavior_activation.cpp:84-85 (restricted_vis to DP_HOLD); build_inputs():136
- 声称: M4-progress.md:14 — D2.5 'M4 side has published /sil/sat2_data.ivp_contributions[] @4Hz'
    实际: Publication exists and fires at 4Hz. However ivp_contributions is float32[6] in SAT2Data.msg but 8 directions are computed; only 6 are written (loop i<6 at line 567). Two directions (270deg, 315deg) silently dropped every cycle.
    证据: SAT2Data.msg:17 (float32[6]); behavior_arbiter_node.cpp:566-574 (loop i<6, kDirections[8] at line 616)
- 声称: M4-spec.md:53 — '/sil/sat2_data.ivp_contributions[] publication' listed as 'not done' (red) in spec status table while M4-progress.md:14,18 marks it done and 'DEMO-2 block removed'
    实际: The spec's own status table (line 53) was never updated after D3.1 merge and still shows the item as incomplete, contradicting the progress doc which marks it resolved.
    证据: M4-spec.md:53 vs M4-progress.md:14,18

## 疑似断流 (flow_gaps_suspected)
- M6 primary_preferred_direction to M4 avoidance direction selection | M4 always builds a starboard avoidance window regardless of what M6 specifies in primary_preferred_direction. A PORT-required scenario (e.g. Rule 13 overtaking from astern) would receive a starboard bias from M4, directly contradicting the M6 COLREGs directive. :: COLREGsConstraint.msg:8 defines primary_preferred_direction; behavior_arbiter_node.cpp has zero reads of this field. IvP avoid pieces at lines 360-395 always use nominal_hdg+colregs_dev (positive = starboard).
- M7 safety supervisor VETO to M4 behavior plan gate | M4 has no subscription to any M7 veto or safety state topic. No mechanism to halt or override pub_plan_ output if M7 raises a VETO. concern_pub_ emits SafetyConcernEvent to /l3/safety/concern but M4 does not consume any safety veto back. This continues the D3 gap from the prior audit. :: behavior_arbiter_node.hpp — no subscription to /l3/m7/* or /l3/safety/*; behavior_arbiter_node.cpp — no safety gate check before pub_plan_->publish()
- /l3/m6/rule_assessment to M4 (undeclared upstream dependency) | M4 subscribes to /l3/m6/rule_assessment which is not declared in M4-spec.md interface contract. Any M6 schema change to RuleAssessment.msg could silently break M4's weight adjustment logic without spec-level traceability. M6 does publish it (colregs_reasoner_node.cpp:346-347,609) so the wire is live, but the coupling is architecturally undocumented. :: behavior_arbiter_node.cpp:65-67; M4-spec.md:19-23 subscription list omits /l3/m6/rule_assessment; colregs_reasoner_node.cpp:346-347 confirms M6 publishes it

## 对抗验证裁决 (本模块)
- [HIGH] primary_preferred_direction from M6 ignored — starboard hardcoded for all encounters => CONFIRMED(HIGH): The current behavior_arbiter_node.cpp (non-salvage, non-archive) contains zero reads of `latest_colregs_->primary_preferred_direction`. The IvP avoid function at lines 301-398 unconditionally constructs a starboard-biased window: the penalty piece covers [nominal_hdg-180, nominal_hdg+colregs_dev) (p
- [HIGH] /l3/m4/reactive_override_cmd publication missing — DEMO-2 P0 item absent => CONFIRMED(HIGH): Current non-salvage files (src/l3_tdl_kernel/m4_behavior_arbiter/src/behavior_arbiter_node.cpp and include/m4_behavior_arbiter/behavior_arbiter_node.hpp) show exactly four publishers created in the constructor: pub_plan_ (/l3/m4/behavior_plan), pub_sat2_ (/sil/sat2_data), pub_asdr_ (/l3/asdr/record)
- [HIGH] MOCK masquerading: Restricted_Visibility behavior — dead code, world_visibility_nm hardcoded 999.0 => CONFIRMED(HIGH): The current (non-salvage) code confirms the claim exactly at both cited locations.

1. `/src/l3_tdl_kernel/m4_behavior_arbiter/include/m4_behavior_arbiter/behavior_activation.hpp:25` — `ArbitrationInputs` declares `world_visibility_nm{999.0}` as default.

2. `/src/l3_tdl_kernel/m4_behavior_arbiter/s
- [HIGH] MOCK masquerading: Channel_Follow / BERTH behavior — world_in_vts_zone never set, no IvP function built => CONFIRMED(HIGH): Both sub-claims are confirmed by current (non-salvage) code:

1. **`world_in_vts_zone` never set**: In `src/l3_tdl_kernel/m4_behavior_arbiter/src/behavior_arbiter_node.cpp`, `build_inputs()` (lines 125–156) populates only `odd_zone/received/age`, `world_received/own_speed_kn/age_world_ms`, `mode_rec
- [HIGH] MOCK masquerading: dictionary_.set_priority_weight() called from on_rule_assessment() but weights never read in IvP function construction => CONFIRMED(HIGH): Current code at the cited lines clearly shows the gap. In `on_rule_assessment` (lines 113-123), `dictionary_.set_priority_weight(BehaviorType::COLREG_AVOID, 0.85/0.70)` mutates the stored `priority_weight` field in the `BehaviorDictionary`. In `arbitration_timer_callback`, the IvP `weighted_fns` are

## 相关 flow 边裁决
- [MEDIUM] M2 -> M4: /l3/m2/world_state = PARTIAL: Functional data flows correctly for transit and geometry. The PARTIAL is because schema_version (CMM contract) and per-target rationale fields are populated as 0/empty by M2 and never checked by M4; and intent_distribution[] absent from WorldState.msg means M4 cannot read multi-modal intent.
- [HIGH] M1 -> M4: /l3/m1/mode_cmd (HARD gate or soft read?) = PARTIAL: Soft read for one value (EMERGENCY→MRC). MODE_LIMITED / MODE_DEGRADED produce zero behavioral change in arbitration_timer_callback.
- [MEDIUM] M1 -> M4: /l3/m1/odd_state (HARD gate or soft read?) = PARTIAL: Soft read. Presence of odd_state gates standby vs active; actual zone value is not exploited for ODD-aware behavior parametrisation in M4.
- [MEDIUM] M6 -> M4: /l3/m6/colregs_constraint (consumed as HARD constraint?) = PARTIAL: PARTIAL: constraint is read and actively used to shape IvP solution space. But IvP solver falls back to geometric heading on infeasibility (line 475-517), meaning constraints can be violated in fallback path without hard stop.

## completeness-critic 相关补充
- [HIGH/FLOW_GAP] fsm_aggregator_node: /l3/m3/mission_state and /l3/m8/operator_state not subscribed :: docker/fsm_aggregator_node.py:116-123 — FsmAggregatorNode subscribes only to /l3/m1/odd_state, /l3/m4/behavior_plan, /l3/m5/avoidance_plan, /l3/m7/safety_alert. It does NOT subscribe to /l3/m3/mission
- [MEDIUM/MOCK] fsm_aggregator_node: active_rule hardcoded 'Rule 14 head-on' for ALL COLREG_AVOIDANCE encounters :: docker/fsm_aggregator_node.py:184-188 — When M4 behavior_plan.behavior == BEHAVIOR_COLREG_AVOID the aggregator sets active_rule = 'Rule 14 head-on' unconditionally, regardless of which actual rule M6 
- [MEDIUM/LEAKED_LOGIC] shell_b_harness: duplicate autopilot implementation diverges from bridge autopilot :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:74-107, 539-593 — ShellBSimulator contains its own HeadingController and SpeedController identical in structure to those in docker/sil_to
- [HIGH/FLOW_GAP] ScoringNode: /l3/colregs_active subscription has no publisher anywhere in the codebase :: src/sim_workbench/sil_nodes/scoring/scoring/node.py:72 — ScoringLifecycleNode.on_configure() subscribes to std_msgs/String on '/l3/colregs_active'. The _colregs_cb() at line 132 parses 'Rule14:ok,Rule
- [MEDIUM/STUB] ScoringNode: t_action_s hardcoded 0.0 and behavior_phase hardcoded 'transit' — evasion timing score always zero :: src/sim_workbench/sil_nodes/scoring/scoring/node.py:151-156 — _score_and_publish() calls HagenScorer.score_frame() with t_action_s=0.0 (time of avoidance action, should be derived from M4 COLREG_AVOID
- [HIGH/DESIGN_IMPL_DESYNC] shell_b_harness doer_composition: M3 and M7 not included — scoring and safety gate both absent from RL loop :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:368-383 — doer_cmd includes m1_odd_manager, m2_world_model, m4_behavior_arbiter, m5_tactical_planner, m8_hmi_bridge. M3 (mission_manager_