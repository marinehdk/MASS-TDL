# M6 审计证据片（来自 2026-06-08 系统审计 @158bba9d；当前代码请用 codegraph 复核）

role: Rule reasoning (ODD-aware), encounter classification, 5-layer decision chain, COLREGsConstraint publication for M4/M5 consumption. Runs at 2 Hz (default 500 ms period); spec states 1 Hz.

## 实际订阅 (subscriptions)
- /l3/m1/odd_state [USED] <l3_msgs/ODDState> src=M1 ODD Envelope Manager :: colregs_reasoner_node.cpp:356-366 — stores OddDomain and timestamp; used to gate Rule19 and select ODD-specific RuleParameters
- /l3/m2/world_state [USED] <l3_msgs/WorldState> src=M2 World Model :: colregs_reasoner_node.cpp:362-366 — stored as last_world_state_; converted via convert_world_state() each reasoning cycle

## 实际发布 (publications)
- /l3/m6/colregs_constraint [PARTIAL_FIELDS] unpop=[schema_version] :: colregs_reasoner_node.cpp:569-576 sets stamp, phase, confidence, rationale, colregs_chain — but schema_version (declared as 114 in COLREGsConstraint.msg) is never assigned anywhere in constraint_generator.cpp or the node; defaults to 0 on every published message
- /l3/m6/rule_assessment [PARTIAL_FIELDS] unpop=[only fires for Rule14 latch — Rule13/15 latched encounters never emit assessment] :: colregs_reasoner_node.cpp:594-609 — only kKey = mmsi<<8|14ULL checked; Rule13 and Rule15 latches exist but no assessment published for those rule IDs
- /l3/sat/data [PARTIAL_FIELDS] unpop=[schema_version,sat2.reasoning_chain (hardcoded empty string),sat2.colregs_chain (never populated),sat2.colregs_chain_target_id,sat2.reasoning_latency_ms,sat3.tmr_s (hardcoded 60.0 not ODD-aware)] :: colregs_reasoner_node.cpp:710-729: msg.sat2.reasoning_chain=''; sat2.colregs_chain array never assigned; sat3.predicted_state='nominal' hardcoded; schema_version never set
- /l3/asdr/record [REAL] unpop=[] :: colregs_reasoner_node.cpp:736-744 — stamp, source_module, decision_type, decision_json all populated; signature intentionally omitted with comment 'not computed in Phase E1'

## 设计职责实现状态 (responsibilities)
- [REAL] ROS2 node + topic (basic pub/sub lifecycle) :: colregs_reasoner_node.cpp:210-223 — constructor initializes all components, publishers, subscribers, timers // Node runs at 2 Hz (reasoning_period_ms=500), not 1 Hz as stated in spec
- [PARTIAL] Rule 5-19 reasoning engine :: Rules 5,6,7,8,13,14,15,16,17,18,19 all have evaluate() implementations. Rule16_GiveWay fires on cpa<1.5*safe regardless of whether Rule13/14/15 determined role (rule16_give_way.cpp:26). Rules 13,14,15,16,17,18 all mark (void)odd — encounter geometry sectors do not adapt to ODD zone. Rule5 always returns is_active=true with role=FREE. // Rule6 and Rule19 are the only rules genuinely ODD-zone-gated. All others use ODD-aware params thresholds only.
- [PARTIAL] 5-layer decision chain internal generation :: build_colregs_chain() at colregs_reasoner_node.cpp:842-966 builds all 5 layers. Chain is populated into COLREGsConstraint.colregs_chain (line 573). When no active encounter, chain is empty — no baseline 'no-conflict' chain emitted. // SAT2Data.colregs_chain on /l3/sat/data is NEVER populated — publish_sat_data() lines 717-719 only set trigger_reason, reasoning_chain='', system_confidence.
- [STUB] SAT-2 colregs_chain[5] serialization output (DEMO-2 P0) :: colregs_reasoner_node.cpp:717-719: sat2.reasoning_chain=''; sat2.colregs_chain never assigned. SAT2Data.msg lines 12-13 declare the fields. The chain IS computed in build_colregs_chain but not copied to sat2.colregs_chain on the SATData publish path. // Spec and progress.md acknowledge this as D2.4 P0 gap. The constraint path (line 573) does fill colregs_chain on COLREGsConstraint.
- [PARTIAL] ODD-aware rule set selection (ADR-1) :: load_odd_thresholds() at line 248 loads per-ODD params (t_standOn_s, t_act_s, t_emergency_s, cpa_safe_m). These flow into evaluate() via RuleParameters. But Rule13/14/15/16/17/18 all mark (void)odd — encounter geometry boundaries do not adjust per ODD zone. // Threshold-parameter ODD-awareness is real; geometry-sector ODD-awareness is absent across 6 of 8 rules.
- [REAL] RuleLatch onset hysteresis (Rule 13(d) — D6 fix) :: colregs_reasoner_node.cpp:526-562 — RuleLatch applied to rid==14 and rid==15. rule_latch.hpp:18-28: releases when opening && (past_and_clear || cpa > release_cpa). past_and_clear = |rel_brg| > 112.5 degrees. Commit 158bba9d verified in source. // Rule16 (rid==16) is NOT latched — its standalone CPA-threshold evaluation can chatter while Rule14/15 latch is held.
- [REAL] conflict_detected derivation (D5 fix — role-derived, not rule-id filter) :: colregs_constraint_generator.cpp:30-35: requires_action() checks role==GIVE_WAY||BOTH_GIVE_WAY||(STAND_ON && critical_phase) — purely role-derived, no {7,8,14} ID filter. commit d8b0c608 fix verified. // Rule5 (always active, role=FREE) and Rule8 (active, role=FREE) correctly do not contribute to conflict_detected.
- [REAL] primary_preferred_direction + numeric_value constraints for M4/M5 :: colregs_constraint_generator.cpp:98-106: Constraint emitted for each active eval where preferred_direction != HOLD; c.numeric_value = eval.min_alteration_deg; c.unit='deg'. M4 consumes colregs_dev at behavior_arbiter_node.cpp:302-371. M5 uses active_rules presence at mid_mpc_node.cpp:173-183. // M5 does NOT read primary_preferred_direction or numeric_value — it only uses active_rules non-empty as a flag and reduces target CPA/TCPA by 80%. Heading direction from M6 never enters M5 MPC cost term.
- [PARTIAL] CMM three-interface: current_state / rationale / forecast :: current_state approximated via sat1.state_summary string (line 714); rationale carried in constraint.rationale (line 111). sat3.predicted_state='nominal' hardcoded (line 723), sat3.prediction_uncertainty=0.5 hardcoded (line 724) — no actual trajectory forecast computed. // sat3 forecast is a placeholder — always 'nominal' regardless of encounter urgency or active phase.

## 设计-实现偏离 (deltas)
- [HIGH] SAT2 colregs_chain never populated — DEMO-2 P0 gap
    设计: M6-spec.md line 25: 'DEMO-2 P0 NEW: /sil/sat2_data.colregs_chain[5] 序列化'; SAT2Data.msg lines 12-13 declares fields
    实现: publish_sat_data() lines 700-729 only sets trigger_reason, reasoning_chain='', system_confidence — colregs_chain array is left empty on every SATData publish
    证据: colregs_reasoner_node.cpp:717-719 — no sat2.colregs_chain assignment; build_colregs_chain() result wired only to COLREGsConstraint (line 573)
- [MEDIUM] schema_version never set on /l3/m6/colregs_constraint
    设计: COLREGsConstraint.msg declares schema_version=114 as mandatory versioning field (per v1.1.3-stub §15.1)
    实现: constraint_generator.cpp and colregs_reasoner_node.cpp never assign constraint.schema_version; every published message carries schema_version=0
    证据: colregs_constraint_generator.cpp:43 — msg is default-constructed, schema_version never assigned; colregs_reasoner_node.cpp:569-576 — no schema_version assignment before publish
- [MEDIUM] Rule16 standalone fires without latch — chatters during latched Rule14/15 encounters
    设计: Rule 16 action obligation derives from Rule13/14/15 role assignment; once encounter is latched per Rule13(d), classification should be stable
    实现: Rule16_GiveWay.evaluate() (rule16_give_way.cpp:26) fires independently on cpa<1.5*cpa_safe regardless of latch state; latch block at colregs_reasoner_node.cpp:530 only covers rid==14 and rid==15
    证据: colregs_reasoner_node.cpp:530: 'if (rid == 14 || rid == 15)'; rule16_give_way.cpp:26: unconditional CPA check
- [MEDIUM] M5 does not consume M6 heading direction — uses only binary active-encounter flag
    设计: M6 produces primary_preferred_direction and numeric_value constraints to inform M5 MPC heading bounds
    实现: mid_mpc_node.cpp:173-183 — M5 only checks active_rules.empty() as a flag, then reduces primary target CPA/TCPA by 80% as cost multiplier; primary_preferred_direction and constraint numeric_value never read by M5
    证据: mid_mpc_node.cpp:173-184 — 3-line M6 consumption block with no direction or alteration field read
- [LOW] M6 runs at 2 Hz, spec states 1 Hz
    设计: M6-spec.md §接口契约: 时间尺度 = 中时 (1 Hz)
    实现: declare_parameter('reasoning_period_ms', 500) yields 2 Hz timer; comment at colregs_reasoner_node.cpp:448 explicitly says '2 Hz'
    证据: colregs_reasoner_node.cpp:231 (default 500 ms), line 448 (comment '2 Hz')
- [LOW] SAT3 forecast always 'nominal' — not encounter-urgency-aware
    设计: CMM ADR-3: forecast(Δt)+uncertainty() must represent actual predicted system state with uncertainty
    实现: publish_sat_data() line 723: sat3.predicted_state='nominal' hardcoded regardless of active encounters, timing phase, or conflict_detected
    证据: colregs_reasoner_node.cpp:723: 'msg.sat3.predicted_state = "nominal";'
- [LOW] Rule13 overtaking heuristic uses relative_speed_kn sign — misclassifies near-equal-speed encounters
    设计: COLREGs Rule 13: overtaking determined by sector geometry and whether ownship is decisively faster than target
    实现: rule13_overtaking.cpp:52 — 'if (kTargetHeadingSameDir && geo.relative_speed_kn > 0.0)' — any positive closing speed triggers GIVE_WAY; near-equal speeds cause classification noise
    证据: rule13_overtaking.cpp:52-74 — binary sign check with no hysteresis threshold

## MOCK / STUB
- [MASQ-伪装真实] SAT3 forecast — always returns 'nominal' / 0.5 uncertainty @ colregs_reasoner_node.cpp:723-724 // sat3.predicted_state='nominal' and sat3.prediction_uncertainty=0.5 are hardcoded constants that look like real CMM forecast output but carry no computed information
- [MASQ-伪装真实] SAT2 reasoning_chain — empty string published as if reasoning chain is serialized @ colregs_reasoner_node.cpp:718 // msg.sat2.reasoning_chain='' published at 10 Hz on /l3/sat/data. The 5-layer chain IS computed but only placed on COLREGsConstraint — the SAT2 field is a stub
- [stub] ASDR signature field intentionally left empty @ colregs_reasoner_node.cpp:743 // Explicit comment 'SHA-256 not computed in Phase E1'; not masquerading as real
- [MASQ-伪装真实] Rule16_GiveWay.evaluate() as standalone CPA gate dressed as COLREGs Rule16 obligation check @ src/l3_tdl_kernel/m6_colregs_reasoner/src/rules/colregs/rule16_give_way.cpp:26-43 // Rule16 fires on cpa < 1.5*cpa_safe and sets role=GIVE_WAY unconditionally. Real Rule16 applies only when give-way role was already determined by Rule13/14/15. The code comment acknowledges this but then does it anyway.

## progress.md OVERCLAIM 修正
- 声称: M6-progress.md line 12: 'IDL + Arrow评分管线✅' for D2.4 status
    实际: COLREGsConstraint IDL is defined and the 5-layer chain is built and emitted on /l3/m6/colregs_constraint. However SAT2Data.colregs_chain (the DEMO-2 P0 serialization path) is never populated, and schema_version is always 0 on the constraint. The '✅' for IDL pipeline is optimistic.
    证据: colregs_reasoner_node.cpp:718 (sat2.colregs_chain never assigned); colregs_constraint_generator.cpp:43 (schema_version never set)
- 声称: M6-spec.md line 43: 'M6 推理已真实' (M6 reasoning is real)
    实际: Mostly accurate for Rules 13-15, 17, 19. But Rule16 is a CPA-threshold proxy not a true Rule16 obligation check; geometry sectors (22.5°, 112.5°, 247.5°) do not adapt to ODD zone for 6 of 8 rules; SAT3 forecast is always 'nominal'.
    证据: rule16_give_way.cpp:26 (CPA threshold only); all Rule13-18 have (void)odd; colregs_reasoner_node.cpp:723 (hardcoded 'nominal')

## 疑似断流 (flow_gaps_suspected)
- M6 SAT2 → M8 ColregsRationaleTree | M8 DEMO-2 ColregsRationaleTree frontend depends on SAT2Data.sat2.colregs_chain which M6 never populates. Chain is computed by build_colregs_chain() but only placed on COLREGsConstraint, not on SATData. :: colregs_reasoner_node.cpp:718 — sat2.reasoning_chain=''; sat2.colregs_chain never assigned in publish_sat_data(). M6-spec.md:25 acknowledges as DEMO-2 P0 missing
- M6 /l3/m6/rule_assessment → M4 (Rule13/15/17 coverage gap) | rule_assessment is published only when Rule14 latch is active for the primary target. For Rule13 (overtaking) or Rule15 (crossing) latched encounters, no rule_assessment is emitted even though M4 subscribes and stores latest_rule_assessment_. :: colregs_reasoner_node.cpp:595: only 14ULL key checked; behavior_arbiter_node.cpp:113-114: on_rule_assessment stores msg but Rule13/15 scenarios never provide one
- M6 COLREGsConstraint heading direction → M5 MPC cost_colreg | M6 emits primary_preferred_direction and per-rule numeric_value (minimum alteration degrees) on COLREGsConstraint. M5's mid_mpc_node.cpp consumes this message but reads only active_rules.empty() as a binary flag and reduces target CPA/TCPA by 80% as cost weight — it does NOT read heading direction or minimum alteration. Directional guidance from M6 never enters MPC heading window. :: mid_mpc_node.cpp:173-184 — entire M6 consumption block is 3 lines with no direction/alteration field read

## 相关 flow 边裁决
- [MEDIUM] M6 -> M4: /l3/m6/colregs_constraint (consumed as HARD constraint?) = PARTIAL: PARTIAL: constraint is read and actively used to shape IvP solution space. But IvP solver falls back to geometric heading on infeasibility (line 475-517), meaning constraints can be violated in fallback path without hard stop.
- [MEDIUM] M6 -> M5: /l3/m6/colregs_constraint = PARTIAL: M6 heading direction reaches M5 only indirectly via M4 IvP → behavior_plan window. The M6→M5 direct path carries only binary encounter flag and target-ID for weight scaling.
- [HIGH] M6 -> M5: /l3/m6/rule_assessment = BROKEN_NO_SUBSCRIBER: Edge documented in phase-1 maps as gap. Confirmed: M5 NLP cost tuning cannot be rule-aware because rule_assessment never arrives at M5.
- [HIGH] M6 -> M7: /l3/m6/colregs_constraint = PARTIAL: Subscriber receives and parses the message. All downstream HC logic (HC-2 COLREGs geometry check) is dead code — the function body does nothing.

## completeness-critic 相关补充
- [MEDIUM/MOCK] fsm_aggregator_node: active_rule hardcoded 'Rule 14 head-on' for ALL COLREG_AVOIDANCE encounters :: docker/fsm_aggregator_node.py:184-188 — When M4 behavior_plan.behavior == BEHAVIOR_COLREG_AVOID the aggregator sets active_rule = 'Rule 14 head-on' unconditionally, regardless of which actual rule M6 
- [HIGH/MOCK] shell_b_harness: TrackedTarget.cpa_m and tcpa_s hardcoded 0.0 — M5/M6/M2 receive zero CPA :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:472-473 — In ShellBSimulator.step() every published TrackedTarget has tgt.cpa_m = 0.0 and tgt.tcpa_s = 0.0. These are published directly 
- [HIGH/FLOW_GAP] ScoringNode: /l3/colregs_active subscription has no publisher anywhere in the codebase :: src/sim_workbench/sil_nodes/scoring/scoring/node.py:72 — ScoringLifecycleNode.on_configure() subscribes to std_msgs/String on '/l3/colregs_active'. The _colregs_cb() at line 132 parses 'Rule14:ok,Rule