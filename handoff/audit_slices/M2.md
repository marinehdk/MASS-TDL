# M2 审计证据片（来自 2026-06-08 系统审计 @158bba9d；当前代码请用 codegraph 复核）

role: Sole authoritative world-view publisher for L3 Tactical Layer: fuses nav-filter + tracked-target + environment inputs, computes CPA/TCPA and COLREG geometric pre-classification, and emits /l3/m2/world_state at 4 Hz (configured; spec says 50 Hz).

## 实际订阅 (subscriptions)
- /fusion/own_ship_state [USED] <l3_external_msgs/FilteredOwnShipState> src=External nav-filter :: world_model_node.cpp:239-243 — SensorDataQoS keep_last(2), ego reentrant cbg
- /fusion/tracked_targets [USED] <l3_external_msgs/TrackedTargetArray> src=External multimodal fusion :: world_model_node.cpp:246-250 — reliable keep_last(5)
- /fusion/environment_state [USED] <l3_external_msgs/EnvironmentState> src=External environment sensor :: world_model_node.cpp:253-257 — reliable transient_local
- /l3/m1/odd_state [USED] <l3_msgs/ODDState> src=M1 ODD Manager :: world_model_node.cpp:260-264 — reliable transient_local; used for zone-aware CPA threshold selection

## 实际发布 (publications)
- /l3/m2/world_state [PARTIAL_FIELDS] unpop=[schema_version (stays 0 — never assigned),targets[].schema_version (stays 0),targets[].rationale (never assigned, stays empty string)] :: world_state_aggregator.cpp:463-483 builds WorldState ws; ws.stamp, ws.targets, ws.own_ship, ws.zone, ws.confidence, ws.rationale are populated; ws.schema_version is never assigned (default uint16=0). Per WorldState.msg line 3: expected 112. TrackedTarget wt: wt.stamp, wt.target_id, wt.position, wt.sog_kn, wt.cog_deg, wt.heading_deg, wt.covariance, wt.cpa_m, wt.tcpa_s, wt.encounter, wt.confidence, wt.source_sensor, wt.brg_deg, wt.rng_m, wt.intent_confidence all set. wt.schema_version and wt.rationale never assigned. Live A4000 sample confirms schema_version=0, confidence=0.0 on waypoints (M5 avoidance_plan) — WorldState targets show same pattern.
- /l3/sat/data [REAL] unpop=[] :: world_model_node.cpp:495-568 publish_sat_data(); stamp, source_module, sat1, sat2, sat3 all populated; triggered at sat_rate_hz (10 Hz configured)
- /l3/asdr/record [REAL] unpop=[] :: world_model_node.cpp:571-583 publish_asdr_record(); stamp, source_module, decision_type, decision_json populated; signature explicitly cleared with TODO comment for v1.1
- /l3/m2/threat_state [MISSING] unpop=[cpa_status,target_relative_position,confidence,rationale] :: world_model_node.hpp:130-132 declares only world_state_pub_, sat_pub_, asdr_pub_ — no threat_state publisher. grep across entire src/ for 'l3/m2/threat_state' returns zero hits in C++/hpp. The bridge subscribes to this topic (sil_topic_bridge.py:418-420) and uses it for latch-release condition 1 (cpa_status==cleared), but no node publishes it — topic is dead on arrival.

## 设计职责实现状态 (responsibilities)
- [PARTIAL] ROS2 node + timer-driven publish :: world_model_node.cpp:32-41 — node exists, 4 timers set up. Timer fires on_aggregation_timer at configured aggregation_rate_hz=4.0 Hz. Spec §接口契约 says 50 Hz; m2_params.yaml line 3 configures 4 Hz. WorldState.msg comment says '4 Hz' so spec body is authoritative for 4 Hz, but top-line spec says 10-50 Hz. // Rate discrepancy: spec upper-bound 50 Hz, YAML config 4 Hz. Consistent with WorldState.msg comment which documents 4 Hz. Not a bug per current design, but spec §时间尺度 says 10-50 Hz.
- [REAL] CPA/TCPA real computation (C++ Eigen) :: world_state_aggregator.cpp:212-235 calls cpa_calc_->compute() for each target; result populates wt.cpa_m, wt.tcpa_s, wt.cpa_covariance_m2, wt.tcpa_covariance_s2. CpaTcpaCalculator uses Eigen with UncertaintyMethod::Linear configured (create_components, world_model_node.cpp:142). cpa_covariance values are clamped to [0, 2500] m² and [0, 100] s². 
- [REAL] COLREG geometric pre-classification (encounter_classifier.cpp) :: encounter_classifier.cpp:38-100 — full classify() logic for OVERTAKING/HEAD_ON/CROSSING, parameterised by Config. Sectors read from params (overtaking_bearing_min_deg=112.5, max=247.5 in m2_params.yaml lines 13-14). Called per-target in world_state_aggregator.cpp:252-257. 
- [REAL] OVERTAKING sector [112.5°, 247.5°] (MUST-1, claimed fixed in D0.1) :: encounter_classifier.cpp:70-71 checks bearing_0_360 >= cfg_.overtaking_bearing_min_deg && <= cfg_.overtaking_bearing_max_deg. m2_params.yaml lines 13-14 set 112.5/247.5. Unit test test_must1_overtaking_sector.cpp confirms boundaries. 
- [STUB] SOG validation f(Manifest.max_speed × 1.2) (MUST-6, claimed fixed in D0.1) :: test_must6_sog_validation.cpp:13-16 defines a file-local validate_sog() function that is NEVER called from world_model_node.cpp or world_state_aggregator.cpp. update_own_ship() at world_state_aggregator.cpp:74-100 assigns snap.sog_kn = msg.sog_kn unconditionally with no validation. No Manifest reference anywhere in M2 source. The test exercises an isolated stub, not production code. // MUST-6 test passes but is testing dead code. The live node accepts any SOG value without checking against max_speed.
- [MISSING] intent_distribution[] field (v3.0 spec, B P1-B-02 resolution) :: WorldState.msg has no intent_distribution field. TrackedTarget.msg has intent_confidence (scalar, D2.2 NEW), but the spec M2-spec.md line 26 requires intent_distribution[] array. Progress doc D2.2 claims 'intent_confidence 字段已落地', which refers to the scalar — the array is absent. world_state_aggregator.cpp:342-355 computes intent_confidence scalar only. 
- [REAL] BRG/RNG per-target output for ARPA table (D2.2 Track C) :: world_state_aggregator.cpp:323 sets wt.brg_deg = bearing_deg; lines 327-339 compute haversine wt.rng_m. TrackedTarget.msg lines 26-27 define both fields. M2-progress.md line 19 claims 'BRG/RNG 已落地'. 
- [PARTIAL] Environment sanity check (visibility/Hs/current/staleness) :: EnvSanityChecker::validate() (env_sanity_checker.cpp:14-73) implements: (1) staleness check (age > staleness_max_s), (2) current_speed > 10 kn rejection, (3) zone-type transition. The class header (env_sanity_checker.hpp:45-50) has a TODO noting 4 spec-mandated checks NOT YET implemented: visibility [0.01,50] nm, Hs [0,20] m, cross-source temperature consistency, current vs zone consistency. These 4 checks are absent because ZoneSnapshot lacks those fields. 
- [MISSING] /l3/m2/threat_state publication (ThreatState with cpa_status/target_relative_position/confidence/rationale) :: No publisher for /l3/m2/threat_state exists anywhere in the M2 implementation. world_model_node.hpp:130-132 declares 3 publishers only. The bridge subscribes (sil_topic_bridge.py:418-420) but will never receive a message. 
- [STUB] Target classification (fishing/passenger/cargo/tanker) :: world_state_aggregator.cpp:280-295 implements classification by SOG threshold. However parameter_loader.hpp:48 declares target_classification_enabled with default false, and m2_params.yaml has no target_classification_enabled key. The parameter stays false at runtime; the entire classification branch is dead code in deployed config. 
- [PARTIAL] CMM interface: current_state()/rationale()/forecast(Δt)+uncertainty() :: SAT data (sat1.state_summary, sat2.reasoning_chain, sat2.system_confidence) published via publish_sat_data() covers rationale() and current_state(). Forecast covered by compute_sat3_forecast() in world_model_node.cpp:546-560. However WorldState itself has schema_version=0 (never set), which is the CMM-mandated field per architecture §3. No explicit current_state() CMM method — state surfaced only through SAT topic, not on WorldState message. 
- [STUB] OwnShipState.r_dot_deg_s (yaw rate) population :: world_state_aggregator.cpp:372 comment: 'r_dot_deg_s and nav_mode not available from OwnShipSnapshot — use defaults'; os_msg.r_dot_deg_s = 0.0 hardcoded. nav_mode = 'OPTIMAL' hardcoded (line 381). OwnShipSnapshot struct has no r_dot field; FilteredOwnShipState is never checked for a matching field. 

## 设计-实现偏离 (deltas)
- [HIGH] schema_version never set on WorldState and TrackedTarget
    设计: WorldState.msg and TrackedTarget.msg declare 'uint16 schema_version # 112 = v1.1.2' as CMM-mandated field per architecture §3 and §15.1.
    实现: world_state_aggregator.cpp:463-483 builds WorldState ws and never assigns ws.schema_version. TrackedTarget wt (line 209) never has wt.schema_version assigned. Both fields default to 0. Live A4000 confirms schema_version=0 on published messages.
    证据: world_state_aggregator.cpp:463-483; WorldState.msg:3; TrackedTarget.msg:3; A4000 ground truth sample schema_version=0
- [HIGH] /l3/m2/threat_state topic published nowhere — bridge subscriber starved
    设计: M2 spec lists /l3/m2/threat_state as an expected output (ThreatState.msg: cpa_status, target_relative_position, confidence, rationale). The bridge uses it for latch-release condition 1 (cpa_status=cleared + target_relative_position=astern).
    实现: No publisher exists. world_model_node.hpp declares only world_state_pub_, sat_pub_, asdr_pub_. grep for 'l3/m2/threat_state' in all src/ C++/hpp returns zero hits. Bridge _on_threat_state() (sil_topic_bridge.py:867-877) will never fire.
    证据: world_model_node.hpp:130-132; sil_topic_bridge.py:418-420, 867-877; ThreatState.msg:1-9
- [HIGH] MUST-6 SOG validation is test-only dead code — production node skips it
    设计: M2-spec.md §关键字段: 'sog 校验 f(Manifest.max_speed × 1.2)'. M2-progress.md D0.1 claims MUST-6 closed. Architecture rule: no vessel constants — Manifest is the authority.
    实现: validate_sog() exists only in test_must6_sog_validation.cpp:13-16 as a file-local anonymous-namespace function. update_own_ship() in world_state_aggregator.cpp:77 does snap.sog_kn = msg.sog_kn with no validation. No Manifest is referenced or injected into M2 at all.
    证据: world_state_aggregator.cpp:74-100; test_must6_sog_validation.cpp:1-44; world_model_node.cpp (no Manifest include)
- [MEDIUM] Aggregation rate 4 Hz vs spec 10-50 Hz
    设计: M2-spec.md §时间尺度: '10–50 Hz'. Architecture module table: M2 '10–50 Hz'.
    实现: m2_params.yaml line 3: aggregation_rate_hz=4.0. WorldState.msg comment says '4 Hz (M2 内部 2 Hz 输入聚合 + 1 次插值/外推)'. on_aggregation_timer() fires at this rate.
    证据: m2_params.yaml:3; world_model_node.cpp:292; WorldState.msg:2
- [MEDIUM] intent_distribution[] array absent — only scalar intent_confidence present
    设计: M2-spec.md §下游发布: 'WorldStateMsg @ 50Hz: 含 intent_distribution[] 字段（v3.0 修订，B P1-B-02 整改）'.
    实现: WorldState.msg has no intent_distribution field. TrackedTarget.msg has intent_confidence (float32 scalar, D2.2 NEW). world_state_aggregator.cpp:342-355 computes the scalar. The B P1-B-02 resolution intent array is absent from both the message definition and the implementation.
    证据: WorldState.msg (full file, no intent_distribution); TrackedTarget.msg:22-23; world_state_aggregator.cpp:342-355; M2-spec.md:26
- [MEDIUM] OwnShipState.r_dot_deg_s hardcoded zero — yaw rate not propagated
    设计: OwnShipState is sourced from FilteredOwnShipState (15-state EKF per spec §上游订阅), which should include yaw rate for downstream M5 MPC initial state.
    实现: world_state_aggregator.cpp:372: os_msg.r_dot_deg_s = 0.0 with comment 'not available from OwnShipSnapshot — use defaults'. OwnShipSnapshot struct never captures this field. M5 tactical planner uses own_ship state including r (yaw rate) for MPC initial conditions.
    证据: world_state_aggregator.cpp:371-372; world_model_node.hpp:107 (OwnShipSnapshot has no r_dot field)
- [MEDIUM] EnvSanityChecker missing 4 of 7 mandated checks
    设计: M2-spec.md §关键字段: '环境字段 sanity check（visibility/Hs/current 范围 + 跨源 + staleness）'. D2.2 progress claims env sanity 落地.
    实现: env_sanity_checker.cpp implements staleness, current_speed, zone_transition only. The class header has an explicit TODO (lines 45-50) listing 4 absent checks: visibility range, Hs range, cross-source temperature, current vs zone. These require ZoneSnapshot field extensions not yet added.
    证据: env_sanity_checker.hpp:45-50; env_sanity_checker.cpp:14-73
- [MEDIUM] Bridge computes CPA/TCPA locally instead of consuming M2 output
    设计: M2 is the single authority for CPA/TCPA. WorldState.targets[].cpa_m and tcpa_s are M2's computed outputs. The bridge should consume these for release decisions.
    实现: sil_topic_bridge.py:775-832 _compute_dcpa_tcpa() reimplements CPA/TCPA from raw position/velocity of own ship and target vessel, with explicit comment (line 789): '[TBD-HAZID][cross-module] M2 ThreatState should publish numeric cpa_m/tcpa_s; this bridge-local computation is an interim measure'. The bridge uses this local computation for geometry-based latch release, ignoring M2's already-computed cpa_m.
    证据: sil_topic_bridge.py:789, 855; docker/sil_topic_bridge.py:108-112 CPA_SAFE_M=1000.0 hardcoded
- [LOW] target_classification_enabled defaults false in all deployments — classification branch dead
    设计: world_state_aggregator.cpp:280-295 implements SOG-based classification (fishing/passenger/cargo/tanker).
    实现: parameter_loader.hpp:48 declares default false. m2_params.yaml has no target_classification_enabled key. Parameter stays false at runtime. The entire classification branch never executes; classification/classification_confidence pass through from the upstream TrackedTargetArray unchanged.
    证据: world_state_aggregator.cpp:280; parameter_loader.hpp:48; m2_params.yaml (no key present)

## MOCK / STUB
- [MASQ-伪装真实] validate_sog() — MUST-6 SOG validation @ src/l3_tdl_kernel/m2_world_model/test/test_must6_sog_validation.cpp:13 // File-local anonymous-namespace function in a test file. Never linked into the node. D0.1 and M2-progress.md claim MUST-6 is closed, but the node does no SOG validation.
- [MASQ-伪装真实] target classification by SOG threshold (fishing/passenger/cargo/tanker) @ src/l3_tdl_kernel/m2_world_model/src/world_state_aggregator.cpp:280 // Code exists and looks real, but target_classification_enabled defaults to false and is absent from m2_params.yaml, making this dead code in all deployed configurations.
- [stub] OwnShipState.r_dot_deg_s hardcoded to 0.0 @ src/l3_tdl_kernel/m2_world_model/src/world_state_aggregator.cpp:372 // Comment explicitly admits it ('not available — use defaults'). Not a silent fake. Downstream M5 receives zero yaw-rate in every cycle.
- [MASQ-伪装真实] os_msg.nav_mode hardcoded to 'OPTIMAL' @ src/l3_tdl_kernel/m2_world_model/src/world_state_aggregator.cpp:381 // Always emits nav_mode='OPTIMAL' regardless of actual vessel or ODD state. No logic connects this to M1 envelope state.
- [MASQ-伪装真实] EnvSanityChecker::validate() — only 3 of 7 checks implemented @ src/l3_tdl_kernel/m2_world_model/src/env_sanity_checker.cpp:14 // Called in production code path (world_state_aggregator.cpp:133). Returns passed=true for most env inputs (only staleness, current_speed, zone_transition checked). Visibility/Hs/temperature/zone-current checks are absent with TODO comment.

## progress.md OVERCLAIM 修正
- 声称: D0.1 Closed: MUST-6 (sog 校验改读 Manifest) — M2-progress.md line 11
    实际: The validate_sog() function exists only in a test file's anonymous namespace and is never called from production code. update_own_ship() in world_state_aggregator.cpp:77 assigns sog_kn unconditionally. No Manifest is injected into M2 anywhere.
    证据: test_must6_sog_validation.cpp:13-16; world_state_aggregator.cpp:74-100
- 声称: D2.2 Closed: 'intent_confidence 字段已落地（B P1-B-02 决议闭环）' — M2-progress.md line 21
    实际: Partially true: intent_confidence scalar (float32) is populated in TrackedTarget. But M2-spec.md §下游发布 requires intent_distribution[] array per the v3.0 / B P1-B-02 resolution. The array field does not exist in WorldState.msg or TrackedTarget.msg. The progress doc conflates the scalar field with the array requirement.
    证据: WorldState.msg (no intent_distribution); TrackedTarget.msg:22-23; M2-spec.md:26
- 声称: D2.2 Closed: 'env sanity' falls under '~900 LOC C++ + 18 Python tests' — M2-progress.md line 14
    实际: EnvSanityChecker is real and called in production, but env_sanity_checker.hpp:45-50 explicitly documents that 4 of the spec-mandated checks are unimplemented (visibility, Hs, cross-source temp, current vs zone). The progress claim implies full env sanity implementation.
    证据: env_sanity_checker.hpp:45-50; env_sanity_checker.cpp:14-73
- 声称: D1.3.2.3 Closed: 'CPA/TCPA 真发布到 /sil/cpa_tcpa（foxglove_bridge 消费端落地）' — M2-progress.md line 12
    实际: The bridge does NOT consume M2's cpa_m/tcpa_s from world_state. It reimplements CPA/TCPA locally (sil_topic_bridge.py:775-832). The bridge comment (line 789) explicitly marks this as an 'interim measure until M2 is updated'. No /sil/cpa_tcpa topic publisher was found in the current bridge code.
    证据: sil_topic_bridge.py:789, 855; docker/sil_topic_bridge.py:398-400 (only records pulse from world_state, does not extract cpa)

## 疑似断流 (flow_gaps_suspected)
- /l3/m2/threat_state: M2 → bridge (sil_topic_bridge._on_threat_state) | Bridge subscribes to this topic for latch-release condition 1. Since M2 never publishes it, the cpa_status='cleared' latch release path is permanently dead. Release relies entirely on the bridge's own _compute_dcpa_tcpa() geometry check. :: sil_topic_bridge.py:418-420, 867-877; world_model_node.hpp:130-132 (no threat_state publisher)
- M2 world_state.targets[].cpa_m → bridge CPA release logic | M2 computes authoritative CPA/TCPA per target (world_state_aggregator.cpp:212-235) and publishes it in world_state. The bridge reads world_state only for module pulse recording (_record_pulse M2, sil_topic_bridge.py:399-400). It then duplicates CPA computation locally. Two inconsistent CPA values now exist in the system with no reconciliation. :: sil_topic_bridge.py:399-400 (pulse only), 855 (_compute_dcpa_tcpa used for release)
- /l3/m1/odd_state → M2 (ODD zone for CPA threshold selection) | If M1 has not yet published odd_state, odd_cache_ is empty and compose_world_state() falls back to OddZone::A for CPA threshold selection (world_state_aggregator.cpp:204-206). Zone A threshold is 1852 m — the most conservative. This is a safe fallback but means early-startup world state uses worst-case CPA threshold. :: world_state_aggregator.cpp:204-206

## 对抗验证裁决 (本模块)
- [HIGH] schema_version never set on WorldState and TrackedTarget => CONFIRMED(HIGH): Both WorldState.msg:3 and TrackedTarget.msg:3 declare 'uint16 schema_version  # 112 = v1.1.2'. The entire world_state_aggregator.cpp contains zero occurrences of the string 'schema_version' (confirmed via grep exit code 1). The WorldState build block (lines 462-483) assigns stamp, targets, own_ship,
- [HIGH] /l3/m2/threat_state topic published nowhere — bridge subscriber starved => CONFIRMED(HIGH): The gap is real and current. WorldModelNode.setup_publishers() (world_model_node.cpp:271-284) creates exactly three publishers — /l3/m2/world_state, /l3/sat/data, /l3/asdr/record — with no /l3/m2/threat_state publisher. The header (world_model_node.hpp:129-132) declares the matching three SharedPtr 
- [HIGH] MUST-6 SOG validation is test-only dead code — production node skips it => CONFIRMED(HIGH): Three independent checks all confirm the gap. (1) codegraph_search finds validate_sog at exactly one location: test/test_must6_sog_validation.cpp:13 — nowhere in production source. (2) world_state_aggregator.cpp:77 does snap.sog_kn = msg.sog_kn with no threshold guard, no Manifest lookup, no call to
- [HIGH] MOCK masquerading: validate_sog() — MUST-6 SOG validation => CONFIRMED(HIGH): Three independent pieces of current-code evidence confirm the gap. (1) validate_sog at test/test_must6_sog_validation.cpp:13 is defined inside an anonymous namespace — it is file-local, only compiled into the test binary, and never declared in any header or called from production code. (2) A Python-
- [HIGH] MOCK masquerading: target classification by SOG threshold (fishing/passenger/cargo/tanker) => CONFIRMED(HIGH): Three convergent pieces of evidence confirm the claim. (1) world_state_aggregator.hpp:39 declares `target_classification_enabled{false}` as the in-struct default. (2) parameter_loader.hpp:48 declares the ROS2 parameter with `false` as its default, so even if the node starts without a yaml override t
- [HIGH] MOCK masquerading: os_msg.nav_mode hardcoded to 'OPTIMAL' => CONFIRMED(HIGH): Line 381 of src/l3_tdl_kernel/m2_world_model/src/world_state_aggregator.cpp unconditionally assigns `os_msg.nav_mode = "OPTIMAL"`. The developer comment at line 371 explicitly acknowledges `nav_mode` is not wired: "r_dot_deg_s and nav_mode not available from OwnShipSnapshot — use defaults". Confirmi
- [HIGH] MOCK masquerading: EnvSanityChecker::validate() — only 3 of 7 checks implemented => CONFIRMED(HIGH): Current code at src/l3_tdl_kernel/m2_world_model/src/env_sanity_checker.cpp (lines 14–73) implements exactly 3 checks inside validate(): staleness (lines 22–30), current_speed (lines 32–41), and zone_transition (lines 43–53). The header env_sanity_checker.hpp lines 45–50 carries an explicit TODO nam

## 相关 flow 边裁决
- [MEDIUM] Fusion -> M2: /fusion/own_ship_state = PARTIAL: The subscription and core position/speed fields are wired correctly. The yaw-rate field (r_dot_deg_s) present in FilteredOwnShipState is silently zeroed before inclusion in WorldState, breaking the kinematic state completeness required for M5 MPC initialization.
- [LOW] M2 -> M1: /l3/m2/world_state = PARTIAL: M1 receives the topic but uses it only as a heartbeat indicator. Actual world-state content (targets, CPA, ODD zone inputs) is not consumed by M1 from this topic — M1 derives its ODD scoring from direct /fusion/own_ship_state and /fusion/environment_state subscriptions, not from M2's synthesized world view.
- [MEDIUM] M2 -> M4: /l3/m2/world_state = PARTIAL: Functional data flows correctly for transit and geometry. The PARTIAL is because schema_version (CMM contract) and per-target rationale fields are populated as 0/empty by M2 and never checked by M4; and intent_distribution[] absent from WorldState.msg means M4 cannot read multi-modal intent.
- [HIGH] M2 -> M5: /l3/m2/world_state (target trajectory for MPC) = PARTIAL: Mid-MPC correctly consumes /l3/m2/world_state and uses targets for MPC formulation. BC-MPC is silently starved because the topic string '/m2/world_state' does not exist on the live bus (live list shows '/l3/m2/world_state' only). This matches the phase-1 finding 'BC-MPC subscribes to wrong world_state namespace'.
- [HIGH] M2 -> ?: /l3/m2/threat_state (consumer identification) = BROKEN_NO_PUBLISHER: The topic appears in the live topic list, which is anomalous given no publisher in source code. This is likely a ghost entry from a prior session or a test injector not reflected in current code. The bridge's latch-release logic in _on_threat_state is therefore never triggered by a real M2 signal — the avoidance teardown path that depends on CPA-cleared status from ThreatState is permanently inert. This is a critical architectural gap: M2 spec requires publishing ThreatState with cpa_status/target_relative_position but the implementation was never written.

## completeness-critic 相关补充
- [HIGH/MOCK] shell_b_harness: TrackedTarget.cpa_m and tcpa_s hardcoded 0.0 — M5/M6/M2 receive zero CPA :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:472-473 — In ShellBSimulator.step() every published TrackedTarget has tgt.cpa_m = 0.0 and tgt.tcpa_s = 0.0. These are published directly 
- [HIGH/DESIGN_IMPL_DESYNC] shell_b_harness doer_composition: M3 and M7 not included — scoring and safety gate both absent from RL loop :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:368-383 — doer_cmd includes m1_odd_manager, m2_world_model, m4_behavior_arbiter, m5_tactical_planner, m8_hmi_bridge. M3 (mission_manager_