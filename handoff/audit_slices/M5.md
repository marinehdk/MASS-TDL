# M5 审计证据片（来自 2026-06-08 系统审计 @158bba9d；当前代码请用 codegraph 复核）

role: Mid-MPC (IPOPT/CasADi, 1 Hz) + BC-MPC (event-driven, 4-10 Hz) — computes avoidance trajectory and short-horizon reactive override. Publishes /l3/m5/avoidance_plan (waypoints → L4 bridge), /sil/sat3_data (trajectory candidates), /m5/reactive_override_cmd (BC-MPC override, not wired to bridge or launch).

## 实际订阅 (subscriptions)
- /l3/m2/world_state [USED] <l3_msgs/WorldState> src=M2 World Model :: mid_mpc_node.cpp:49-53 — sub_world_ subscribes to /l3/m2/world_state
- /l3/m4/behavior_plan [USED] <l3_msgs/BehaviorPlan> src=M4 Behavior Arbiter :: mid_mpc_node.cpp:55-59 — sub_behavior_ subscribes to /l3/m4/behavior_plan
- /l3/m6/colregs_constraint [USED] <l3_msgs/COLREGsConstraint> src=M6 COLREGs Reasoner :: mid_mpc_node.cpp:61-65 — sub_colregs_ subscribes to /l3/m6/colregs_constraint (remapped from /m6/colregs_constraint by launch file m5_mid_mpc.launch.py:17)
- /l2/planned_route [USED] <l3_external_msgs/PlannedRoute> src=L2 Voyage Planner :: mid_mpc_node.cpp:67-71 — sub_route_ subscribes; NOT in has_required_inputs_() so silently treated as optional
- /l2/speed_profile [USED] <l3_external_msgs/SpeedProfile> src=L2 Voyage Planner :: mid_mpc_node.cpp:73-77 — sub_speed_ subscribes; also NOT in has_required_inputs_(), optional
- /m2/world_state [DECLARED_UNUSED] <l3_msgs/WorldState> src=M2 World Model :: bc_mpc_node.cpp:34-37 — BC-MPC subscribes to /m2/world_state (no leading /l3/), a different namespace from what M2 publishes (/l3/m2/world_state). No remapping in launch or compose. BC-MPC will receive zero messages in production.
- /m5/avoidance_plan [DECLARED_UNUSED] <l3_msgs/AvoidancePlan> src=Mid-MPC (self) :: bc_mpc_node.cpp:40-43 — BC-MPC subscribes to /m5/avoidance_plan (no /l3/ prefix); Mid-MPC publishes on /m5/avoidance_plan (mid_mpc_node.cpp:79). Namespace matches each other but NOT what the bridge consumes (/l3/m5/avoidance_plan, sil_topic_bridge.py:372). BC-MPC sub works but plan is ALSO published on wrong namespace for bridge.
- /l3/m1/odd_state [EXPECTED_MISSING] <l3_msgs/ODDState> src=M1 ODD Envelope Manager :: mid_mpc_node.hpp (full file) and mid_mpc_node.cpp (full constructor) — no subscription to M1 ODD state. M5 does not gate on ODD state at all. ADR-1 (ODD = unique authority) violated.
- /l3/checker/veto [EXPECTED_MISSING] <l3_external_msgs/CheckerVetoNotification> src=X-axis Deterministic Checker :: mid_mpc_node.cpp and bc_mpc_node.cpp — neither node subscribes to any checker veto topic. M5 has no hard-gate on X-axis VETO.

## 实际发布 (publications)
- /m5/avoidance_plan [PARTIAL_FIELDS] unpop=[waypoints[*].schema_version (NLP success path only),waypoints[*].stamp (all paths),waypoints[*].confidence (NLP success path only),waypoints[*].rationale (NLP success path only),cost_colreg / cost_dist / cost_vel (always zero — unpack_solution comment: 'Phase E1: cost breakdowns not split out', mid_mpc_nlp_formulation.cpp:296-298)] :: mid_mpc_node.cpp:79 publishes on /m5/avoidance_plan. Bridge consumes /l3/m5/avoidance_plan (sil_topic_bridge.py:372). Topic mismatch — mid_mpc_node.cpp:79 has no /l3/ prefix, no remapping exists in launch for avoidance_plan. However, the system is live at 5 Hz per A4000 ground truth, which means remapping must be applied at the compose/run level (not visible in launch file). Plan-level stamp set in publish_outputs_ (mid_mpc_node.cpp:392-393). Waypoint-level fields: in NLP success path (wp_gen_.generate), schema_version, stamp, confidence, rationale are NOT SET on AvoidanceWaypoint (mid_mpc_waypoint_generator.cpp:87-129 — no wp.schema_version/stamp/confidence/rationale assignments). In geometric fallback, wp.schema_version=112 (mid_mpc_node.cpp:354), wp.confidence=0.6f (mid_mpc_node.cpp:362), wp.rationale set (mid_mpc_node.cpp:364). So NLP success path leaves waypoint CMM fields at zero/empty, matching live SUSPECT observation (confidence=0.0, schema_version=0, rationale='').
- /m5/asdr_record [REAL] unpop=[] :: mid_mpc_node.cpp:80, publish_outputs_ at mid_mpc_node.cpp:402-409 — stamp, source_module, decision_type, decision_json, sha256 signature all populated.
- /m5/sat_data [PARTIAL_FIELDS] unpop=[] :: mid_mpc_node.cpp:411-417 — stamp, source_module, sat2.trigger_reason, sat2.reasoning_chain, sat2.system_confidence populated. SAT-3 fields not populated here (they go to /sil/sat3_data). No schema_version on SATData.
- /sil/sat3_data [PARTIAL_FIELDS] unpop=[tc.rule_compliant (always false — not evaluated),tc.confidence (always 1.0F — not derived from solver quality)] :: mid_mpc_node.cpp:428-456 — stamp and schema_version=112 set on SAT3Data. Each TrajectoryCandidate: tc.confidence=1.0F (hardcoded), tc.rule_compliant=false (hardcoded always, mid_mpc_node.cpp:441), actual COLREGs compliance not evaluated. Nomoto fallback used for all candidates regardless of NLP solver state.
- /m5/reactive_override_cmd [STUB] unpop=[] :: bc_mpc_node.cpp:46-47, publish_override_ at bc_mpc_node.cpp:164-181 — BC-MPC node exists and compiles; publishes on /m5/reactive_override_cmd. However: (1) BC-MPC is NOT in m5_mid_mpc.launch.py (only m5_mid_mpc_node is launched), (2) BC-MPC is not listed in verify_demo1_e2e.sh node checks, (3) no bridge subscription to reactive_override_cmd exists (docker/sil_topic_bridge.py has zero matches), (4) BC-MPC subscribes to /m2/world_state — wrong namespace. BC-MPC is a dead node in the running system.
- /m5/asdr_record_bc [STUB] unpop=[] :: bc_mpc_node.cpp:48-49 — declared but BC-MPC node is never launched; output never produced.

## 设计职责实现状态 (responsibilities)
- [PARTIAL] Mid-MPC NLP (IPOPT/CasADi) N=18 / 90s horizon avoidance trajectory at 1-2 Hz :: mid_mpc_node.cpp:87-92 — timer at 1 Hz (std::chrono::seconds(1)). NLP formulation default n_horizon=12 (mid_mpc_nlp_formulation.hpp:67); m5_params.yaml declares n_steps=18. Config{} in main passes default struct, n_horizon stays 12 unless parsed from param — no declare_parameter for n_steps found in mid_mpc_node.cpp. Params file key 'n_steps' not mapped to nlp.n_horizon in node constructor. Effective N may be 12 not 18. NLP is real CasADi/IPOPT code but intermittently fails (Restoration_Failed keystone D1 OPEN). // m5_params.yaml n_steps=18 dt_s=5.0 → nominal 90 s horizon if applied. But the only declare_parameter in the node constructor is m5.nominal_speed_kn (line 47). The NlpFormulation::Config is passed via MidMpcNode::Config default-constructed — no ROS param wiring for n_horizon or dt_s. Effective horizon may default to 12×5s=60s, not 18×5s=90s.
- [STUB] BC-MPC 13-candidate short-horizon override at 4-10 Hz (SLA < 150ms) :: bc_mpc_node.cpp exists with real solver calls but: node not launched (not in m5_mid_mpc.launch.py), not in docker-compose, subscribes to wrong topic /m2/world_state (should be /l3/m2/world_state), output /m5/reactive_override_cmd not consumed by bridge. Dead code path in production. // Node code appears functionally correct but is unreachable in the running SIL stack.
- [REAL] Geometric fallback marked DEGRADED (not VALID) — D2 fix :: mid_mpc_node.cpp:314 — plan.status = 'DEGRADED'; plan.confidence = 0.6f. D2 fix verified present. 
- [REAL] M4 TRANSIT → empty plan (no waypoints) to release bridge from avoidance — D-DEMO1 fix :: mid_mpc_node.cpp:243-254 — behavior==TRANSIT emits plan with schema_version=112, status=NORMAL, confidence=1.0, rationale set, waypoints left empty. Verified present. 
- [MISSING] Gate on M1 ODD state (ADR-1: ODD = unique authority) :: mid_mpc_node.cpp constructor (lines 34-93) — no subscription to /l3/m1/odd_state or any ODD topic. M5 runs regardless of ODD envelope status. ADR-1 broken. 
- [MISSING] Gate on X-axis Checker VETO (ADR-1: VETO authority) :: mid_mpc_node.cpp and bc_mpc_node.cpp — no subscription to checker veto notification. M5 does not honor X-axis VETO. 
- [REAL] ROT_max from Capability Manifest (MUST-5, no hardcoded value) :: mid_mpc_node.cpp:209 — rot_max_rad_s = vessel_model_.rot_max_rad_s(inp.own_ship.u_mps, hs_m). CapabilityManifest loaded from YAML at construction (mid_mpc_node.cpp:36-38). Comment at line 208: [TBD-HAZID] sea state from EnvironmentState (hs_m hardcoded 0.0). 
- [PARTIAL] DEMO-2 P0: /sil/sat3_data trajectory_candidates at 2 Hz (Nomoto fallback) :: mid_mpc_node.cpp:423-457 — pub_sat3_data_ publishes on /sil/sat3_data every solve cycle (1 Hz, not 2 Hz). Nomoto fallback solve called; tc.rule_compliant hardcoded false (line 441); tc.confidence hardcoded 1.0F regardless of solve quality. // Progress doc claims '2Hz' but solve_timer_ is 1 Hz (std::chrono::seconds(1), mid_mpc_node.cpp:88-92).
- [MISSING] urgency_level > 0.95 triggers BC-MPC ±60° expansion (P2-B-01) :: bc_mpc_node.cpp — BC-MPC not launched; urgency_level field not read anywhere in mid_mpc_node.cpp or bc_mpc_node.cpp. 
- [PARTIAL] MRM walks M7 path (MUST-9): M5 escalates to M7 on consecutive failures :: mid_mpc_solver.cpp:169-170, 207-210 — logs spdlog::critical when consecutive_failures_ > 5. But 'escalation' is only a log message; no ROS2 publication to M7 MRM topic, no actual MRM-02 trigger. MUST-9 not implemented beyond logging. // kConsecutiveFailureEscalation=5 is [TBD-HAZID] uncalibrated.
- [REAL] FM-4 hardcoded fallback deleted (MUST-5) :: No FM-4 hardcoded fallback found. Fallback is now the geometric arc from build_geometric_fallback_plan_ which reads from CapabilityManifest-derived rot_max. MUST-5 compliant. 
- [PARTIAL] CMM three-interface: current_state(), rationale(), forecast(Δt)+uncertainty() via schema_version/confidence/rationale fields :: Plan-level fields populated (stamp, status, rationale, confidence). Waypoint-level fields unpopulated in NLP success path (schema_version=0, stamp=zero, confidence=0.0, rationale='' — matches live A4000 SUSPECT observation). cost_colreg/cost_dist/cost_vel always zero (nlp_formulation.cpp:296-298 comment: 'Phase E1: not split out'). 

## 设计-实现偏离 (deltas)
- [CRITICAL] BC-MPC node never launched — entire short-horizon layer is dead in production
    设计: Spec §1: BC-MPC runs at 4-10 Hz, SLA <150ms, with 13 candidate branches. It is the short-horizon safety layer complementing Mid-MPC.
    实现: BC-MPC node exists in src/ and compiles, but m5_mid_mpc.launch.py only launches m5_mid_mpc_node (mid_mpc). There is no BC-MPC entry in docker-compose.yml, no launch inclusion, and no mention in verify_demo1_e2e.sh node list. The only reference is main_bc_mpc.cpp binary. In the running A4000 SIL stack, BC-MPC produces zero output.
    证据: m5_mid_mpc.launch.py:11-21 (only m5_mid_mpc_node launched); docker/sil_topic_bridge.py (zero matches for reactive_override_cmd or bc_mpc); scripts/verify_demo1_e2e.sh:228 (m5_mid_mpc_node checked, no m5_bc_mpc_node)
- [HIGH] BC-MPC subscribes to wrong world_state namespace (/m2/ instead of /l3/m2/)
    设计: M5 consumes M2 WorldState. In the running system M2 publishes on /l3/m2/world_state (A4000 ground truth: 20 Hz).
    实现: bc_mpc_node.cpp:35 subscribes to /m2/world_state (no /l3/ prefix). No remapping in any launch or compose file. Even if BC-MPC were launched, it would receive zero world_state messages.
    证据: bc_mpc_node.cpp:35 vs mid_mpc_node.cpp:50 (/l3/m2/world_state)
- [HIGH] AvoidanceWaypoint CMM fields (schema_version, stamp, confidence, rationale) unpopulated in NLP success path
    设计: Architecture report ADR: every message must carry stamp, schema_version, confidence∈[0,1], rationale. Live A4000 observation: per-waypoint schema_version=0, confidence=0.0, rationale='', stamp.sec=0.
    实现: mid_mpc_waypoint_generator.cpp:87-129 (build_waypoints_) never sets wp.schema_version, wp.stamp, wp.confidence, or wp.rationale. These fields are populated in the geometric fallback path (mid_mpc_node.cpp:354-367) but not in the NLP success path.
    证据: mid_mpc_waypoint_generator.cpp:87-129 (no schema_version/stamp/confidence/rationale assignments); mid_mpc_node.cpp:354-367 (fallback sets all four)
- [HIGH] cost_colreg / cost_dist / cost_vel always zero — COLREGs cost term invisible in rationale
    设计: Spec: avoidance_plan carries IPOPT KKT residual and cost breakdown. Rationale should reflect COLREGs cost weighting.
    实现: mid_mpc_nlp_formulation.cpp:296-298 comment: 'Phase E1: cost_total / cost_colreg / cost_dist / cost_vel are not split out from CasADi stats. They remain zero-init.' compose_rationale_ in mid_mpc_waypoint_generator.cpp:139-141 logs cost_colreg=0.000 always — this was previously misdiagnosed as a bug but is a confirmed Phase E1 TODO.
    证据: mid_mpc_nlp_formulation.cpp:296-298; mid_mpc_waypoint_generator.cpp:139
- [HIGH] M5 has no ODD state subscription — ADR-1 (ODD = unique authority) violated
    设计: ADR-1: M1 ODD state is the unique switching authority. No module may run avoidance logic outside ODD envelope without M1 gating.
    实现: mid_mpc_node.cpp constructor (lines 34-93): no subscription to /l3/m1/odd_state or any ODD topic. M5 runs its full solve cycle regardless of ODD state (NORMAL/DEGRADED/CRITICAL/OUT-of-ODD). On_solve_cycle_ is timer-driven only.
    证据: mid_mpc_node.cpp:34-93 (complete constructor — no ODDState subscription); mid_mpc_node.hpp (no sub_odd_ member)
- [MEDIUM] Mid-MPC publishes avoidance_plan on /m5/ namespace but bridge consumes /l3/m5/ — resolved in run configuration, but launch file incomplete
    设计: Bridge subscribes /l3/m5/avoidance_plan (sil_topic_bridge.py:372). Spec says publish to /l3/m5/avoidance_plan.
    实现: mid_mpc_node.cpp:79 creates publisher on /m5/avoidance_plan (no /l3/ prefix). m5_mid_mpc.launch.py only remaps /m6/colregs_constraint, NOT avoidance_plan. System is live at 5 Hz on A4000, implying a compose-level remapping exists outside this repo's visible launch config, but the launch file is incomplete and the mismatch is a latent fragility.
    证据: mid_mpc_node.cpp:79 vs sil_topic_bridge.py:372; m5_mid_mpc.launch.py:16-18 (only one remapping)
- [MEDIUM] M7 MRM escalation is log-only — MUST-9 not wired
    设计: Spec MUST-9: MRM walks M7 path. M5 must escalate to M7 on persistent NLP failure (FM-2 / consecutive failures).
    实现: mid_mpc_solver.cpp:169-170, 207-210 — emits spdlog::critical log only. No publication to any M7 topic. No ROS2 service call. MRM-02 escalation exists only as a log message.
    证据: mid_mpc_solver.cpp:169-170, 207-210
- [MEDIUM] Mid-MPC horizon N=18 from params likely not applied — n_horizon defaults to 12
    设计: Spec / RFC-001 LOCKED: Mid-MPC N=18 / 90s (N=18, dt=5s).
    实现: mid_mpc_nlp_formulation.hpp:67 sets n_horizon default=12. main_mid_mpc.cpp passes MidMpcNode::Config{} (default). mid_mpc_node.cpp constructor has only one declare_parameter call (m5.nominal_speed_kn at line 47) — no declare_parameter for n_horizon or dt_s. m5_params.yaml n_steps=18 is declared but never read into nlp config by the node. Effective horizon is likely 12×5s=60s, not 18×5s=90s.
    证据: mid_mpc_nlp_formulation.hpp:67 (n_horizon{12}); mid_mpc_node.cpp:47 (only declare_parameter); m5_params.yaml:5 (n_steps: 18 unused)
- [LOW] /sil/sat3_data trajectory_candidates: rule_compliant always false, confidence always 1.0F
    设计: SAT-3 trajectory view should indicate which branches are COLREGs-compliant.
    实现: mid_mpc_node.cpp:441 tc.rule_compliant = false (hardcoded); tc.confidence = 1.0F (hardcoded regardless of branch quality or COLREGs evaluation).
    证据: mid_mpc_node.cpp:437, 441

## MOCK / STUB
- [MASQ-伪装真实] BC-MPC node (BcMpcNode) — entire short-horizon layer @ src/l3_tdl_kernel/m5_tactical_planner/src/bc_mpc/bc_mpc_node.cpp:1 / src/l3_tdl_kernel/m5_tactical_planner/src/bc_mpc/main_bc_mpc.cpp:1 // Real implementation code (solver, formulation, override generator, ASDR record all implemented). Masquerades as real because progress doc claims BC-MPC is complete (D3.2 ✅ 2751 LOC). But never launched, never wired to bridge, wrong world_state topic namespace, reactive_override_cmd not consumed anywhere. In production: zero output.
- [MASQ-伪装真实] M5 MRM-02 escalation — spdlog::critical log treated as real MRM dispatch @ src/l3_tdl_kernel/m5_tactical_planner/src/mid_mpc/mid_mpc_solver.cpp:169-170, 207-210 // Comment says 'M7 MRM-02 escalation'; code only calls spdlog::critical(). No ROS2 publish, no M7 topic, no service call. MUST-9 is a log stub.
- [stub] cost_colreg / cost_dist / cost_vel fields in MidMpcSolution @ src/l3_tdl_kernel/m5_tactical_planner/src/mid_mpc/mid_mpc_nlp_formulation.cpp:296-298 // Explicitly commented as Phase E1 deferred: 'They remain zero-init.' compose_rationale_ emits 'cost_colreg=0' in rationale string (mid_mpc_waypoint_generator.cpp:139), giving appearance of populated data in logs.
- [stub] Sea-state input to VesselDynamicsModel (hs_m hardcoded to 0.0) @ src/l3_tdl_kernel/m5_tactical_planner/src/mid_mpc/mid_mpc_node.cpp:208 // Comment: '[TBD-HAZID] sea state from EnvironmentState'. ROT_max computation uses hs_m=0.0 always, meaning rough_sea_factor is never applied. Acknowledged placeholder.
- [stub] M4 consecutive-failure counter in BC-MPC (mid_mpc_consecutive_failures hardcoded 0) @ src/l3_tdl_kernel/m5_tactical_planner/src/bc_mpc/bc_mpc_node.cpp:155 // Comment: 'Phase E1; Phase E2 reads shared state'. BC-MPC urgency computation receives inp.mid_mpc_consecutive_failures=0 always.
- [stub] [M5DIAG] debug residual logging in mid_mpc_solver.cpp — marked temporary @ src/l3_tdl_kernel/m5_tactical_planner/src/mid_mpc/mid_mpc_solver.cpp:24-65 // Comment: 'temporary, revert before merge'. Diagnostic logging for ongoing D1 keystone investigation. Not a stub but a temp intrusion in production binary.

## progress.md OVERCLAIM 修正
- 声称: D3.2 Closed in ✅ 2026-05-25: 'M5 双 MPC 完整实装（2751 LOC src + 2218 LOC test）' — dual MPC fully implemented
    实际: BC-MPC (the second MPC) exists as compilable code but is not launched, not wired to the bridge, subscribes to wrong topic namespace (/m2/world_state vs /l3/m2/world_state), and /m5/reactive_override_cmd has zero consumers. In the running A4000 SIL system, BC-MPC contributes nothing. 'Implemented' ≠ 'integrated and operational'.
    证据: m5_mid_mpc.launch.py:11-21 (no m5_bc_mpc_node); bc_mpc_node.cpp:35 (/m2/world_state); docker/sil_topic_bridge.py (zero matches for reactive_override_cmd)
- 声称: DEMO-2 阻塞 ✅ 已解除: '/sil/sat3_data.trajectory_candidates[] @2Hz 已实装'
    实际: sat3_data IS published but at 1 Hz (solve_timer_ = std::chrono::seconds(1), mid_mpc_node.cpp:88-92), not the claimed 2 Hz. Additionally tc.rule_compliant is hardcoded false and tc.confidence hardcoded 1.0F for all candidates regardless of COLREGs compliance.
    证据: mid_mpc_node.cpp:88-92 (std::chrono::seconds(1)); mid_mpc_node.cpp:437, 441 (rule_compliant=false, confidence=1.0F hardcoded)
- 声称: D0.1 MUST-9 Surgical fix ✅ Closed: 'MRM 走 M7 路径'
    实际: MRM escalation is a spdlog::critical() log call only. No ROS2 message to M7 MRM topic is published on consecutive NLP failures. M7 cannot react to M5 escalation signals because no signal is sent.
    证据: mid_mpc_solver.cpp:169-170, 207-210

## 疑似断流 (flow_gaps_suspected)
- BC-MPC → L4 Guidance via /m5/reactive_override_cmd | BC-MPC node never started; /m5/reactive_override_cmd has zero publishers in the running system. The entire short-horizon emergency avoidance layer is disconnected. :: m5_mid_mpc.launch.py (no bc_mpc_node); docker/sil_topic_bridge.py (no subscription to reactive_override_cmd)
- M5 consecutive NLP failures → M7 MRM-02 trigger | After 5 consecutive IPOPT failures, M5 logs spdlog::critical but publishes nothing. M7 has no way to detect this condition and cannot escalate to MRM. The MRM escalation path is broken at its source. :: mid_mpc_solver.cpp:207-210 (log only); no ROS2 publisher for M7 topic in mid_mpc_node
- M1 ODD state → M5 solve inhibit | M5 runs its full IPOPT solve cycle unconditionally. If ODD transitions to OUT-of-ODD or CRITICAL, M5 continues publishing avoidance_plan with no knowledge of ODD status. ADR-1 gate is absent. :: mid_mpc_node.cpp:34-93 constructor — no sub_odd_; on_solve_cycle_() checks only has_required_inputs_() (world_state + behavior_plan + colregs_constraint), not ODD
- X-axis Checker VETO → M5 output suppression | No subscription to checker veto in either Mid-MPC or BC-MPC node. A VETO from the X-axis Deterministic Checker is not acted upon by M5; the plan pipeline continues outputting unchecked. :: mid_mpc_node.cpp and bc_mpc_node.cpp — grep for veto returns zero matches

## 对抗验证裁决 (本模块)
- [CRITICAL] BC-MPC node never launched — entire short-horizon layer is dead in production => CONFIRMED(HIGH): All three cited evidence paths check out in current (non-salvage) code. (1) m5_mid_mpc.launch.py contains exactly one Node entry: executable="m5_mid_mpc_node" — no BC-MPC entry. (2) docker/sil_entrypoint.sh line 284, the l3_launch_specs list that drives every L3 subprocess launch, includes only ('m5
- [HIGH] BC-MPC subscribes to wrong world_state namespace (/m2/ instead of /l3/m2/) => CONFIRMED(HIGH): Current on-disk bc_mpc_node.cpp line 35 subscribes to "/m2/world_state" (missing the "/l3/" prefix). Mid-MPC uses the correct "/l3/m2/world_state" on line 50 of mid_mpc_node.cpp. The launch file (full_l3_stack.launch.py lines 36-41) registers m5_bc_mpc_node with no remappings= argument. Since M2 pub
- [HIGH] AvoidanceWaypoint CMM fields (schema_version, stamp, confidence, rationale) unpopulated in NLP success path => PARTIALLY_CONFIRMED(HIGH): The core claim is confirmed: in the NLP success path, `build_waypoints_` (mid_mpc_waypoint_generator.cpp lines 87–129) never assigns `wp.schema_version`, `wp.stamp`, `wp.confidence`, or `wp.rationale` on any `AvoidanceWaypoint`. These fields stay at their ROS2 default-constructed zero values (schema
- [HIGH] cost_colreg / cost_dist / cost_vel always zero — COLREGs cost term invisible in rationale => CONFIRMED(HIGH): All three fields (cost_colreg, cost_dist, cost_vel) are zero-initialized in MidMpcSolution (types.hpp:143-145) and are never assigned in unpack_solution() (mid_mpc_nlp_formulation.cpp:340-392). The formulation file contains an explicit self-acknowledging comment at lines 337-338 confirming this is a

## 相关 flow 边裁决
- [HIGH] M2 -> M5: /l3/m2/world_state (target trajectory for MPC) = PARTIAL: Mid-MPC correctly consumes /l3/m2/world_state and uses targets for MPC formulation. BC-MPC is silently starved because the topic string '/m2/world_state' does not exist on the live bus (live list shows '/l3/m2/world_state' only). This matches the phase-1 finding 'BC-MPC subscribes to wrong world_state namespace'.
- [HIGH] M1 -> M5: speed/trajectory constraints (how delivered?) = BROKEN_NO_SUBSCRIBER: Design intent per ADR-1: M1 ODD state should gate M5 solve or supply envelope constraints. Actual code: M5 derives all constraints from M4 behavior_plan only.
- [MEDIUM] M6 -> M5: /l3/m6/colregs_constraint = PARTIAL: M6 heading direction reaches M5 only indirectly via M4 IvP → behavior_plan window. The M6→M5 direct path carries only binary encounter flag and target-ID for weight scaling.
- [HIGH] M6 -> M5: /l3/m6/rule_assessment = BROKEN_NO_SUBSCRIBER: Edge documented in phase-1 maps as gap. Confirmed: M5 NLP cost tuning cannot be rule-aware because rule_assessment never arrives at M5.
- [HIGH] M3 -> M1/M5/bridge: /l3/m3/mission_state = BROKEN_NO_PUBLISHER: M3's internal MissionStateMachine tracks state (MissionState enum) but never publishes it as a ROS2 topic. M1 subscribes to /l3/m3/mission_state (per phase-1 maps) but data does not originate from M3.
- [CRITICAL] M5 -> M7: /l3/m5/avoidance_plan (for checking) = PARTIAL: All six HC functions are dead code stubs per consolidated findings. The plan content arrives at M7 but the scalars extracted from it are zeroed before any check runs. The safety loop M5->M7->gate is structurally broken.
- [HIGH] M5 -> bridge -> L4: /l3/m5/avoidance_plan -> /sil/actuator_cmd (bridge translates plan->actuator) = MOCK_INTERCEPTED: The bridge is the authoritative actuator controller, not L4. M5's waypoints inform turn_radius_m as a fallback rudder angle only if no M4 avoidance target heading is available. The CPA release logic (line 834-865) is bridge-local, not sourced from M2 or M6.

## completeness-critic 相关补充
- [HIGH/FLOW_GAP] fsm_aggregator_node: /l3/m3/mission_state and /l3/m8/operator_state not subscribed :: docker/fsm_aggregator_node.py:116-123 — FsmAggregatorNode subscribes only to /l3/m1/odd_state, /l3/m4/behavior_plan, /l3/m5/avoidance_plan, /l3/m7/safety_alert. It does NOT subscribe to /l3/m3/mission
- [HIGH/MOCK] shell_b_harness: TrackedTarget.cpa_m and tcpa_s hardcoded 0.0 — M5/M6/M2 receive zero CPA :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:472-473 — In ShellBSimulator.step() every published TrackedTarget has tgt.cpa_m = 0.0 and tgt.tcpa_s = 0.0. These are published directly 
- [MEDIUM/LEAKED_LOGIC] shell_b_harness: duplicate autopilot implementation diverges from bridge autopilot :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:74-107, 539-593 — ShellBSimulator contains its own HeadingController and SpeedController identical in structure to those in docker/sil_to
- [HIGH/FLOW_GAP] ScoringNode: /l3/colregs_active subscription has no publisher anywhere in the codebase :: src/sim_workbench/sil_nodes/scoring/scoring/node.py:72 — ScoringLifecycleNode.on_configure() subscribes to std_msgs/String on '/l3/colregs_active'. The _colregs_cb() at line 132 parses 'Rule14:ok,Rule
- [HIGH/DESIGN_IMPL_DESYNC] shell_b_harness doer_composition: M3 and M7 not included — scoring and safety gate both absent from RL loop :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:368-383 — doer_cmd includes m1_odd_manager, m2_world_model, m4_behavior_arbiter, m5_tactical_planner, m8_hmi_bridge. M3 (mission_manager_