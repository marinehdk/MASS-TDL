# M7 审计证据片（来自 2026-06-08 系统审计 @158bba9d；当前代码请用 codegraph 复核）

role: Doer-Checker (Checker role): IEC 61508 + SOTIF dual-track safety watchdog over M1-M6. Publishes /l3/m7/heartbeat, /l3/m7/safety_alert. Per spec also supposed to publish /l3/checker/veto (CheckerVetoNotification) as a hard gate preventing M4/M5 from executing un-checked plans.

## 实际订阅 (subscriptions)
- /l3/m1/odd_state [USED] <l3_msgs/ODDState> src=M1 ODD Envelope Manager :: safety_supervisor_node.cpp:129-132 — stored to last_odd_, fed to watchdog kM1 + assumption_monitor + mrm_selector
- /l3/m2/world_state [USED] <l3_msgs/WorldState> src=M2 World Model :: safety_supervisor_node.cpp:134-137 — stored to last_world_, fed to watchdog kM2 + fault_monitor + performance_monitor
- /l3/m4/behavior_plan [DECLARED_UNUSED] <l3_msgs/BehaviorPlan> src=M4 Behavior Arbiter :: safety_supervisor_node.cpp:285-290 — msg parameter is /*msg*/ (discarded); only watchdog kM4 heartbeat tick is recorded; plan content never inspected
- /l3/m5/avoidance_plan [USED] <l3_msgs/AvoidancePlan> src=M5 Tactical Planner :: safety_supervisor_node.cpp:292-305 — stored to last_avoidance_; triggers run_hard_constraint_checks() but that function is a no-op stub (line 548-552)
- /l3/m6/colregs_constraint [USED] <l3_msgs/COLREGsConstraint> src=M6 COLREGs Reasoner :: safety_supervisor_node.cpp:308-344 — stored to last_colregs_, rule_id mapped to ColregsRule enum for HC-2; fed to assumption_monitor + fault_monitor
- /l3/checker/veto [USED] <l3_external_msgs/CheckerVetoNotification> src=X-axis Deterministic Checker (external) :: safety_supervisor_node.cpp:174-178 — M7 SUBSCRIBES to /l3/checker/veto; forwards to veto_handler + checker_veto_counter for rate tracking; but NO production node publishes this topic (only mock publisher at /checker/veto_notification with wrong topic name)
- /l3/m4/reactive_override_cmd [USED] <l3_msgs/ReactiveOverrideCmd> src=M4 :: safety_supervisor_node.cpp:167-172 — watchdog kM3 heartbeat tick only
- /l3/reflex/activation [USED] <l3_external_msgs/ReflexActivationNotification> src=Y-axis Reflex Arc :: safety_supervisor_node.cpp:181-186 — sets reflex_freeze_required_ flag
- /l3/override/active [USED] <l3_external_msgs/OverrideActiveSignal> src=HMI/external :: safety_supervisor_node.cpp:188-193 — sets override_active_, triggers resume_handler
- /l3/safety/concern [USED] <l3_msgs/SafetyConcernEvent> src=M1 (W9 watchdog) :: safety_supervisor_node.cpp:195-200 — only logs a warning on 'M3_route_stale_watchdog'; no action taken

## 实际发布 (publications)
- /l3/m7/heartbeat [REAL] unpop=[schema_version (Header has none — but spec CMM contract requires it; SafetyAlert fields are the concern here)] :: safety_supervisor_node.cpp:221-223 pub_heartbeat_ created; on_heartbeat_tick() at 10Hz sets stamp+frame_id and publishes (lines 526-530)
- /l3/m7/safety_alert [PARTIAL_FIELDS] unpop=[schema_version (always zero — no assignment in alert_generator.cpp or safety_arbitrator.cpp)] :: safety_supervisor_node.cpp:209-211 pub_alert_ created; published in run_monitor_evaluation (lines 461-468) and publish_hard_constraint_alert (554-563). Fields stamp, severity, confidence, rationale, description, recommended_mrm ARE populated via AlertGenerator::build_safety_alert (alert_generator.cpp:17-30). schema_version field is NEVER set (zero). Alert only published when severity > SEVERITY_INFO — nominal state emits nothing.
- /l3/checker/veto [MISSING] unpop=[] :: M7 node header (safety_supervisor_node.hpp:68-71) lists only pub_alert_, pub_asdr_, pub_sat_, pub_heartbeat_. No CheckerVetoNotification publisher exists anywhere in production C++ code. The spec (M7-spec.md:31) says M7 should publish CheckerVetoNotification to M4/M5. The bridge subscribes to /l3/checker/veto for debug trace only (sil_topic_bridge.py:476-479).
- /l3/asdr/record [REAL] unpop=[] :: safety_supervisor_node.cpp:213-215; on_asdr_periodic_tick() at 2Hz publishes SHA-256 signed record (lines 495-517)
- /l3/sat/data [PARTIAL_FIELDS] unpop=[sat3 forecast fields (hardcoded empty),sat2.reasoning_chain (empty for all periodic ticks)] :: safety_supervisor_node.cpp:217-219; on_sat_tick() at 10Hz publishes SAT data. sat3 forecast fields left at default per comment in alert_generator.cpp:75
- /sil/sotif_metrics [STUB] unpop=[violation_score (zeroed in stub mode),window_count (zeroed in stub mode),raw_value (zeroed in stub mode)] :: sotif_metrics_publisher.cpp:29-37 — stub_mode_ defaults to true (sotif_metrics_publisher.hpp:21). When stub_mode_ is true, violation_score, window_count, raw_value are all hard-zeroed regardless of actual AssumptionStatus. set_stub_mode(false) is never called in any production code (grep confirms only one caller: the setter itself). Progress doc (M7-progress.md:21) claims 'M7 /sil/sotif_metrics @10Hz 已实装' — this is an overclaim; the topic publishes all-zero metric values.

## 设计职责实现状态 (responsibilities)
- [STUB] HC-1: CPA minimum distance hard constraint (<10ms end-to-end) :: core/hard_constraint_cpa.cpp implements compute_cpa_m7() and check_cpa_consistency() with real geometry. mrm_chain_executor.cpp:18 defines build_safety_alert_from_hard_constraints(). But run_hard_constraint_checks() at safety_supervisor_node.cpp:548-552 is: (void)now; } — a complete no-op. None of the six HC functions are called from any production path. 
- [MISSING] HC-2: UKC (under-keel clearance) hard constraint :: No UKC check function exists anywhere in m7_safety_supervisor/src. The spec lists it as HC-2 but there is no hard_constraint_ukc.cpp/hpp in the file tree. mrm_chain_executor.cpp does not reference UKC either. 
- [STUB] HC-3: ROT upper limit hard constraint :: core/hard_constraint_rot.cpp implements check_rot_limit() with real arithmetic and 5% tolerance. mrm_chain_executor references it in build_safety_alert_from_hard_constraints. But run_hard_constraint_checks() never calls it — dead code. 
- [STUB] HC-4: Speed upper limit hard constraint :: core/hard_constraint_speed.cpp implements check_speed_limit(). Same as HC-3 — function exists, never invoked from production path. 
- [MISSING] HC-5: ODD boundary check :: No ODD-boundary hard constraint function in m7_safety_supervisor (M1 does ODD scoring, but M7 spec requires an independent check). mrm_chain_executor has no ODD boundary entry. 
- [PARTIAL] HC-6: MRM trigger condition :: MrmSelector.select() + MrmChainExecutor.build_safety_alert_from_hard_constraints() define MRM logic. MrmSelector is called in run_monitor_evaluation (line 452). But MRM is selected based on SOTIF/IEC assumption scores, not on direct HC violations — the HC inputs (CPA, ROT, speed) are never fed to MrmSelector because run_hard_constraint_checks is a no-op. 
- [REAL] IEC 61508 watchdog (HC-watchdog: M1-M6 heartbeat loss detection) :: iec61508/watchdog_monitor.cpp is real; WatchdogMonitor.evaluate() called in run_monitor_evaluation (line 425). Watchdog kM1..kM6 ticked on every subscription callback. Result fed to SafetyArbitrator and can produce SEVERITY_CRITICAL or SEVERITY_MRC_REQUIRED alert. 
- [PARTIAL] SOTIF assumption violation detection (5 classes per spec) :: sotif/assumption_monitor.cpp implements AIS/radar consistency, motion predictability, perception coverage, COLREGs solvability. CommLink assumption (class 5: RTT/packet loss) is hardcoded to CommLinkState{} (zero) per node comment at line 432: '[TBD-comm-monitor] rtt_s and packet_loss_pct are zero until comm monitor integrated'. Veto rate feeds assumption #6 (kCheckerVetoRate) but /l3/checker/veto has no live publisher so rate is always 0. 
- [PARTIAL] Doer-Checker triple quantification matrix (LOC ≥50:1, CC ≥30:1, SBOM ∩ = ∅) :: SBOM independence enforced by lint check in checker_verification.py:66-86 (no OR-Tools in M7). LOC/CC ratios not verified by any CI tool found in the repository — spec claims D3.3a complete (M7-progress.md:13), but no automated CI job for these ratios is visible. 
- [MISSING] CheckerVetoNotification publisher → M4/M5 as hard gate (spec:31) :: M7 has zero CheckerVetoNotification publishers. M7 only SUBSCRIBES to /l3/checker/veto. M4 and M5 source code contain no subscription to /l3/checker/veto and no safety_alert gate. No live node publishes /l3/checker/veto (mock publishes to wrong topic /checker/veto_notification). 
- [MISSING] FMEDA M7 table ≥20 failure modes :: M7-spec.md:67 marks FMEDA as 🔴 未做. M7-progress.md:13 marks D3.3a as ✅ with 'FMEDA M7 v1.0' — contradiction. No FMEDA file found under docs/Design/Phase 3/D3.3a-m7-core/ (not directly readable but spec status is authoritative). 
- [MISSING] PATH-S CI 0-violation automated check :: M7-spec.md:68 marks as ⚫ 未验. Progress doc claims D3.3a ✅ (PATH-S CI通过) but spec still marks unverified. No CI config referencing m7 PATH-S found. 
- [REAL] ASDR SHA-256 signed audit trail :: common/sha256.cpp implements SHA-256; alert_generator.cpp:44-47 applies digest to decision_json. Published at 2Hz via on_asdr_periodic_tick. 
- [REAL] ResumeHandler after override/MRC deactivation :: core/resume_handler.cpp is non-trivial; called in on_override_signal() and revert_from_override(). mrm_selector_->reset() called on override exit. 
- [REAL] MRM chain: 4 MRM types (drift/anchor/heave-to/mooring) :: mrm/ subdirectory: mrm_01_drift.cpp through mrm_04_mooring.cpp each implement can_execute()/execute() with real precondition checks. MrmSelector.select() uses world state. 
- [MISSING] CMM three-interface: current_state()/rationale()/forecast() :: M7 node exposes no current_state(), rationale(), or forecast(Δt)+uncertainty() ROS2 service or topic. SAT data (pub_sat_) provides a partial rationale proxy but is not the CMM contract interface that M8 should aggregate. 

## 设计-实现偏离 (deltas)
- [CRITICAL] D3 (unverified): /l3/checker/veto is NEVER published — M7 has NO veto authority
    设计: Spec (M7-spec.md:31): M7 publishes CheckerVetoNotification to M4/M5 as a synchronous hard gate — 'forced withdrawal to nominal'. ADR-2: Doer-Checker with VETO right.
    实现: M7 node has exactly four publishers: pub_alert_, pub_asdr_, pub_sat_, pub_heartbeat_ (safety_supervisor_node.hpp:68-71). No CheckerVetoNotification publisher exists. M7 only SUBSCRIBES to /l3/checker/veto to count incoming vetoes. M4/M5 have no subscription to /l3/checker/veto and no safety_alert gate, so no downstream gating exists either. The bridge subscribes for debug trace only (sil_topic_bridge.py:476-479).
    证据: safety_supervisor_node.hpp:68-71 (publisher list); grep across all production .cpp/.py returns zero create_publisher calls for CheckerVetoNotification except the mock at wrong topic /checker/veto_notification (external_mock_publisher.py:58); checker_verification.py:89-120 verify_checker_veto_topic() silently passes when topic absent or DDS empty
- [CRITICAL] All six HC functions are dead code — run_hard_constraint_checks is a no-op stub
    设计: Spec: 6 hard constraints (CPA, UKC, ROT, speed, ODD boundary, MRM trigger) must be evaluated in <10ms on every avoidance plan arrival.
    实现: run_hard_constraint_checks() at safety_supervisor_node.cpp:548-552 body is: `(void)now;` — exactly one statement that discards its argument. The six HC implementation functions (check_cpa_consistency, check_rot_limit, check_speed_limit, check_colregs_geometry, evaluate_dc_constraint, evaluate_watchdog_constraint) and the alert builder (build_safety_alert_from_hard_constraints) exist in their source files but are called ONLY from unit tests, never from production paths. publish_hard_constraint_alert() (lines 554-563) is also dead code with no callers.
    证据: safety_supervisor_node.cpp:548-552; grep for check_rot_limit/check_speed_limit/compute_cpa_m7/evaluate_dc_constraint in src/ (excluding test) returns only their own definition files
- [HIGH] HC-2 UKC (under-keel clearance) constraint is completely absent from implementation
    设计: Spec §HC list item 2: UKC (under-keel clearance) is a hard constraint.
    实现: No hard_constraint_ukc.cpp/hpp exists. mrm_chain_executor.cpp handles HC-1 CPA, HC-2 COLREGs, HC-3 watchdog, HC-4 DC, HC-5 speed, HC-6 ROT — mapping is shifted: spec's HC-2 UKC is silently absent, replaced by HC-2 COLREGs geometry. UKC is never computed, thresholded, or alarmed anywhere in M7.
    证据: File tree from codegraph_files shows no *ukc* file under m7_safety_supervisor; mrm_chain_executor.cpp function signature covers CPA/colregs/speed/rot/watchdog/dc (6 args, none is UKC)
- [HIGH] /sil/sotif_metrics publishes all-zeros in stub_mode (default, never disabled)
    设计: M7-progress.md:21 claims 'M7 /sil/sotif_metrics @10Hz 已实装; 前端SotifMonitorStrip数据源就绪'. Spec:32: 6-metric aggregation for DEMO-2.
    实现: sotif_metrics_publisher.hpp:21 `bool stub_mode_{true}` — default is stub. In stub mode (sotif_metrics_publisher.cpp:29-37) violation_score, window_count, raw_value are all set to 0.0F/0 regardless of actual AssumptionStatus. set_stub_mode(false) has exactly one definition (line 43) and zero call sites in production code. Front-end SotifMonitorStrip always receives all-zero metrics.
    证据: sotif_metrics_publisher.hpp:21; sotif_metrics_publisher.cpp:29-37; grep for set_stub_mode returns only its own definition
- [HIGH] CommLink SOTIF assumption permanently zeroed — assumption #5 never evaluated
    设计: Spec: SOTIF assumption class 5 = comms link RTT >2s / packet loss >20%.
    实现: safety_supervisor_node.cpp:432: `sotif::CommLinkState const kCommLink{};` — zero-initialized struct (rtt_s=0, packet_loss_pct=0) passed to assumption_monitor->evaluate() every tick. In-code comment: '[TBD-comm-monitor] rtt_s and packet_loss_pct are zero until comm monitor integrated. Blocker: medium.' The assumption is structurally always non-violated.
    证据: safety_supervisor_node.cpp:429-437
- [MEDIUM] on_behavior_plan discards message content — M4 behavior not inspected
    设计: Spec: M7 subscribes to M4 BehaviorPlanMsg to monitor Doer outputs for constraint violations.
    实现: on_behavior_plan() parameter is `const& /*msg*/` — payload discarded. Only the watchdog heartbeat tick for kM4 is recorded. M7 never checks whether M4's heading window, speed command, or behavior enum violates any HC.
    证据: safety_supervisor_node.cpp:285-290
- [MEDIUM] SafetyAlert schema_version always zero — CMM field violation
    设计: Architecture §3 (CLAUDE.md): all messages must carry `schema_version`. M4 behavior_plan publishes schema_version=113 (observed live). M7 safety_alert is consumed by M1 and M8.
    实现: alert_generator.cpp builds SafetyAlert with stamp/severity/confidence/rationale/description/recommended_mrm but never sets schema_version. SafetyArbitrator::arbitrate() also does not set it. The field defaults to zero (ROS2 message default).
    证据: alert_generator.cpp:17-30 (no schema_version line); safety_arbitrator.cpp (no schema_version line); grep for schema_version in m7 src/ returns zero production matches
- [MEDIUM] Gate-6 veto latency test hardcoded PASS stub
    设计: Gate-6 Doer-Checker Independence must verify veto round-trip latency <50ms.
    实现: checker_verification.py:123-125 run_veto_latency_test() returns `True, 'VETO latency test: Phase 2 (real M5/M7 ROS2 nodes not yet deployed)'` — unconditional hardcoded pass, no measurement performed.
    证据: checker_verification.py:123-125
- [LOW] on_avoidance_plan zeroes extracted scalars immediately after storing plan
    设计: M7 should extract speed/ROT/heading-change/DCPA from avoidance plan to feed HC checks.
    实现: safety_supervisor_node.cpp:298-301: `last_avoidance_speed_ = 0.0F; last_avoidance_rot_ = 0.0F; last_avoidance_heading_change_ = 0.0F; last_avoidance_dcpa_ = 0.0F;` — these fields are zeroed every time the plan arrives rather than extracted from the plan. As run_hard_constraint_checks is already a no-op, the HC inputs would be permanently zero even if the stub were replaced.
    证据: safety_supervisor_node.cpp:297-301

## MOCK / STUB
- [MASQ-伪装真实] run_hard_constraint_checks() — complete no-op stub @ src/l3_tdl_kernel/m7_safety_supervisor/src/safety_supervisor_node.cpp:548-552 // Called from on_avoidance_plan() as if it performs <10ms HC evaluation. Body is `(void)now; }`. The function signature, call site, and code comments all imply it is operational. The six HC implementation functions and build_safety_alert_from_hard_constraints exist but are dead code callable only from unit tests.
- [MASQ-伪装真实] SotifMetricsPublisher stub_mode=true (default, permanent) @ src/l3_tdl_kernel/m7_safety_supervisor/include/m7_safety_supervisor/sotif/sotif_metrics_publisher.hpp:21 // Publishes to /sil/sotif_metrics at correct frequency with correct schema_version=113 and is_violated flags, but violation_score/window_count/raw_value are all hard-zeroed. Frontend SotifMonitorStrip receives structurally valid but semantically empty data. Progress doc claims 'already deployed'.
- [stub] CommLinkState{} zero-init in run_monitor_evaluation @ src/l3_tdl_kernel/m7_safety_supervisor/src/safety_supervisor_node.cpp:432-437 // Honest TBD comment in code: '[TBD-comm-monitor]'. The assumption monitor's comm-link arm is structurally wired but always receives zero input. Not masquerading — comment is explicit.
- [MASQ-伪装真实] run_veto_latency_test() hardcoded pass @ src/sil_orchestrator/checker_verification.py:123-125 // Returns True unconditionally with Phase-2 deferral reason. Gate-6 reports 5/5 checks passed but veto latency is never measured.
- [stub] publish_hard_constraint_alert() — unreachable dead code @ src/l3_tdl_kernel/m7_safety_supervisor/src/safety_supervisor_node.cpp:554-563 // Has a real publish body but has zero callers in production paths. Only run_hard_constraint_checks() would call it, and that function is a no-op stub.
- [stub] build_safety_alert_from_hard_constraints() — defined, never called in production @ src/l3_tdl_kernel/m7_safety_supervisor/src/core/mrm_chain_executor.cpp:18 // Real logic for all six HC violation conditions, real alert construction. But no production call site exists. Called only from unit tests.

## progress.md OVERCLAIM 修正
- 声称: D3.3a Closed in ✅ 2026-05-25: M7-core: 6硬约束 + FMEDA M7 v1.0 + MRM chain + ResumeHandler + PATH-S CI通过 (M7-progress.md:13)
    实际: 6 hard constraint FUNCTIONS exist in source files but are completely disconnected from the runtime evaluation path. run_hard_constraint_checks() is a no-op stub. HC-2 UKC has no implementation at all. FMEDA M7 is listed as 🔴 未做 in M7-spec.md:67 — a direct contradiction with the progress doc. PATH-S CI is marked ⚫ 未验 in spec:68.
    证据: safety_supervisor_node.cpp:548-552 (HC stub); M7-spec.md:67-68 (🔴/⚫ status); no hard_constraint_ukc.* file in codegraph
- 声称: D3.3b Closed in ✅ 2026-05-25: SotifMetricsPublisher 已发布 @10Hz (M7-progress.md:14 + M7-progress.md:21)
    实际: SotifMetricsPublisher publishes at correct frequency but in permanent stub_mode (default true, never disabled). All violation_score/window_count/raw_value fields are hard-zeroed. The progress doc says 'DEMO-2 blocking already resolved' — frontend strip shows all-zero metrics.
    证据: sotif_metrics_publisher.hpp:21 (stub_mode_{true}); sotif_metrics_publisher.cpp:29-37; grep confirms set_stub_mode() never called in production
- 声称: DEMO-2阻塞贡献 ✅ 已解除: M7 /sil/sotif_metrics @10Hz 已实装 (M7-progress.md:21)
    实际: Topic publishes at 10Hz with correct structure, but actual metric values are all zeros due to permanent stub_mode. The DEMO-2 frontend data source is structurally present but semantically empty — the 'unblocking' claim is premature.
    证据: sotif_metrics_publisher.cpp:29-37

## 疑似断流 (flow_gaps_suspected)
- /l3/checker/veto: nobody publishes → M7 subscribes (and M4/M5 never gate) | M7 subscribes to /l3/checker/veto at safety_supervisor_node.cpp:174-178 and counts veto rate via VetoHandler + CheckerVetoCounter. The live mock publisher uses wrong topic /checker/veto_notification (external_mock_publisher.py:58). No production C++ node publishes /l3/checker/veto. CheckerVetoCounter.current_rate() is always 0 — the SOTIF assumption kCheckerVetoRate is permanently non-violated. Gate-6 checker_veto topic check (checker_verification.py:112-117) silently passes when DDS topic list is empty. :: external_mock_publisher.py:58 (wrong topic); safety_supervisor_node.hpp:68-71 (no veto publisher in M7); M4/M5 source — no /l3/checker/veto subscription found
- /l3/m7/safety_alert → M5/bridge: M5 ignores it, bridge has no execution gate | M1 and M8 consume /l3/m7/safety_alert. M5 tactical planner and the docker bridge have no subscription to /l3/m7/safety_alert. When M7 raises SEVERITY_CRITICAL or SEVERITY_MRC_REQUIRED, M5 continues planning and the bridge continues forwarding actuator commands without pause. :: grep for safety_alert in m5_tactical_planner/src returns zero results; docker/sil_topic_bridge.py grep for safety_alert returns zero results; M1 consumes it (odd_envelope_manager_node.cpp:378-383, 507-510) for FSM state change but M1 → M4/M5 stop-command path not verified here
- HC scalar extraction from AvoidancePlan → HC functions: zeroed before use | on_avoidance_plan() stores last_avoidance_ (the full plan) but immediately zeroes last_avoidance_speed_, last_avoidance_rot_, last_avoidance_heading_change_, last_avoidance_dcpa_ (lines 298-301). These are the intended inputs to HC-3/4/5/6. Even if run_hard_constraint_checks is un-stubbed, all HC inputs will be zero unless the extraction logic is also added. :: safety_supervisor_node.cpp:297-301
- CommLink state monitor: no publisher, always zero input to AssumptionMonitor | SOTIF assumption #5 (comm RTT >2s / packet loss >20%) receives CommLinkState{rtt_s=0, packet_loss_pct=0} every cycle. No ROS2 node in the codebase publishes comm-link quality metrics that M7 could consume. The assumption is structurally wired but will never trip in any live scenario. :: safety_supervisor_node.cpp:432 (CommLinkState{}); no topic publishing RTT/packet_loss found in production src/

## 对抗验证裁决 (本模块)
- [CRITICAL] D3 (unverified): /l3/checker/veto is NEVER published — M7 has NO veto authority => CONFIRMED(HIGH): All four legs of the claim are verified against current (non-salvage) source:
1. M7 publisher list is exactly pub_alert_, pub_asdr_, pub_sat_, pub_heartbeat_ — no CheckerVetoNotification publisher exists (safety_supervisor_node.hpp:67-71; setup_publishers() at .cpp:207-224).
2. M7 only subscribes to
- [CRITICAL] All six HC functions are dead code — run_hard_constraint_checks is a no-op stub => CONFIRMED(HIGH): All evidence is confirmed in current (non-salvage) code. (1) run_hard_constraint_checks at safety_supervisor_node.cpp:548-552 has a single-statement body `(void)now;` — a complete no-op. (2) It IS called from production: on_avoidance_plan (safety_supervisor_node.cpp:304) invokes it on every incoming

## 相关 flow 边裁决
- [HIGH] M6 -> M7: /l3/m6/colregs_constraint = PARTIAL: Subscriber receives and parses the message. All downstream HC logic (HC-2 COLREGs geometry check) is dead code — the function body does nothing.
- [CRITICAL] M5 -> M7: /l3/m5/avoidance_plan (for checking) = PARTIAL: All six HC functions are dead code stubs per consolidated findings. The plan content arrives at M7 but the scalars extracted from it are zeroed before any check runs. The safety loop M5->M7->gate is structurally broken.
- [CRITICAL] M7 -> M1: /l3/checker/veto, /l3/m7/safety_alert (synchronous gate?) = BROKEN_NO_PUBLISHER: run_hard_constraint_checks at safety_supervisor_node.cpp:548-552 is literally: (void)now; return; — all six HC checks are dead. The veto publisher declared in architecture (X-axis hard VETO) has no implementation. /l3/m7/safety_alert -> M1 is soft-only with no downstream behavioral gate in M4/M5/bridge confirmed.
- [CRITICAL] M7/M8 -> /sil/sotif_metrics -> HMI (via foxglove) = SCHEMA_MISMATCH: Double failure: schema mismatch makes the data unusable at the HMI even if it arrived, AND the topic is absent from the live list suggesting it is not publishing in the current A4000 deployment.

## completeness-critic 相关补充
- [HIGH/FLOW_GAP] fsm_aggregator_node: /l3/m3/mission_state and /l3/m8/operator_state not subscribed :: docker/fsm_aggregator_node.py:116-123 — FsmAggregatorNode subscribes only to /l3/m1/odd_state, /l3/m4/behavior_plan, /l3/m5/avoidance_plan, /l3/m7/safety_alert. It does NOT subscribe to /l3/m3/mission
- [HIGH/DESIGN_IMPL_DESYNC] shell_b_harness doer_composition: M3 and M7 not included — scoring and safety gate both absent from RL loop :: src/sim_workbench/shell_b_harness/shell_b_harness/simulator.py:368-383 — doer_cmd includes m1_odd_manager, m2_world_model, m4_behavior_arbiter, m5_tactical_planner, m8_hmi_bridge. M3 (mission_manager_